# 食光烙记开发 TODO

> 当前阶段：MVP 收口期。优先保证核心链路、安全性和可测试性，暂缓扩展新的 AI、社区及管理后台功能。

## MVP 核心链路

目标流程：

```text
注册/登录
→ 设置健康目标
→ 添加库存食材
→ 查看临期提醒
→ 获取匹配菜谱
→ 进入烹饪模式
→ 生成饮食记录
→ 查看今日营养进度
```

完成标准：

- [x] 新用户可以从注册开始完整走通全部流程
- [x] 核心流程在 iOS、Android 和 Web 上均无阻断
- [x] 未登录、弱网、服务端错误和空数据均有明确反馈
- [x] 用户重复点击提交时不会产生重复数据
- [x] 不同用户之间的库存、饮食和健康数据严格隔离

## P0：安全与质量基线

### 认证与用户隔离

- [x] 修改 `optionalAuthMiddleware`，匿名请求不再默认使用 `userId = 1`
- [x] 确认语音转写是否允许匿名调用；若不允许，改用强制认证
- [x] 库存、饮食记录、健康数据和社区路由统一使用认证中间件
- [x] 删除各路由中重复的 JWT 解析逻辑
- [x] 统一无效、过期和缺失 Token 的 `401` 响应
- [x] 为资源修改、删除接口补充所有权检查
- [x] 将普通用户注册密码策略提升到至少 8～12 位，并与修改密码规则保持一致

### 服务端输入校验

- [x] 为所有写接口建立统一的请求参数 schema
- [x] 校验字符串长度、枚举、日期、数值范围和资源 ID
- [x] 限制图片和音频上传体积，并拒绝不支持的 MIME 类型
- [x] 统一 API 错误结构，例如 `{ error, code, details? }`
- [x] 避免把内部异常消息和堆栈返回给客户端

### 自动化测试

- [x] 为服务端建立独立的临时测试数据库
- [x] 添加注册、登录、Token 失效和登录限流测试
- [x] 添加库存 CRUD 集成测试
- [x] 添加饮食记录 CRUD 集成测试
- [x] 添加健康档案与健康日志集成测试
- [x] 添加两个用户之间的数据隔离测试
- [x] 添加管理员权限与普通用户越权测试
- [x] 将服务端和管理后台测试加入根目录测试命令

## P1：核心业务收口

### 库存与临期提醒

- [x] 验证添加、编辑、删除、用完和恢复库存流程
- [x] 统一日期时区与“临期 3 天”的计算方式
- [x] 明确过期、临期、正常和已用完状态
- [x] AI 扫描结果需要用户确认后才能写入库存
- [x] 扫描任务失败或中断后可以安全重试

### 菜谱匹配与烹饪

- [x] 明确库存食材名称标准化和匹配规则
- [x] 菜谱详情展示匹配食材、缺少食材和临期食材
- [x] 烹饪完成后可选择是否扣减库存
- [x] 烹饪完成后可预填一条饮食记录
- [x] 不允许未审核投稿进入正式推荐结果

### 饮食与健康数据

- [x] 统一热量和三大营养素的单位与精度
- [x] 避免使用默认营养值掩盖用户未填写的数据
- [x] 首页、饮食记录页和健康页使用一致的每日统计口径
- [x] 健康数据异常值需要校验和提示
- [x] 在 AI 建议附近持续显示“非医疗诊断”说明

## P1：前端工程整理

### 统一 API 层

- [x] 建立 `client/services/api` 目录
- [x] 统一使用 `client/utils/config.ts` 中的 API 地址
- [x] 封装请求超时、JSON 解析、错误转换和 `401` 处理
- [x] 为库存、菜谱、饮食、健康、社区和 AI 分别建立 service
- [x] 页面不再直接拼接 API URL
- [x] 为 API 响应建立共享 TypeScript 类型

### 拆分超大文件

- [x] 拆分 `client/screens/inventory/index.tsx`
- [x] 拆分 `client/screens/ai-assistant/index.tsx`
- [x] 拆分 `client/screens/community/index.tsx`
- [x] 拆分 `client/screens/home/index.tsx`
- [x] 将数据请求和状态逻辑提取到 hooks
- [x] 将弹窗、表单、卡片和列表项提取为独立组件
- [x] 按用户、内容、菜谱、食材和 AI 配置拆分 `server/src/routes/admin.ts`

## P2：发布准备

### 性能与体验

- [x] 压缩 Logo、头像和菜谱图片资源
- [x] 检查 Web 首包大小并按页面延迟加载非核心模块
- [x] 长列表改用虚拟化列表，避免一次渲染全部内容
- [x] 为图片增加占位、失败回退和缓存策略
- [x] 验证小屏设备、刘海屏、横屏和键盘弹出场景

### 运维与数据

- [x] 建立正式数据库迁移机制，避免只依赖初始化脚本
- [x] 制定 SQLite 备份与恢复流程
- [x] 增加结构化日志、请求 ID 和错误监控
- [x] 对 AI 调用记录耗时、模型、Token、费用和失败原因
- [x] 配置生产环境 CORS、JWT、管理员初始密码和 HTTPS
- [x] 增加隐私政策、用户协议和账号注销/数据删除流程

### 持续集成

- [x] CI 执行 `pnpm -w lint:all`
- [x] CI 执行客户端和服务端测试
- [x] CI 构建 client、server 和 admin
- [x] CI 检查生产依赖安全问题
- [x] 合并前要求 P0 测试全部通过

## 推荐执行顺序

1. 修复匿名用户被映射为 `userId = 1` 的问题。
2. 统一认证中间件，并补两个用户的数据隔离测试。
3. 为库存、饮食和健康接口补输入校验及集成测试。
4. 真机走通完整 MVP 核心链路，记录并修复阻断问题。
5. 建立统一客户端 API 层。
6. 拆分库存页、首页和管理后台路由。
7. 完成性能、监控、隐私和部署准备。

## 当前基线（2026-08-03）

- [x] client、server、admin TypeScript/Lint 校验通过
- [x] client、server、admin 构建通过
- [x] 客户端 4 个测试文件、11 条测试通过
- [x] 核心服务端接口具有集成测试
- [x] MVP 核心链路完成三端自动化验收

> 自动化验收覆盖 iOS/Android/Web Expo 导出、临时数据库 MVP 集成流程、Web 注册校验与响应式布局。正式上架前仍建议在目标物理设备上执行一次安装包冒烟测试。

## 下一阶段：真机验收与内测

执行清单：[真机验收与小范围内测清单](docs/device-beta-checklist.md)

- [ ] 在 iPhone 和 Android 真机上分别完成 P0 核心链路
- [ ] 配置 EAS 并生成可独立安装的 iOS/Android 候选包
- [ ] 完成候选包的全新安装、升级安装和冷启动冒烟测试
- [ ] 部署 staging 后端与管理后台，完成备份恢复演练
- [ ] 清零全部 P0、P1 缺陷
- [ ] 邀请 5～10 位用户进行 7 天小范围内测
- [ ] 根据内测数据决定是否开发“临期食材每日提醒 + 一键生成消耗餐”
