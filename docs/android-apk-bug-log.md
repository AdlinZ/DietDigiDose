# Android APK 实机测试问题清单

> 本文档仅用于收集与整理实机测试反馈。在收到“开始修复”的明确指令前，不修改业务代码。

## B-1：实机食谱卡片缺少图片占位区域

- 状态：已修复；默认餐具图标已替换为中性食谱封面，待 Android APK 实机回归
- 类型：UI / 图片展示 / Android 实机差异
- 优先级：P2（总顺序 11）
- 记录日期：2026-08-13
- 测试端：Android APK 实机
- 现象：Android 实机的“膳食资产 → 食谱”列表中，当前截图可见的食谱卡片均未展示顶部图片区域，卡片直接从食谱标题开始。这里的核心问题是默认占位也没有显示，并不只是缺少真实菜品图片。
- 对照：Web 调试页面始终保留食谱卡片顶部的图片区域；食谱有图时展示菜品图，没图时也会展示带餐具图标的默认占位图。Android 实机在食谱没图时没有对应的占位区域。
- 期望：Android 实机与 Web 保持一致——食谱有图片时展示菜品图；食谱没有图片或图片加载失败时展示中性食谱封面，不能直接省略整个图片区域，也不应使用容易被误认为真实菜品的替代图。
- 复现路径：打开 Android APK → 进入“膳食资产” → 切换至“食谱”。
- 复现频率：从当前截图看，屏幕内所有可见卡片均出现；是否覆盖全部食谱尚待确认。
- 附件：
  - Android 实机截图：`codex-clipboard-a2c3270b-bc9a-4d82-a688-594239bfb710.jpg`
  - Web 对照截图：`codex-clipboard-34ff778e-b503-41df-a3f7-9cc442412e89.png`
- 备注：封面区域继续保留，以维持卡片高度和信息层级稳定；无图与加载失败统一使用不包含具体菜品和文字的中性空餐盘插画。

## B-2：食谱列表到底后无法正常继续加载，且两端均不支持触底自动加载

- 状态：已修复，待 Android APK 实机回归
- 类型：交互 / 分页加载 / Android 实机布局
- 优先级：P1（总顺序 3）
- 记录日期：2026-08-13
- 测试端：Android APK 实机、Web
- 表现一（Android 实机）：用户将食谱列表滑到底部后，继续加载控件位于底部导航栏后方，被遮挡而无法正常点击。截图底部只能隐约看到“已展示 12 / 32 道”等文字，未能正常操作继续加载。
- 表现二（Android 与 Web）：列表滑到底部后均不会自动加载下一批食谱；Web 仍需要点击“再看 12 道”，Android 对应的手动入口又因底部遮挡而无法点击。
- 期望：用户滑动到列表底部附近时自动加载下一页，并提供加载中、加载失败重试和全部加载完成状态；底部内容还应避开悬浮导航栏及设备安全区，保证所有控件可见、可点击。
- 复现路径：打开“膳食资产” → 切换至“食谱” → 持续下滑至当前列表底部。
- 复现频率：当前反馈为可复现；精确频率待确认。
- 附件：
  - Android 实机截图：`codex-clipboard-239c0635-0fa4-401a-b2eb-22b79488890f.jpg`
  - Web 对照截图：`codex-clipboard-d227bda3-8647-4d86-acb2-b9e091c6605d.png`
- 备注：截图表明 Web 当前采用“再看 12 道”的手动分页方式。Android 截图中的总数为 32，Web 对照中的总数为 372；这里只记录交互与遮挡现象，不根据数据量差异推断分页或数据源根因。

## B-3：社区内容详情页无法显示真实 IP 属地

- 状态：已修复并部署，线上烟测通过；待 Android APK 实机 UI 回归
- 类型：数据展示 / IP 属地
- 优先级：P2（总顺序 7）
- 记录日期：2026-08-13
- 测试端：Android APK 实机
- 现象：社区内容详情页的发布信息区域显示 `IP: 极简生活馆`，该内容是非地理文本，未显示用户发布时对应的真实 IP 属地。
- 期望：`IP:` 后展示由真实请求 IP 解析得到的规范属地信息；不应显示用户名称、机构名称或其他无关字段。若无法获得或解析属地，应采用明确且统一的缺省状态，而不是展示错误数据。
- 复现路径：打开 Android APK → 进入社区 → 打开一条内容详情 → 查看发布日期右侧的 `IP:` 信息。
- 复现频率：当前截图所示内容可复现；是否影响全部内容尚待确认。
- 附件：
  - Android 实机截图：`codex-clipboard-6c41e993-55f4-48cc-940d-1ceab5fdfab6.jpg`
- 备注：当前只确认页面展示值不是有效地理属地，不判断问题来自发布记录、接口字段映射、IP 获取、代理转发配置或 IP 归属地解析服务。

## B-4：发表评论时看不到评论输入框

- 状态：UI 修改已确认有效，但 Android 兼容性回归未通过，问题仍存在
- 类型：交互 / 键盘适配 / 评论输入
- 优先级：P1（总顺序 1）
- 记录日期：2026-08-13
- 补充记录日期：2026-08-14
- 测试端：Android APK 实机
- 现象：用户在帖子详情页点击底部评论输入入口后，评论框先于系统输入法弹出；输入法随后出现且页面内容被半透明遮罩覆盖，但评论输入框及发送操作区没有稳定地停靠在输入法上方。用户无法看到当前输入内容，也无法正常找到提交评论的操作入口。
- 期望：键盘弹出后，评论输入框和发送按钮应始终位于键盘上方的可见区域；输入内容应可见，且可正常取消或提交评论。
- 复现路径：打开 Android APK → 进入社区 → 打开帖子详情 → 点击底部“说点什么吧，与食友交流...”评论入口。
- 复现频率：当前截图所示场景可复现；精确频率待确认。
- 附件：
  - Android 实机截图：`codex-clipboard-89959c26-3c06-4459-81f2-259614437e91.jpg`
- 当前实现线索：现有流程先显示评论 `Modal`，再通过约 120ms 的固定延时聚焦输入框，由输入框触发系统键盘；弹层使用 `slide` 动画，并依赖 `KeyboardAvoidingView` 在 Android 上二次调整高度。固定延时不代表键盘已经完成显示，不同输入法、设备性能和厂商动画时长都可能造成时序与位置不一致。
- 修改思路：点击评论入口后先让输入控件获取焦点并请求系统键盘；Android 监听 `keyboardDidShow`，以事件返回的实际键盘坐标或高度作为评论框定位依据，确认键盘可见后再展示评论框，或让评论框与键盘联动动画并始终贴合键盘上沿。不得仅通过增加固定延时规避问题。
- 布局约束：无论系统采用窗口 `resize` 还是弹层独立窗口，评论框底部位置都应由当前可视区域与键盘实际边界计算；需要避免页面外层与弹层内层的多重键盘避让重复生效，并正确处理底部安全区、键盘切换、键盘收起和弹层关闭。
- 验证重点：覆盖首次打开、连续打开关闭、切换不同输入法、切换表情/数字键盘、添加评论图片、长文本输入和返回键收起键盘；确认评论框不闪跳、不被遮挡，发送与取消按钮始终可见可点击。

## B-5：无法发布带图片的帖子

- 状态：已修复，Android APK 实机确认帖子可以成功发布；后续流程问题见 B-27
- 类型：功能 / 媒体上传 / 发布配置
- 优先级：P1（总顺序 2）
- 记录日期：2026-08-13
- 测试端：Android APK 实机
- 现象：用户在“写动态”页面选择图片并点击发布后，帖子发布失败，弹窗提示“发布失败：媒体对象存储尚未配置”。因此当前无法正确发布带图片的帖子。
- 期望：带图片的帖子能够完成图片上传并成功发布；如媒体服务暂时不可用，应提供准确且可操作的错误提示，并避免让用户误以为内容已经发布。
- 复现路径：打开 Android APK → 进入社区发帖页“写动态” → 填写标题和正文 → 选择图片 → 点击“发布”。
- 复现频率：当前截图所示场景可复现；精确频率待确认。
- 附件：
  - Android 实机截图：`codex-clipboard-b469165f-12a3-42a6-9cdc-7a007b239879.jpg`
- 备注：错误信息直接指向“媒体对象存储尚未配置”，但当前阶段只记录外部表现，不检查或变更对象存储配置，也不判断具体缺失项。实机回归已确认帖子实际能够成功发布；发布后的页面状态、成功反馈及后续审核流程另记为 B-27。

## B-6：社区瀑布流卡片出现不等宽

- 状态：已修复，待 Android APK 实机回归
- 类型：UI / 双列布局 / Android 实机适配
- 优先级：P3（总顺序 16）
- 记录日期：2026-08-13
- 测试端：Android APK 实机
- 现象：社区“寻味”页面的双列瀑布流中，卡片会出现明显不等宽。同一屏幕内左列卡片整体宽于右列卡片；这不是瀑布流正常的内容高度差异，而是两列横向宽度不一致。
- 期望：双列瀑布流允许卡片高度随内容变化，但左右两列卡片应保持等宽，同时保持统一的左右页面边距和列间距。
- 复现路径：打开 Android APK → 进入“社区” → 选择“寻味” → 浏览双列内容卡片。
- 复现频率：当前截图所示场景可复现；是否受特定卡片内容、图片比例或无图卡片影响尚待确认。
- 附件：
  - Android 实机截图：`codex-clipboard-3168a41b-f8ca-4f9d-a9ed-44995db2b709.jpg`
- 备注：截图可确认左、右列卡片宽度不同，暂不判断是列宽计算、卡片自身样式、图片尺寸、容器布局还是屏幕取整造成。

## B-7：社区首次加载始终显示相同顺序的帖子

- 状态：已修复，待 Android APK 实机回归
- 类型：推荐流 / 内容排序 / 首次加载
- 优先级：P2（总顺序 12）
- 记录日期：2026-08-13
- 测试端：Android APK 实机
- 现象：不论用户如何重新进入或重新加载社区“寻味”页面，首次加载展示的帖子及其排列顺序始终相同，首屏内容流没有刷新变化。
- 期望：社区首次加载的内容和排序应按照产品既定的推荐策略动态更新，例如结合内容新鲜度、用户偏好或推荐结果；不应长期固定返回完全相同的首屏帖子顺序。
- 复现路径：打开 Android APK → 进入“社区”的“寻味”页 → 记录首屏帖子顺序 → 离开后重新进入或重新加载 → 对比首屏顺序。
- 复现频率：用户反馈为每次必现；后续修复阶段再进行多轮复现验证。
- 附件：
  - Android 实机截图：`codex-clipboard-249f527b-6325-4e61-b026-ff13d520600b.jpg`
- 备注：当前只记录“首次加载顺序固定”的产品表现，暂不判断是客户端缓存、固定排序参数、服务端推荐结果、分页游标还是种子数据导致；具体应采用随机、个性化或时效排序，也留待统一分析时结合既定推荐规则确认。

## B-8：顶栏与底栏的玻璃效果不一致，选中标签文字出现重阴影

- 状态：已修复，待 Android APK 实机回归
- 类型：UI / 导航栏视觉效果 / Android 实机适配
- 优先级：P4（总顺序 19）
- 记录日期：2026-08-13
- 测试端：Android APK 实机
- 现象一：页面滑动时，顶部导航栏采用的是可直接看出下层内容的半透明背景，而底部导航栏呈现为磨砂玻璃效果，两处导航栏的背景材质与模糊效果不一致。
- 现象二：底部导航栏的标签文字在选中后会出现明显的加重阴影或重影，导致字形显得过粗、发糊，影响清晰度。
- 期望：顶部与底部导航栏采用统一、符合设计规范的背景材质和模糊效果；底栏选中态可通过颜色或合理字重区分，但不应产生异常的叠加阴影或重影。
- 复现路径：打开 Android APK → 进入带顶部和底部导航栏的页面 → 滑动页面观察顶栏背景 → 切换底部标签并观察选中文字。
- 复现频率：实机上较明显；精确频率及涉及页面范围待确认。
- 附件：本条暂无单独截图，依据实机观察记录。
- 备注：当前只记录两处视觉表现，不判断顶栏是否缺少模糊层、两端是否使用了不同组件，也不判断底栏文字阴影来自字体渲染、选中态样式叠加或重复绘制。

## B-9：帖子分享内容缺少分享码与可点击链接

- 状态：已修复并部署，线上烟测及 Android APK 实机回归通过
- 类型：功能 / 帖子分享 / Deep Link / 剪贴板识别
- 优先级：P3（总顺序 13）
- 记录日期：2026-08-13
- 测试端：Android APK 实机
- 现象：点击帖子详情页右上角的分享后，可以生成类似“【食光社区】查看来自 健身达人Jack 的健康分享：训练后不想喝粉？这一碗牛肉彩椒糙米饭有 38g 蛋白质，饱腹感拉满。...”的文字摘要，但分享内容仅为普通文本，无法引导接收者直接打开对应帖子。
- 缺失能力一：分享内容没有生成可唯一定位帖子的随机分享码；接收者复制分享内容并打开 App 后，App 无法从剪贴板识别分享码、提示用户并跳转到对应帖子。
- 缺失能力二：分享内容没有包含可点击的帖子链接，接收者无法通过链接直接打开帖子详情；也没有体现未安装 App 时的合理承接方式。
- 期望：分享时同时生成可唯一定位且具备合理有效期或失效策略的分享码，以及可点击的帖子链接。App 启动或回到前台时可在获得用户授权并符合平台规范的前提下识别剪贴板分享码，经用户确认后跳转；点击分享链接时可通过 Deep Link / Universal Link / App Link 打开对应帖子，并为未安装 App 的情况提供网页或下载承接页。
- 复现路径：打开 Android APK → 进入社区帖子详情 → 点击右上角分享 → 查看生成的分享内容并尝试从接收端返回帖子。
- 复现频率：当前分享行为可复现；精确频率待确认。
- 附件：本条暂无单独截图，依据实机分享结果记录。
- 备注：当前仅记录产品能力缺失，不确定最终采用的分享码格式、链接域名、有效期、安全校验、剪贴板隐私交互和未安装 App 的承接方案；这些需在统一分析与修复阶段设计确认。

## B-10：发帖时间未适配本地时区，底部评论输入框默认过高且截断

- 状态：已修复，Android APK 实机回归通过
- 类型：时间展示 / 时区 / 评论输入框布局
- 优先级：P2（总顺序 6；评论框部分与 B-4 同批处理）
- 记录日期：2026-08-13
- 测试端：Android APK 实机
- 现象一（时区）：帖子顶部和正文信息区显示发布时间为 `2026-08-13 05:30:10`，与实机当前本地时间（截图状态栏约为 `14:03`）明显不在同一时区，发布时间没有按用户设备或产品指定时区正确转换展示。
- 现象二（布局）：帖子详情页底部“说点什么吧，与食友交流...”评论输入框在尚未输入内容的默认状态下高度异常，并在屏幕底部出现截断，输入区域没有完整、正常地显示。
- 期望：服务端时间采用明确的标准时间保存，客户端根据用户设备时区或产品指定时区转换后展示，并保持帖子各处时间一致；底部评论输入框在默认状态下应采用合理高度，避开系统安全区并完整显示，不应被屏幕底部裁切。
- 复现路径：打开 Android APK → 进入社区 → 打开帖子详情 → 对比状态栏本地时间与帖子发布时间，并观察页面底部默认状态的评论输入框。
- 复现频率：当前截图所示帖子可复现；是否影响所有帖子及全部时区待确认。
- 附件：
  - Android 实机截图：`codex-clipboard-ed8f7a9c-7c89-456e-979a-92358ac1ef48.jpg`
- 备注：截图中的时间差可以确认显示结果未符合实机本地时间，但暂不判断原始时间值、服务端序列化、数据库时区或客户端解析中的具体责任点；评论输入框问题也暂不判断来自固定高度、底部定位、安全区间距还是裁切属性。此前关于“纯文字帖展示框过高”的描述属于误解，已从本条记录中删除。

## B-11：首页轮播卡片无法手动滑动

- 状态：未修复，Android APK 实机回归未通过，问题仍存在
- 类型：交互 / 首页轮播 / 手势
- 优先级：P1（总顺序 15）
- 记录日期：2026-08-13
- 测试端：Android APK 实机
- 现象：首页展示的轮播卡片能够按照设定自动切换，但不响应用户的左右滑动手势，用户无法主动切换到上一张或下一张卡片，只能等待自动轮播。
- 期望：首页轮播在保留自动切换能力的同时，应支持用户左右滑动手动切换卡片；手动操作期间及操作后的自动轮播恢复方式应自然，避免与用户手势冲突。
- 复现路径：打开 Android APK → 进入首页 → 在自动轮播卡片区域尝试左右滑动。
- 复现频率：当前反馈为可复现；精确频率待确认。
- 附件：本条暂无单独截图，依据实机交互反馈记录。
- 备注：当前只记录轮播区域不响应手动滑动，不判断是轮播组件未启用手势、父级滚动容器拦截、覆盖层阻挡还是自动播放状态冲突。

## B-12：首页顶栏搜索框没有实际功能

- 状态：未修复，Android APK 实机回归未通过，问题仍存在
- 类型：功能 / 搜索 / 首页交互
- 优先级：P1（总顺序 4）
- 记录日期：2026-08-13
- 测试端：Android APK 实机
- 现象：首页顶部虽然展示了搜索框，但该区域如同静态装饰，用户无法通过它完成有效的搜索操作或进入搜索结果，搜索入口没有发挥实际功能。
- 期望：点击首页搜索框后应进入可用的搜索状态或搜索页面，允许用户输入关键词、查看建议并获得对应结果；若搜索功能尚不可用，则不应以可交互搜索框的形式误导用户。
- 复现路径：打开 Android APK → 进入首页 → 点击或尝试使用顶部搜索框。
- 复现频率：当前反馈为可复现；具体点击后的表现待后续复测确认。
- 附件：本条暂无单独截图，依据实机交互反馈记录。
- 备注：当前仅记录搜索框没有有效功能，不判断是点击事件缺失、搜索页面未接入、输入框被禁用还是搜索接口尚未实现。

## B-13：AI 交互页面顶栏按钮过多、过于复杂

- 状态：已优化，待 Android APK 实机回归
- 类型：UI / 信息架构 / AI 对话页导航
- 优先级：P4（总顺序 20）
- 记录日期：2026-08-13
- 测试端：Android APK 实机
- 现象：AI 交互页面顶栏在同一行集中展示返回、食光/AI 食语标识、采购清单、语音播报、新建（加号）和更多等多个元素或按钮，操作入口密度过高，视觉拥挤且主次关系不清楚，用户需要额外判断每个按钮的用途，也更容易误触。
- 期望：精简顶栏的常驻操作，仅保留当前页面最高频、最关键的入口；低频功能可收纳至“更多”菜单或移动到更符合使用场景的位置，并通过一致的图标、文字和层级清晰表达功能。
- 复现路径：打开 Android APK → 进入 AI 交互/“AI 食语”对话页面 → 查看页面顶栏。
- 复现频率：页面常态可见。
- 附件：
  - Android 实机截图：`codex-clipboard-94bc76e0-6f23-4dc5-90d2-86804bee951f.jpg`
- 备注：当前仅记录信息架构与视觉复杂度问题，不预先决定具体删除哪些按钮、哪些入口合并或最终顶栏结构；需在统一分析阶段结合功能使用频率和页面流程确定。

## B-14：首页“社区精选”卡片未跳转到对应帖子详情

- 状态：已修复，Android APK 实机回归通过
- 类型：功能 / 页面跳转 / 路由参数
- 优先级：P1（总顺序 5）
- 记录日期：2026-08-13
- 测试端：Android APK 实机
- 现象：用户从首页点击“社区精选”区域中的某一篇具体帖子卡片后，没有直接打开该帖子的内容详情，而是被带到社区首页，丢失了用户所选择的目标帖子。
- 期望：点击首页“社区精选”的具体帖子卡片后，应携带对应帖子标识并直接跳转至该帖详情；返回时应能自然回到首页原位置。若目标帖子已删除、不可见或加载失败，应显示明确状态，而不是无提示地跳到社区首页。
- 复现路径：打开 Android APK → 进入首页 → 找到“社区精选” → 点击任意具体帖子卡片 → 观察落地页面。
- 复现频率：当前反馈为可复现；是否影响全部精选卡片待确认。
- 附件：本条暂无单独截图，依据实机交互反馈记录。
- 备注：当前只记录跳转目标错误，不判断是首页卡片未传帖子 ID、目标路由未注册、参数解析失败、登录拦截还是回退逻辑导致。

## B-15：本地缓存工作状态存疑，清除缓存页面不显示缓存大小

- 状态：已修复并补充缓存统计，Android APK 实机回归通过
- 类型：缓存 / 设置 / 状态反馈
- 优先级：P3（总顺序 18；先验证缓存是否实际失效）
- 记录日期：2026-08-13
- 测试端：Android APK 实机
- 现象一：实机使用过程中怀疑本地缓存未正常工作，但当前缺少可见信息来确认数据是否被缓存、缓存是否被读取，以及缓存清理后是否确实失效。
- 现象二：在“设置 → 清除缓存”中无法查看当前缓存占用的具体大小，用户无法判断是否存在可清理内容，也无法对比清理前后的变化。
- 期望：本地缓存应按照既定策略正常写入、读取、过期和清理；设置页应显示可理解的缓存占用大小，并在清理完成后及时刷新为实际结果，同时给出成功或失败反馈。
- 复现路径：打开 Android APK → 进入“我的/设置” → 打开“清除缓存”相关入口 → 查看缓存大小并尝试确认缓存状态。
- 复现频率：缓存大小无法查看为当前反馈；缓存本身是否失效尚未确认，需在统一分析阶段专项验证。
- 附件：本条暂无单独截图，依据实机观察记录。
- 备注：本条不直接认定缓存功能已经损坏。后续需要区分图片缓存、接口响应缓存、离线数据、WebView 缓存及其他临时文件，并明确设置页所统计和清理的缓存范围。

## B-16：“关于食光烙记”页面未正确展示 Logo

- 状态：已修复，Android APK 实机回归通过
- 类型：UI / 品牌资源 / 关于页面
- 优先级：P3（总顺序 17）
- 记录日期：2026-08-13
- 测试端：Android APK 实机
- 现象：进入“设置 → 关于食光烙记”后，页面没有正确展示应用 Logo，Logo 可能表现为缺失、空白或错误资源。
- 期望：“关于食光烙记”页面应清晰、完整地展示当前正式应用 Logo，尺寸、比例和清晰度符合品牌设计，并在 Android APK 构建中稳定加载。
- 复现路径：打开 Android APK → 进入“我的/设置” → 打开“关于食光烙记” → 查看 Logo 区域。
- 复现频率：当前反馈为可复现；精确频率待确认。
- 附件：本条暂无单独截图，依据实机观察记录。
- 备注：当前只记录 Logo 未正确显示，不判断是资源引用路径、文件格式、打包遗漏、缓存还是布局样式导致。

## B-17：切回“我的”页面后滑动顶栏持续处于错误显示状态

- 状态：未修复，Android APK 实机回归未通过，问题仍存在
- 类型：UI / 页面状态恢复 / 滚动顶栏
- 优先级：P2（总顺序 9）
- 记录日期：2026-08-13
- 测试端：Android APK 实机
- 现象：在部分场景切换回“我的”页面时，会错误显示仅应在滚动到特定位置后出现的悬浮/滑动顶栏。该顶栏覆盖在页面顶部个人资料区域之上，导致头像、用户名等内容重叠。此错误状态并非仅出现于首帧：只要页面状态不再变化，它就会一直保持；用户再次滚动页面后，顶栏状态才会重新计算并自行恢复正常。
- 期望：切回“我的”页面时，应立即根据当前实际滚动位置正确计算并同步滑动顶栏状态，无需用户通过第二次滚动来纠正；页面位于顶部时不应显示收起态顶栏，也不应与原始个人信息区域重叠。
- 复现路径：打开 Android APK → 在“我的”页面与其他底部页面或子页面之间切换 → 返回“我的” → 在不滚动的情况下观察错误顶栏持续显示 → 再次滚动页面后观察其自行恢复。
- 复现频率：偶发，仅在部分情况下出现；稳定触发条件待确认。
- 附件：
  - Android 实机截图：`codex-clipboard-83d27008-a742-4aea-bca4-8a78898e4937.jpg`
- 备注：已确认错误状态会持续到用户再次滚动，而不是短暂的首帧闪现。当前不判断是保留了上次滚动位置、页面重新聚焦时未同步滚动偏移、滚动监听仅在事件触发时更新、动画状态还是条件渲染时序导致。

## B-18：首页主推荐卡片对非食物内容错误展示 `1 kcal`

- 状态：已修复，待 Android APK 实机回归
- 类型：内容展示 / 推荐卡片 / 指标适配
- 优先级：P2（总顺序 8）
- 记录日期：2026-08-13
- 测试端：Android APK 实机
- 现象：首页第一个主推荐卡片展示“记录一餐”这类行为引导内容时，右侧仍固定显示 `1 kcal`。该推荐并不是一份具有明确热量的食物或食谱，`1 kcal` 缺乏实际意义，也容易使用户误认为这是有效的营养数据。
- 期望：主推荐卡片应根据内容类型决定右侧辅助信息。具体食物或食谱可展示真实热量；记录饮食、补水、完善资料等行为引导应展示与任务相关的状态、收益、操作提示或不展示数值，不应使用无意义的占位热量。
- 复现路径：打开 Android APK → 进入首页 → 查看第一个主推荐轮播卡片 → 切换到“记录一餐”等非具体食物推荐 → 观察右侧指标。
- 复现频率：在对应推荐内容出现时可见；其他非食物推荐是否同样显示 `1 kcal` 待确认。
- 附件：
  - Android 实机截图：`codex-clipboard-01199261-f24b-482e-aeed-6ec729407fe9.jpg`
- 备注：当前只确认 `1 kcal` 不适合该推荐内容，不预先指定替换成哪一种信息；后续应根据各推荐类型及产品目标统一设计字段和文案。

## B-19：社区“榜单”顶部绿色区域二次切换后不加载内容

- 状态：已修复，待 Android APK 实机回归
- 类型：UI / 页面切换 / 内容加载状态
- 优先级：P2（总顺序 10）
- 记录日期：2026-08-13
- 测试端：Android APK 实机
- 现象：进入“社区 → 榜单”时，顶部绿色信息区域首次可能正常显示；切换到其他社区栏目后再次返回“榜单”，该区域似乎不会重新加载内部内容，只剩一整块绿色空白背景。下方 TOP 1 及其他榜单卡片仍能正常显示。
- 期望：每次进入或重新聚焦“榜单”栏目时，顶部绿色区域都应根据当前数据和状态完整显示内容；不得只保留背景容器。加载中、无数据或失败时也应显示明确状态，而不是无说明空白。
- 复现路径：打开 Android APK → 进入“社区 → 榜单” → 切换到“寻味/活动/问答”等其他栏目 → 再次切回“榜单” → 观察顶部绿色区域。
- 复现频率：疑似在二次切换时出现；稳定触发条件及具体频率待确认。
- 附件：
  - Android 实机截图：`codex-clipboard-0d8e3d96-0c2d-4d81-99c4-0ea01fd8af64.jpg`
- 备注：当前只记录二次切换后顶部区域内容缺失，不判断是页面焦点生命周期、条件渲染、动画、数据请求、缓存状态还是文字颜色与背景相同导致。

## B-20：食谱列表不支持像社区一样通过滑动刷新

- 状态：已修复，Android APK 实机回归通过
- 类型：交互 / 列表刷新 / 食谱
- 优先级：P3（总顺序 15；与 B-2 协调实现）
- 记录日期：2026-08-13
- 测试端：Android APK 实机
- 现象：“膳食资产 → 食谱”列表无法像社区信息流一样通过用户的滑动刷新操作重新获取内容，页面缺少一致、直观的主动刷新能力。
- 期望：食谱列表应提供与社区一致或符合平台习惯的滑动刷新交互，显示刷新中、刷新成功或失败状态，并在刷新后正确更新列表内容，同时避免重复数据和滚动位置异常。
- 复现路径：打开 Android APK → 进入“膳食资产 → 食谱” → 尝试执行与社区页面相同的滑动刷新操作。
- 复现频率：当前反馈为可复现；精确频率待确认。
- 附件：本条暂无单独截图，依据实机交互反馈记录。
- 备注：按用户描述记录为“上滑刷新/滑动刷新”。后续统一分析时需对照社区现有交互，确认这里具体指触顶下拉刷新还是另一种滑动触发方式，并统一各列表页面的手势规范。本条与 B-2 的“触底自动加载下一页”不同：B-2 是分页加载更多，本条是主动刷新当前列表内容。

## B-21：“我的”页面“记一餐”按钮仅跳转首页，需评估替换

- 状态：已调整为直达记餐流程，待 Android APK 实机回归
- 类型：交互 / 导航 / 功能入口设计
- 优先级：P4（总顺序 21；待产品确认）
- 记录日期：2026-08-13
- 测试端：Android APK 实机
- 现象：用户在“我的”页面点击悬浮的“记一餐”按钮后，只会跳转到首页，并未直接打开记餐页面或开始记餐流程。按钮文案所表达的操作与实际落点不完全一致，也增加了一次不必要的中转。
- 产品建议：评估是否将该按钮替换为更符合“我的”页面使用场景的功能入口；如果继续保留“记一餐”，则应考虑直接进入实际记餐流程，而不是只跳到首页。
- 期望：按钮名称、视觉定位和点击后的实际行为保持一致；最终是改为直达记餐还是替换为其他功能，需在统一分析阶段结合“我的”页面信息架构确定。
- 复现路径：打开 Android APK → 进入“我的” → 点击左下角悬浮“记一餐”按钮 → 观察跳转结果。
- 复现频率：当前反馈为可复现；精确频率待确认。
- 附件：本条暂无单独截图，依据实机交互反馈记录。
- 备注：本条同时包含现有行为问题和产品改版建议，目前不预先决定替代按钮的具体功能。

## B-22：管理端“对话记录”页面出现异常布局

- 状态：已修复并部署，通过线上 1280 × 720 浏览器布局回归
- 类型：管理端 / 页面布局 / 滚动容器
- 优先级：P2（总顺序 13）
- 记录日期：2026-08-13
- 测试端：Web 管理端
- 现象：管理端“AI 服务 → 对话记录”页面显示异常。主内容中的对话列表在页面中部被截断或遮挡，下半屏出现大面积空白；左侧导航栏则继续延伸并在更下方显示管理员信息和退出入口，侧栏与主内容区的高度、可视区域及滚动区域明显不同步。
- 期望：管理端侧栏和主内容区应正确适配浏览器视口；对话列表能够完整展示或在明确的滚动容器内连续滚动，不应在页面中部被无说明地截断，也不应产生异常的大面积空白。
- 复现路径：打开 Web 管理端 → 进入“AI 服务 → 对话记录” → 浏览或滚动对话列表页面。
- 复现频率：当前截图所示场景可见；触发条件与精确频率待确认。
- 附件：
  - Web 管理端截图：`codex-clipboard-96e4beef-0683-4978-802d-6e50995dd53b.png`
- 备注：本条虽来自 Web 管理端，仍暂存于当前统一问题清单。截图可确认布局和滚动区域异常，但暂不判断是固定高度、视口单位、`overflow`、粘性侧栏、浏览器缩放还是某个覆盖层导致。

## B-23：AI 将自己的名称“食语”错误用于称呼用户

- 状态：已修复，Android APK 实机回归通过
- 类型：AI / 用户身份上下文 / 工具能力
- 优先级：待统一排序
- 记录日期：2026-08-13
- 测试端：Android APK 实机
- 现象：AI 助手的名称被设计为“食语”，但回复开头却显示“食语您好，我是食光烙记的 AI 助手食语。”，把 AI 自己的名称同时当成了用户称呼，造成助手身份与用户身份混淆。系统中实际已经保存了当前用户的用户名，但 AI 没有正确获取并使用。
- 方案建议：为 AI 新增一个受控的只读用户资料工具，由服务端根据当前已认证会话读取用户在系统中存储的用户名或昵称，使 AI 在需要称呼用户时获取正确名称；不得允许模型通过参数读取其他用户资料，也不应暴露与称呼无关的敏感字段。
- 期望：AI 明确认知自身名称为“食语”，用户名称来自当前登录账户。称呼用户时优先使用用户设置的昵称或用户名；名称缺失或不适合展示时使用“您好”等自然的通用称呼，不得把助手名称当作用户名称。
- 复现路径：打开 Android APK → 进入“AI 食语”页面 → 发起对话 → 查看 AI 回复开头对用户的称呼。
- 复现频率：当前截图所示对话可复现；是否影响全部新对话待确认。
- 附件：
  - Android 实机截图：`codex-clipboard-d562186d-1cd3-4cb4-b0f1-d2578d5ae84c.jpg`
- 备注：当前仅记录身份称呼错误和新增用户资料工具的建议，不开始实现。后续需先检查现有系统提示词、上下文注入和工具清单，确认是否必须新增工具，或可安全复用已有的当前用户资料能力。

## B-24：管理端无法查看 AI 调用的具体报错信息

- 状态：待分析，未修复
- 类型：管理端 / AI 可观测性 / 错误诊断
- 优先级：高，待统一排序
- 记录日期：2026-08-13
- 测试端：Web 管理端
- 现象：管理端“AI 服务 → 对话记录”列表只能显示某段会话有 `1 次失败`、`2 次失败` 等失败次数，但无法正确查看每一次失败对应的具体报错内容，导致管理员无法判断失败原因和发生位置。
- 期望：管理员展开具体对话后，应能在安全脱敏的前提下查看失败轮次、发生时间、请求或模型配置标识、错误类型、错误码、可理解的错误信息、失败阶段及请求追踪 ID；应区分模型调用、工具调用、配置、超时、限流、网络和服务端异常。敏感密钥、完整认证信息及不必要的用户隐私不得展示。
- 复现路径：打开 Web 管理端 → 进入“AI 服务 → 对话记录” → 找到标有失败次数的会话 → 点击“查看对话”并尝试定位具体错误。
- 复现频率：当前存在失败记录的会话可见；具体错误详情不可用的范围待确认。
- 附件：
  - Web 管理端截图：`codex-clipboard-6118ade1-3113-49d5-9f48-9d33b3e0847d.png`
- 备注：截图确认列表层只有失败计数，具体错误详情是否完全未保存、接口未返回或管理端未展示，留待后续专项检查。

## B-25：新注册用户或新安装设备用户疑似无法使用 AI 功能

- 状态：严重问题，待专项验证，未修复
- 类型：AI / 新用户首次使用 / 新设备初始化
- 优先级：严重（高优先级）
- 记录日期：2026-08-13
- 测试端：Android APK 实机、服务端 AI 功能
- 现象：怀疑新注册用户，以及在新安装设备上首次使用的用户，无法成功使用 AI 功能。管理端截图中的新用户 `xel`（ID 21）对话显示 `2 次失败`，与该问题存在关联迹象，但目前尚不足以单凭截图确认统一根因。
- 影响：如果确认，将直接阻断新用户的 AI 首次体验，属于核心功能不可用的严重问题；也可能使换机、清除数据或重新安装后的既有用户受到影响。
- 期望：新注册用户完成必要的登录与初始化后，应能立即正常发起 AI 对话；新安装设备上的已登录用户也应能正常恢复所需账户状态并使用 AI。缺少必要资料或配置时，应提供可完成的引导，而不是直接调用失败。
- 待验证范围：分别测试全新注册账号、既有账号在全新安装环境登录、清除应用数据后重新登录，以及同一账号在原设备继续使用；对比认证状态、用户初始化数据、默认健康资料、AI 会话创建、模型配置和工具权限等环节。
- 复现路径：安装全新 APK 或清除应用数据 → 注册新用户或在新设备登录 → 进入“AI 食语” → 发起首次对话 → 检查客户端结果及管理端失败记录。
- 复现频率：疑似可复现，尚待受控环境验证。
- 附件：
  - Web 管理端截图：`codex-clipboard-6118ade1-3113-49d5-9f48-9d33b3e0847d.png`
- 备注：本条暂不直接认定所有新用户必然失败。B-24 缺乏具体报错详情会显著妨碍 B-25 的定位，因此两项在后续排查阶段具有关联，但仍需分别验证和修复。

## B-26：AI 对话图片无法作为附件与文字一起发送

- 状态：待产品设计，未修复
- 类型：AI / 多模态对话 / 图片附件交互
- 优先级：待统一排序
- 记录日期：2026-08-13
- 测试端：Android APK 实机
- 现象：当前用户只能通过输入区的“+”进入“识菜热量”等预设功能。选取图片后会直接触发固定的美食/菜品识别流程，用户无法先把图片保留在消息输入框中、继续输入自己的问题，再将图片和文字作为同一轮对话共同发送。
- 期望：图片应支持作为普通多模态对话附件。用户选图后，输入框内显示可预览、可移除或可更换的待发送图片；用户可继续输入任意问题，点击发送后将图片与文字绑定为同一轮用户消息。发送成功后，图片也应随该轮消息保留在对话记录中，重新进入历史对话时仍能看到，而不是只显示固定的识菜任务提示。
- 建议交互：“+”可继续作为附件入口，但应提供“从相册选择/拍照”等通用图片附件能力；“识菜热量”可作为快捷提示或专项模式，不应成为发送图片的唯一方式。
- 复现路径：打开 Android APK → 进入“AI 食语” → 点击输入框旁的“+” → 选择“识菜热量”并选取图片 → 观察图片直接进入固定识别流程，无法在输入框中附图并补充问题后一同发送。
- 复现频率：当前交互设计下可复现。
- 附件：
  - Android 实机截图：`codex-clipboard-7fbf4432-588b-451c-ac32-1b7dad380756.jpg`
- 备注：当前仅记录产品能力与交互诉求，不开始实现。后续需要一并确认图片上传与持久化、消息数据结构、历史记录展示、图片数量和大小限制、压缩、失败重试、隐私提示及模型多模态输入能力。

- 修复说明：管理端壳层固定为浏览器视口高度；页面 `body` 不再参与滚动；侧栏导航与主内容分别使用独立纵向滚动容器；管理员信息和退出入口固定在侧栏底部；同时将“对话记录”纳入 AI 服务分组的激活路由。
- 验证结果：线上 1280 × 720 视口下，根容器、侧栏、主内容均为 720px 高；侧栏导航可视高度 433.5px、内容高度 608px，可独立滚动；主内容高度 720px、内容高度 869px，可连续滚动；AI 服务分组保持展开且“对话记录”为当前页。

## B-27：帖子发布成功后仍停留在“写动态”页面且无成功提示，审核机制待补充

- 状态：新发现，待分析，未修复
- 类型：发帖流程 / 成功反馈 / 页面导航 / 内容审核
- 优先级：待统一排序
- 记录日期：2026-08-13
- 测试端：Android APK 实机
- 关联问题：B-5（带图片帖子已能够成功发布，本条为修复后暴露的后续流程问题）
- 现象：用户在“写动态”页面提交帖子后，帖子实际已经成功发布，但客户端仍停留在当前编辑页面，没有显示发布成功提示，也没有跳转至帖子详情或社区页面。页面状态会让用户误以为发布尚未完成或已经失败，可能导致重复点击发布并产生重复帖子。
- 期望（当前流程）：客户端收到发布成功结果后，应立即结束提交状态并提供明确反馈，同时离开“写动态”页面；具体跳转至新帖子详情还是社区列表，可在修复时结合现有导航流程确认。发布按钮在请求期间应避免重复提交。
- 后续需求（审核机制）：发帖流程后续需增加帖子审核机制。提交成功后应明确告知用户内容已提交以及当前审核状态；审核通过前是否允许作者预览、是否进入社区列表，以及审核拒绝后的原因展示与修改重提方式，需进一步完成产品设计。客户端提示、帖子状态、服务端接口和管理端审核能力应保持一致。
- 复现路径：打开 Android APK → 进入社区“写动态” → 填写内容并发布 → 帖子实际发布成功 → 观察页面仍停留在“写动态”且没有成功提示。
- 复现频率：当前实机测试可复现；精确频率待确认。
- 风险：用户无法判断发布结果，可能重复提交；在审核机制接入前，成功提示不得错误宣称帖子已公开展示。
- 附件：本条暂无单独截图，依据 Android APK 实机测试反馈记录。
- 备注：本条不否定 B-5 的修复结果。B-5 解决“帖子无法成功发布”，B-27 记录“发布成功后的客户端收尾与内容审核流程不完整”。

## B-28：AI 功能中的录音无法正常工作，疑似未获得录音权限

- 状态：新发现，待分析，未修复
- 类型：AI / 语音录入 / Android 权限 / 录音
- 优先级：待统一排序
- 记录日期：2026-08-13
- 测试端：Android APK 实机
- 现象：用户在 AI 功能中尝试使用录音时，录音无法正常工作，因而不能通过语音完成输入。当前怀疑应用未能获得设备的录音权限。
- 期望：用户首次使用录音功能时，应用应按 Android 平台规范请求麦克风权限；获得授权后能够正常开始、显示进行中状态、停止并处理录音。用户拒绝授权或系统限制权限时，应展示准确提示，并提供重新授权或前往系统设置的可操作入口。
- 待验证范围：检查 APK 是否声明麦克风权限、应用是否在使用录音前发起运行时权限请求、系统设置中的麦克风授权状态、拒绝或永久拒绝后的处理，以及权限已授予时录音模块的初始化与错误信息。
- 复现路径：打开 Android APK → 进入 AI 功能 → 点击录音入口 → 尝试开始录音 → 观察录音无法正常工作，并检查是否出现系统权限请求或错误提示。
- 复现频率：当前实机测试可复现；精确频率及受影响的 Android 版本待确认。
- 附件：本条暂无单独截图或录屏，依据 Android APK 实机测试反馈记录。
- 备注：“无法获取录音权限”目前仅为疑似原因，尚未确认。后续需结合系统权限状态、客户端日志和录音模块错误信息定位，不应在验证前直接认定为权限声明问题。

## B-29：食谱数量显示与服务器数据不一致，食谱库需考虑区分个人/家庭库与官方库

- 状态：新发现，待分析与产品设计，未修复
- 类型：食谱 / 数据一致性 / 数据分库 / 产品设计
- 优先级：待统一排序
- 记录日期：2026-08-13
- 测试端：Android APK 实机、服务端
- 现象：Android APK 中显示的食谱具体数量与服务器中的实际食谱数量不一致，用户无法从当前数量信息准确判断可用食谱规模。具体是总数口径、筛选范围、分页统计、缓存还是数据同步导致，尚待核对。
- 当前期望：客户端显示的食谱数量应与服务端接口在相同用户、家庭、筛选条件和数据范围下返回的统计结果一致。列表总数、已加载数量及空状态应采用明确且统一的统计口径，并在新增、删除、刷新或切换库后及时更新。
- 产品建议：参考现有食材的组织方式，将食谱明确划分为“个人/家庭库”和“官方库”。个人/家庭库用于展示当前用户或家庭创建、收藏或共享的食谱；官方库用于展示平台提供和维护的公共食谱。两个库应有清晰的切换入口、独立数量和一致的筛选及分页行为，避免将不同来源的数据混为一个不透明的总数。
- 待确认事项：个人食谱与家庭共享食谱是否合并展示；收藏官方食谱后属于收藏关系还是复制进个人/家庭库；官方库是否允许用户编辑；重复食谱如何处理；数量是否包含隐藏、下架、待审核或已删除记录；家庭切换后数据归属与统计如何刷新。
- 复现路径：打开 Android APK → 进入“膳食资产 → 食谱” → 查看页面显示的食谱数量 → 与服务端相同账户及相同数据范围下的食谱记录或接口统计进行对比。
- 复现频率：当前实机测试已发现；具体差值、涉及账户及稳定复现条件待确认。
- 风险：数量口径错误会影响用户对数据完整性的判断；若未先确定数据归属模型，直接修正单一总数可能在引入个人/家庭库和官方库后再次产生不一致。
- 附件：本条暂无单独截图，依据 Android APK 实机测试及服务器数据对比反馈记录。
- 备注：本条包含一个需先定位的数据一致性缺陷，以及一个相关但范围更大的食谱库产品改造建议。修复数量显示时应先确认现有接口和统计口径；分库方案需完成产品与数据模型设计后再实施。

## B-30：点击“存厨具”后触发 JavaScript 异常

- 状态：新发现，待分析，未修复
- 类型：功能阻断 / Android 崩溃 / React Hooks / 厨具
- 优先级：P1
- 记录日期：2026-08-13
- 测试端：Android APK 实机
- 应用版本：`1.0.5-beta.1`
- 现象：用户点击“存厨具”后，应用发生 JavaScript 异常，当前操作无法正常完成。系统“三方应用分析”显示食光烙记存在稳定性问题，并记录了对应错误堆栈。
- 错误信息：`com.facebook.react.common.JavascriptException: TypeError: Cannot read property 'useRef' of null`。
- 堆栈线索：截图中的调用栈包含 `SmartDateInput`。该组件可能与异常路径有关，但仅凭当前截图尚不能认定它是最终根因。
- 影响：用户无法完成保存厨具操作；异常还可能导致当前页面中断、应用退出或被系统判定为不稳定。
- 期望：点击“存厨具”后应正常保存厨具信息，并提供明确的成功或失败反馈；不得触发 JavaScript 异常或导致应用崩溃。
- 复现路径：打开 Android APK → 进入厨具相关新增或编辑页面 → 填写必要信息 → 点击“存厨具” → 应用发生异常。
- 复现频率：当前实机测试已复现；精确频率待确认。
- 问题发生时间：`2026-08-13 21:35:29`。
- 待验证范围：检查“存厨具”操作触发的组件渲染路径、`SmartDateInput` 的导入与 Hook 调用环境、是否存在重复或不兼容的 React 实例、Android 生产包与开发环境的差异，以及保存请求是否在异常发生前已经提交，避免修复后产生重复数据。
- 附件：
  - Android 系统“三方应用分析”截图：`codex-clipboard-942dd8e3-ebd7-4854-8b6f-bc1ed4b3e7ab.jpg`
- 备注：当前已确认的是点击“存厨具”后出现 `useRef` 相关 JavaScript 异常。尚未确认数据是否已在后台保存，也不应在完成代码与日志检查前直接认定为单纯的日期输入组件问题。

## B-31：尚未实现深色模式，Android 系统强制深色化导致显示异常

- 状态：已确认产品现状，待后续实现正式深色模式
- 类型：UI / 深色模式缺失 / Android 系统强制深色化
- 优先级：待统一排序
- 记录日期：2026-08-13
- 测试端：Android APK 实机
- 产品现状：应用目前尚未正式设计和实现深色模式。Android 系统或设备厂商在系统深色模式下会对应用界面进行强制深色化处理，导致原本按浅色模式设计的颜色被系统自动修改。
- 现象：系统强制修改背景、文字、图标或其他界面颜色后，应用出现非预期显示效果。该问题不是现有深色主题切换逻辑失效，而是应用缺少完整深色主题时被系统自动转换所致。
- 期望：后续正式实现深色模式，为背景、文字、图标、边框、弹窗、输入框、导航栏和系统状态栏提供完整且统一的深色主题设计，避免依赖系统强制转换颜色。
- 阶段性处理建议：在正式深色模式上线前，评估是否应阻止 Android 对应用进行强制深色化，使应用继续稳定使用既有浅色主题；最终仍以完成正式深色模式为解决方向。
- 待验证范围：实现深色模式后，在系统浅色与深色模式间切换并重启应用，逐页检查主题切换、颜色对比度、系统栏、弹窗和交互状态，并验证不同 Android 版本及厂商系统的表现。
- 复现路径：将 Android 系统主题切换为深色模式 → 打开或重新进入食光烙记 → 系统强制修改应用颜色 → 观察界面显示异常。
- 复现频率：在会对未适配应用执行强制深色化的 Android 实机环境中可见；具体设备和系统范围待确认。
- 附件：本条暂无截图或录屏，依据 Android APK 实机测试反馈记录。
- 备注：本条作为后续主题能力建设需求保留。当前已明确根本背景是“产品尚未实现深色模式，同时系统进行了强制深色化”，不再按已有深色主题兼容故障描述。

## B-32：需要登录的操作只弹出警告，未引导至登录页

- 状态：待分析，未修复
- 类型：交互 / 登录拦截 / 操作续接
- 优先级：待统一排序
- 记录日期：2026-08-14
- 测试端：Android APK 实机
- 现象：未登录用户执行点赞等需要登录的操作时，页面只弹出“登录后点赞 / 登录后即可点赞支持这篇分享”的警告弹窗，且仅提供“OK”按钮。用户确认后无法直接进入登录页面，需要自行寻找登录入口，操作流程被中断。
- 期望：触发受登录保护的操作时，应明确提供“去登录”和“取消”等操作；用户选择登录后直接进入登录页。登录成功后应返回原页面并保留目标帖子上下文，允许自动续接原操作或让用户便捷地再次执行，避免登录后丢失原浏览位置。
- 适用范围：除点赞外，还应统一检查关注、收藏、评论、发帖及其他需要账户身份的入口，避免各处使用不同的警告和跳转方式。
- 复现路径：以未登录状态打开 Android APK → 进入社区帖子详情 → 点击点赞 → 观察仅出现警告弹窗且无法直接前往登录页。
- 复现频率：当前点赞场景可复现；其他受限操作的影响范围待确认。
- 附件：
  - Android 实机截图：`codex-clipboard-fcec7a36-1a66-41f1-96bf-62b38c05ea2a.jpg`
- 备注：当前仅记录登录拦截体验问题，不开始修改认证路由。后续需统一设计登录前来源页与待执行动作的保存、登录成功后的安全续接，以及用户取消登录时的返回行为。

## B-33：食谱详情“开始烹饪”应改为“加入烹饪队列”，并新增队列事务

- 状态：待产品设计，未实现
- 类型：产品功能 / 食谱 / 烹饪队列
- 优先级：待统一排序
- 记录日期：2026-08-14
- 测试端：Android APK 实机、Web
- 现状：食谱详情页底部当前直接提供“开始烹饪”按钮。该入口假定用户查看食谱后会立即开始制作，无法支持先选择多道菜、稍后统一安排和管理烹饪任务的使用场景。
- 产品建议：将食谱详情页的主要按钮修改为“加入烹饪队列”。用户先将计划制作的食谱加入队列，再从队列中选择具体项目进入真正的“开始烹饪”流程。
- 烹饪队列基础事务：查看待烹饪食谱、加入队列、防止重复加入、调整顺序、移除、清空、标记计划时间或餐次，以及从队列项目开始烹饪。
- 烹饪过程事务：至少考虑 `待准备 → 可开始 → 烹饪中 → 已完成/已取消` 等状态；开始后应关联步骤进度、计时或提醒，并在完成时正确更新队列状态。
- 关联能力：加入队列时可检查食材库存与厨具条件，提示缺失项，并允许加入采购清单；多道菜同时安排时，可进一步考虑总耗时、并行步骤和上菜顺序，但具体范围需产品确认。
- 期望：食谱详情、烹饪队列、采购清单、库存和实际烹饪流程之间形成清晰衔接；按钮文案应准确反映当前操作，不应在用户只是计划制作时直接进入“开始烹饪”。
- 复现/查看路径：打开食谱详情 → 滚动或查看页面底部固定操作区 → 当前显示“开始烹饪”。
- 附件：
  - Web 食谱详情截图：`codex-clipboard-20c240e2-7e55-44dc-b0ba-74c08b6fb251.png`
- 备注：本条属于功能与流程改造，不是单纯替换按钮文案。后续需确认队列归属个人还是家庭、是否跨设备同步、容量和重复规则、状态持久化，以及原“开始烹饪”页面如何接入队列。

## B-34：帖子详情顶部用户名与日期显示异常

- 状态：待分析，未修复
- 类型：UI / 帖子详情顶栏 / 文本布局
- 优先级：待统一排序
- 记录日期：2026-08-14
- 测试端：Android APK 实机
- 现象：帖子详情页顶部的作者用户名没有正常展示，只能看到省略号；发布日期所在区域被压缩成极窄宽度，日期字符逐个纵向换行显示，例如年份、月份、日期和时间被拆成单字符列。异常文本把顶栏高度大幅撑开，头像、关注按钮、菜谱状态和分享按钮之间出现大面积空白。
- 期望：顶栏应在有限宽度内稳定展示作者用户名和发布日期。用户名过长时使用单行合理截断；日期保持一行或按设计最多有限换行，不得逐字符纵向排列，也不应撑高整个顶栏。其他操作按钮的位置和间距应保持稳定。
- 复现路径：打开 Android APK → 进入社区 → 打开受影响的帖子详情 → 查看顶部作者信息和发布日期区域。
- 复现频率：当前截图所示帖子可复现；是否与特定用户名、字体缩放、屏幕宽度或日期格式有关待确认。
- 附件：
  - Android 实机截图：`codex-clipboard-246d47e8-5401-4a63-b1a9-d6cafd95c26a.jpg`
- 备注：当前只记录用户名缺失/截断和日期逐字符换行造成的布局异常，不判断是弹性布局收缩、文本宽度、`flexShrink`、日期格式、字体缩放还是其他顶栏元素占宽导致。

## B-35：加载更多食谱时新内容插入已有列表而非从底部衔接

- 状态：待分析，未修复
- 类型：食谱 / 分页合并 / 列表顺序
- 优先级：待统一排序
- 记录日期：2026-08-14
- 测试端：Android APK 实机
- 现象：用户下滑触发加载新一批食谱后，新获取的食谱没有从当前列表底部继续追加，而是被插入已经展示的食谱之间。已有卡片的位置和顺序因此发生变化，用户当前阅读位置可能跳动，也难以分辨哪些是新加载内容。
- 期望：常规向下分页加载时，应保持已展示食谱的相对顺序不变，将去重后的下一页内容连续追加到列表末尾；加载过程不应造成当前视口跳动。只有用户主动刷新或切换排序/筛选规则时，才允许明确地重新排列整个列表。
- 复现路径：打开 Android APK → 进入“膳食资产 → 食谱” → 记住当前列表中若干食谱的顺序 → 下滑触发加载下一批 → 对比加载前后的卡片位置。
- 复现频率：当前反馈为可复现；是否与推荐排序、双列布局或特定筛选条件有关待确认。
- 附件：本条暂无单独截图或录屏，依据实机交互反馈记录。
- 备注：当前只记录分页后列表发生中间插入和重排，不判断是客户端分页合并、去重与排序、响应数据顺序、推荐重排、列表 key 不稳定还是瀑布流重新分列导致。本条与 B-2 的“无法正常继续加载/触底自动加载”相关，但关注点不同：B-35 专门记录加载成功后的内容衔接顺序。
