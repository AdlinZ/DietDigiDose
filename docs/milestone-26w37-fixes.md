# 26w37 修复进度（2026-09-09）

目标里程碑：26w37｜可信 Beta 发布基线（GitHub milestone 9）。当前 16 项 open；此记录不表示发布验收完成。

最新验证（2026-09-09）：本机临时 PostgreSQL 17.11 完整迁移集成测试已通过；下文较早记录中的“PostgreSQL 未实跑”是历史状态。当前仍未完成制作流程集成、真实语音与设备验收，不能据此关闭整个里程碑。

## 本地修复

- #183：静音先停止本地采集再同步心跳；合并进行中的连续点击；恢复失败清理部分启动的录音器；停止会话后丢弃恢复响应；静音时不处理迟到的识别结果，也不被自动重连定时器重新启动。
- #184：SQLite/PostgreSQL pending 查询按 attempts、created_at、id 排序。失败任务保持 pending 并轮换到未尝试任务之后；保留 claim token 和原子互斥。有限任务集合内，最早 25 个失败任务不再饿死第 26 个有效任务；人工重试仍可立即执行。
- #185：示例环境不再设置正式 Android 包名，使用各 EAS profile 默认身份；所有安装名称按 AGENTS.md 保持“食光烙记”。测试覆盖加载实际示例环境后的三个 preview profile、candidate 和 production。

- #189：Agent 新增/修正/消耗复用库存仓储事务。部分消耗带批次、数量、单位和版本；不完整的 ID-only 旧提案转为补充确认，不再整批下架。数量文本可确定时同步结构化值，未知时清空旧数值；记录来源 ai，丢弃原因单独写入历史且不创建摄入；复用 Action ID 去重及整组事务回滚。AI 上下文提供批次 ID、版本、结构化数量。

- #190（实现中）：新增制作批次、剩余份量及食用/丢弃/延期事件。仅制作扣减原料，实际食用按日期与本人份量生成摄入，未知营养保持 null；队列/计划及 Agent 共用事务入口。新客户端增加待吃餐页面与制作分配，保留旧 diet_record API 兼容；SQLite v60 与 PostgreSQL 0002 迁移已生成。尚需补全计划/队列关联边界回归及设备验证，不能视作 issue 已验收。

- #191（实现中）：Agent 新增库存和修正库存接入既有十分钟撤销窗口。按保存后的版本核验；新增软删除，修正恢复原业务字段但版本继续递增；撤销写入 ai 历史记录，与 Action 状态同事务。后续消耗/修改引起版本变化时拒绝撤销并提示库存页纠正。重复撤销返回 undone=0，其他账号与不存在运行仍拒绝。扫描恢复沿用 jobId+index 项目标识，重复检查只按该标识去重，不再按名称合并数量；同名不同标识保留并提示核对独立批次。自动接受判定、字段已知/估计/未知、集中确认列表尚未实现；来源任务+source_item_id 的服务端去重已实现，换请求编号恢复会复用项目，部分补交只新增缺项。来源相同的 PostgreSQL 写入共用事务锁。助手扫描卡片已由逐项普通新增改走同一批量事务，并与库存页统一 source_item_id；不同扫描任务的重叠照片识别仍待实现；同一运行多次改同一批次的合并撤销仍需处理。

- #192（实现中，可独立推进）：扩展已有 kitchen_constraints，保存常用餐次、地点、携带、冷藏/加热条件；档案页可编辑且保留“否”和“未知”。厨房偏好按字段合并，避免只改人数清空已有设置；SQLite 在事务内合并，PostgreSQL 在 UPDATE 中合并 JSONB。客户端/AI 上下文未设时长回退 30 分钟，已有显式值保留。共享解析函数定义本次覆盖优先于长期设置且不修改原对象；Supervisor 文字/媒体路由已提取仅本次 kitchenOverride，传入营养、菜谱及业务规划上下文，读取上下文工具同时提供有效偏好；不写长期档案。长期 update_kitchen_preferences 动作已接入高风险确认，限制为厨房字段并按字段合并；持久意图门槛拒绝普通本次表述及否定保存。Supervisor 路由携带已知偏好，避免缺失上下文导致重复询问。完整确认摘要、复杂语言意图识别及推荐条件硬校验仍待验证/完善，真实语音抽取效果未验证。

- #193（实现中）：修复共用 FEFO 预览对每个需求重复读取全部库存的问题，整次预览共享批次剩余数量；扣减项统一为库存单位，避免跨菜混合 kg/g 时客户端合并漏算。预览不修改真实库存。响应区分 name_available 及 sufficient/insufficient/unknown/unavailable；空数量或不可换算包装标为 unknown，烹饪页提示核对用量，不再报告确定缺口。推荐候选传出 serving_size/prep_time；默认时长复用 30 分钟，已知准备时间纳入筛选，说明不再以单菜烹饪时间声称整套符合约束，评分版本提升为 rules-2026-09-08.1 使旧快照失效。待吃餐新增保留/解除保留状态（SQLite v61、PostgreSQL 0003），通过已有计划调整事件保存并保持数量及原计划日期；页面有操作按钮，AI 上下文分开可用与保留项。推荐服务新增 /plan-requirements 及营养 Agent 查询工具，按日期/餐次分配未保留且未排除的待吃餐，跨餐共享份量，返回待补做份量；结果强制 requires_validation，不代表保鲜、过敏及热加工条件已验证。新增 /cooking-plan：从统一候选过滤结果选择有明确份数和精确原料用量的菜谱，按补做份量缩放并共享库存预算，偏好不超过已知顺序累计时长的候选。未知份数/用量会留下未解决餐次，时间仍注明收尾/设备容量未知。方案查询现已直接接收本次 preferences，Agent 工具把本次覆盖传入筛选；返回 effectivePreferences，长期档案不变。方案整体仍为 requires_validation；库存替代、保鲜/携带/加热依据、完整设备调度与前端方案操作未完成。

## 验证

- 客户端定向测试 26 项通过：useRealtimeCookingVoice、realtimeVoiceSessionUpdater、buildConfig。
- #189 定向测试 30 项通过；#190 初版后的服务端完整测试 303 项通过。SQLite 仓储覆盖十枚减两枚、重复提交、单位/版本/不足冲突、丢弃、未知数量、整组回滚；PostgreSQL 同场景用例已增加，尚未执行。
- #190 追加 API/Agent 定向测试 3 项通过：制作三份吃一份、跨日半份、丢弃不计摄入、延期、重复请求、并发版本冲突、失败事务回滚。Agent 重放去重与整组回滚通过。计划关联队列完成、过期版本、新旧客户端跨入口重放通过；修复新版制作后旧版餐单完成再次新增摄入的问题，已软删除餐次禁止新建制作。PostgreSQL 等效事件测试已增加，未实跑。
- Web Expo export 通过（输出 /tmp/w37-web-preview，不分配打包编号）；修正共享契约 .js 导出导致 Metro 无法解析的错误。contracts check 通过。浏览器页面交互仍待验收。
- #192/#193 衔接回归通过：长期 45 分钟、本次 20 分钟时方案筛选使用 20，下一请求回到 45；冷藏 false 覆盖亦不改变长期 true。
- #193 菜谱草案回归通过：两个餐次合需 4 枚蛋、库存 3 枚时第二餐缺 1；顺序累计 40 分钟明确超过 30；未知份数不生成可执行菜谱分配；不修改真实库存。推荐模块 5 项通过，服务端 302 项完整回归通过。
- #193 数量分配 API 回归通过：已有 1 份需 3 份时补 2 份、跨餐不重复分配、排除后重算、账号隔离，库存/摄入不写入。
- #193 保留状态 API 回归通过：持久化、重复请求去重、份量及日期不变、无摄入、从可分配上下文移出及解除保留。迁移 schema 检查通过；PostgreSQL 尚未实跑。
- #193 时间回归通过：准备 20+烹饪 20 不通过默认 30 分钟，已设 45 分钟可保留候选但仍注明整套时间未验证；请求 30 分钟覆盖长期 45 分钟。推荐模块 4 项测试通过，更新评分逻辑后的服务端原 298 项完整回归通过。
- #193 数量状态回归通过：名称存在数量未知、袋与克不可换算、已知不足、足量和未找到库存分别表达；OpenAPI 增量字段保持可选兼容。
- #193 共享预算回归通过：库存三枚蛋满足两个各需两枚的请求时，覆盖量为 2/1、第二项缺 1；0.2kg+300g 统一扣减 200g+300g，原库存输入不变。
- #192 长期动作策略/仓储回归通过：今天与以后再说不通过长期意图门槛、持久动作保持高风险、不能提交过敏字段、保存保留原档案、Action 重放去重。PostgreSQL 同步实现，尚未实跑。
- #192 请求覆盖上下文回归通过：本次人数/时长/冷藏覆盖生效，未覆盖的明确不可加热保留，下次上下文和数据库仍使用长期值。
- #192 API 回归与契约测试通过：部分厨房偏好更新保留过敏、忌口和其他厨房字段；30 分钟回退、显式时长保留、本次覆盖不改原对象、false 与 null 区分。
- #191 服务端项目去重 API 回归通过：部分入库、换请求键恢复、同名独立项目、账号隔离、重复标识拒绝。PostgreSQL 并发/补交用例已添加，未实跑。OpenAPI 已更新且向后兼容检查通过。
- #191 扫描标识恢复与同名多批次客户端测试通过；撤销重试及账号隔离定向测试通过。
- #191 SQLite API 数据库回归通过：修正撤销恢复数量、重复撤销不再写入、后续变化冲突、新增撤销软删除及历史记录。PostgreSQL 实现已同步，未实跑。
- 数据库 schema 检查通过（94 表、145 外键），模块边界通过。
- 媒体清理测试 4 项通过，包含真实 SQLite 内存数据库的 25 条旧任务阻塞、后续有效任务、恢复重试与 claim token 校验。
- pnpm -w lint:all 通过（client TSC/ESLint、server TSC、admin oxlint）；git diff --check 通过。
- PostgreSQL 集成用例已增加，但本机无运行中的 Docker daemon，也无 PostgreSQL 测试连接，本轮尚未执行。
- 实际 HTTPS staging 烟测 10 步通过：health、version、register、login、inventory.create、inventory.list、recipes.list、cooking.complete、diet-records.list、account.delete；使用独立临时账号，已清理。服务器重启持久性、隔离与恢复尚未验收。
- 尚未完成 Web 浏览器和 Android/iOS 真机录音、APK 并存与保留数据升级；因此没有关闭相关 issues。

## 后续顺序

#190 关联边界与页面验收 → #191 → #192 → #193 → #194。#189 本地代码和 SQLite 回归已落地，仍需 PostgreSQL 实际执行与真实语音验证。

#187 工作区已有其他基础数据导入改动，保持原样。发布侧 #105/#106/#108/#107/#171/#62 仍需真实 staging、备份恢复、媒体存储和设备证据。GitHub 已设置 STAGING_BACKEND_BASE_URL=https://dietdigidose.top，不能沿用历史文档中的“未配置”结论。

## 用户指定的交付顺序

全部修复并验收后，创建 PR、合并到 main，再打包 26w37b。打包前核对远端 apk-build 标签与已分配编号；不提前分配编号，遵循失败构建也占用编号的仓库规则。当前尚未创建 PR、合并或打包。

#194 已新增备餐草案页面与餐单入口，展示跨餐待吃分配、补做份量、整套原料缺口及时间待核对项。输入变化、页面离开或账号切换使在途旧结果失效。页面尚未完成统一执行、替换及反馈流程，不能作为该 issue 的完整验收。

2026-09-09：新增备餐页面后 client TSC/ESLint、Expo Web export 与 git diff --check 通过，尚未做页面交互验收。远端编号只读查询目前仅有 apk-build/26w37a；尚未预留 26w37b。Android APK 工作流可从 main 手动触发 preview，自动预留下一编号，合并提交不应额外触发重复构建。

2026-09-09 #194：新增 POST /meal-plans/drafts，复用 meal_plans.constraints_json 保存待核对草案快照（餐次、待吃分配、补做、原料批次来源及时间）。客户端保存后可从餐单恢复；输入修改后重新计算会使用新保存编号，同一次保存失败重试复用编号。SQLite 事务和 PostgreSQL 同编号事务锁防止重复保存；不同内容/账号复用编号返回冲突。共享运行时契约解析草案与来源批次。当前保存的仍是草案快照，尚未转换成可执行餐次，局部替换和制作入队不能据此声称完成。

验证：草案 API 回归验证保存后重新读取、同编号重试、不同内容冲突、账号隔离、原料来源保留，且没有新增摄入/队列。服务端完整 304 项通过，client/server/admin 静态检查、契约检查与架构边界通过；新增恢复入口后的 Web export 通过。PostgreSQL 增加并发保存和新仓储实例恢复用例，尚未实跑。SQLite 直接访问新增仅在现有 mealPlans 仓储适配器内，配套 PostgreSQL 端口实现；据此更新访问计数基线。这是功能扩展导致的计数增长，不是旧 SQLite 访问收敛的证据。仍需真实重启、弱网和页面交互验收。

2026-09-09 #194 局部替换：新增 /recommendations/cooking-plan/replace，只替换指定餐次中的唯一新做菜，保留其余菜谱、原料需求、待吃分配和排除项。候选仍经过当前账号偏好与约束筛选；替换后按整套需求重新计算 FEFO 原料预算和已知顺序耗时。缺口、未知换算、超时及无法核实的保留菜谱给出冲突说明，不重新分配其他餐次。客户端先显示预览与冲突，采用后才更换当前草案；取消或失败保留原草案。保存替换目前仍创建新草案快照，尚未原位更新已保存餐单，不能视作已确认餐单的完整替换流程；Agent 语音动作与实际餐次执行仍待衔接。

该轮验证：定向 7 项推荐测试通过，包括只替换晚餐、保留午餐和半份待吃分配、保留排除项、库存 4 枚需求变成 5 枚后缺 1 枚、整套 35 分钟超 30 分钟、不修改输入/库存、目标模糊和没有替代菜时拒绝。服务端完整 305 项通过，静态检查、契约检查与 Web export 通过。页面交互与 PostgreSQL/设备验证仍未完成。

2026-09-09 #194 原位更新：新增 PATCH /meal-plans/:id/draft，草案沿用原餐单 ID 更新 currentCookingDraft 并递增 version，原始保存请求仍保留用于创建请求重放。最后一次更新的相同幂等键/内容直接返回当前餐单；同键不同内容或旧版本拒绝。仅允许尚未进入执行且没有实际餐次项的草案更新。SQLite 事务和 PostgreSQL 行锁承接并发保护。客户端恢复时优先读取 currentCookingDraft；保存成功以服务端返回值为准；局部替换后的保存现在更新当前餐单而非另建草案。查看菜谱后返回保留当前草案和请求编号，账号切换仍清空。尚未将草案转换为可制作餐次/队列，Agent 语音衔接和设备验收仍待完成。

验证：SQLite API 覆盖原 ID 更新、两个相同请求只更新一次版本、日期范围随草案更新、原始创建快照保留、同键不同内容/旧版本冲突和跨账号拒绝；PostgreSQL 并发更新/重放用例已添加但未执行。完整服务端 305 项、全仓静态检查、契约检查及 Web export 通过。SQLite 计数基线同步现有仓储中的新增草案写入，未新增服务层直接数据库访问；该基线变更仍需在最终 PR 中说明功能扩展原因。

2026-09-09 #193/#194 份量一致性：共享草案契约增加跨字段校验，餐次总需求必须等于待吃加补做，待吃明细之和及总补做份量必须匹配；拒绝重复餐次、重复/悬空新做菜、补做与待解决项重复、没有原料用量的新做菜、已排除待吃餐仍被分配、同批次重复分配及跨餐版本不一致。超时标识必须与已知时间一致。生成接口也通过同一契约解析，再返回可保存草案。此校验只证明数量关系，不证明真实库存、存放安全或制作可执行性。

验证：新增 3 项契约回归包含半份待吃、合理跨餐共用、待解决方案及上述无效组合；API 确认不一致草案返回 400 且数据库没有餐单写入。契约共 7 项、服务端完整 305 项和推荐定向 7 项通过；全仓静态检查通过，生成接口追加契约解析后再次通过 server TSC 与 Web export。制作队列衔接尚未完成。


2026-09-09 PostgreSQL 实跑：通过 Homebrew 安装 postgresql@17（17.11），创建独立临时集群 /tmp/w37-pg-f_mwnpe5，仅监听 127.0.0.1:55437，使用全新 w37_integration_2 数据库运行 `pnpm --dir server test:postgres-migration`。未连接 staging。完整执行通过，包括 94 表迁移与重复/并发导入、Agent 库存数量/历史/回滚、制作与后续份量事件、批量入库恢复去重、草案并发创建/原位更新、媒体清理轮换、其他仓储、最小权限及 PostgreSQL 应用运行时。首次执行定位两处过时测试预期：表数仍为 92，重复撤销仍预期报错；分别更新为 94 与幂等 undone=0，再用全新数据库完整重跑通过。日志 /tmp/w37-postgres-test.log。测试结束停止临时实例，不启用开机服务；该结果不等于 staging 迁移、备份恢复或设备验收通过。

2026-09-09 #191 分组撤销：同一 run 对同一库存批次的 add/update 按结果版本倒序撤销，不再依赖 Action ID 字典序；补偿写入仍逐次递增实际版本，用独立原始版本链验证前一步。连续修正可以回到最初业务状态；修改链中若夹有不属于该组的消费/修改，拒绝并回滚整组补偿。不同库存和非库存 Action 的相对槽位保持原有顺序，现有十分钟窗口及后续版本冲突检查保留。

验证：SQLite 真实数据库场景以 z/b/a 反向命名 Action，新增 10 枚、改 8 枚、改 6 枚后撤销三步，最终软删除且版本为 6；重试 undone=0，只有三条撤销日志。夹有外部版本修改时失败，库存、版本和撤销日志均回滚。PostgreSQL 新增同场景并在新库 w37_integration_3 完整集成实跑通过；临时实例已停止。服务端完整 306 项、server TSC 与 diff 检查通过。扫描自动接受、字段不确定性与集中确认仍未完成，不能关闭 #191。

2026-09-09 #191 未知扫描字段：服务端扫描接口、库存页和助手卡片复用共享 normalizeInventoryScanItems，缺失/无效数量保持空字符串，位置保持空，期限保持 null，置信度仅接受 0..1 有限数值。移除 1 份、冷藏、7 天的无依据回填；助手编辑也不再把空字段转为默认值。库存复核与助手卡片明确提示待确认信息，补齐后才提交；已有识别期限显示为建议并提示核对。识别提示要求未知字段明确返回空/null，不以固定默认值填补。修正字段后清除对应旧提示。服务端历史扫描接口中的默认值也已移除，避免仅修客户端而后端继续伪造已知值。

验证：客户端 4 项扫描回归覆盖来源身份恢复、同名独立批次、未知/非法/null 字段和已知半袋/12 天的原样保留；完整服务端 306 项、契约检查、全仓静态检查及 Web export 通过。此轮未实现字段依据元数据持久化、可靠项自动接受和未知到期信息直接保存，仍不能关闭 #191；真实图片/语音识别效果仍需实际验收。

2026-09-09 #191 字段依据入库：新增可选 field_evidence 契约，记录名称/数量/位置/期限的 known、estimated、unknown 状态及 user、recognition、barcode、rule、unknown 来源。扫描解析对已有识别值标 estimated、缺失值标 unknown，不因高置信度直接声称 known。用户编辑某字段后只将该字段标为 user/known；助手编辑保存未变化字段不提升其状态。复核 UI 标出识别估计数量和建议期限。批量入库在同一事务新增创建历史，将字段依据、来源任务、项目身份和置信度写入 metadata；也保留在批次确认载荷，重放复用项目且不重复历史。OpenAPI 可选字段已更新。

验证：SQLite API 回归确认两项恢复入库仅两条创建日志，历史接口可读回 estimated/recognition；PostgreSQL 在 w37_integration_4 完整测试通过，包含并发恢复仅一条创建日志及依据回读；已停止临时实例。服务端 306 项、扫描 4 项、全仓静态检查、契约检查及 Web export 通过。SQLite 计数同步新增仓储历史写入。字段依据目前为入库时的审计快照；尚未形成随后续修改更新的当前字段依据视图，推荐服务尚未读取该信息，可靠项自动接受与未知到期直接入库仍待完成。

2026-09-09 #191/#193 数量依据进入预算：扫描创建依据现在绑定库存版本；SQLite/PostgreSQL 库存预览仓储和推荐仓储读取最近数量依据，非当前版本降为 unknown。共享 FEFO 预算对 estimated/unknown 批次不声明确定覆盖，正常名称匹配仍保留并提示核对数量；制作草案及局部替换均传递此状态。手工实际修正数量后写入当前版本的 known/user 依据。没有识别依据的既有记录沿用原行为，不能用本次验证声称所有历史数据已完成依据回填。

验证：SQLite API 测试估计库存预览 unknown、手工改数量后 sufficient、再改版本后旧依据 unknown；服务端完整 307 项通过，包含 10 枚估计鸡蛋不能直接覆盖 2 枚需求的草案测试。PostgreSQL w37_integration_5 完整测试通过，验证库存预览和推荐仓储都读到 estimated，版本变化后 unknown；已停止临时实例。server TSC 通过。当前普通推荐卡片评分/过滤仍有按名称覆盖的路径，未全部替换；数量依据跨消费等受控变更的延续尚未实现，目前按版本失效处理。因此这仍是整套数量可信性的部分实现，不能据此关闭 #193。

2026-09-09 #193 普通推荐数量覆盖：推荐评分与 full/missing_few 筛选开始按菜谱产出份数和本次/长期人数缩放原料，复用整次 FEFO 预算。matchedIngredients 仅为已知足量项，missingIngredients 是已知缺口，uncertainIngredients 单独表示未知用量/估计库存；未知项不进入 full，也不被当成确定缺少一两样。覆盖率说明改为已知用量覆盖，评分版本提升 rules-2026-09-09.1 使旧推荐快照失效。库存页卡片优先使用服务端数量结果，不再用本地名称匹配覆盖；未知状态显示原料用量待核对，缺少新字段的旧响应同样不显示确定足量。

验证：服务端 308 项通过，新增库存 1 枚/需求 2 枚不 full、库存 2 枚足量、人数翻倍后不足、估计数量不 full/missing_few 的回归。原 API 测试缺少结构化库存数量的场景现在明确期待 unknown 和覆盖率 0。PostgreSQL w37_integration_6 完整实跑通过；已停止临时实例。全仓静态检查通过。菜谱详情页及匿名/离线的本地匹配逻辑仍需继续核对，完整制作衔接和设备验收未完成。

2026-09-09 #194 草案转餐次：新增 /meal-plans/:id/activate，在现有餐单事务内将草案的新做菜生成 meal_plan_items；保留目标餐次、计划份量、缩放原料与原方案待吃分配，更新 active 状态和版本。相同源版本重试复用原餐次；版本冲突、未解决餐次、已有实际项目或菜谱失效拒绝，不产生半套餐次。客户端增加转餐单入口，餐单页完成表单默认沿用 plannedServings。转换不会扣库存、记录摄入或自动占用待吃餐，仍要求执行前核对草案中的时间/存放条件。

验证：SQLite API 覆盖三份草案→三份餐次、六枚蛋用量、中文餐次展示、两个相同转换仅一项、现有入队接口成功、无摄入、失效菜谱不写项目或版本。PostgreSQL w37_integration_7 完整测试通过，包含并发转换和份量恢复；已停临时库。服务端完整 309 项、全仓静态检查与 Web export 通过。SQLite 计数基线同步新增仓储转换事务。烹饪队列/完成页对计划份量及多目标分配的完整传递、活动餐单的局部替换重校验仍未完成，不能关闭 #194。

2026-09-09 #194 计划份量进入队列：SQLite 62 / PostgreSQL 0004 新增 source_plan_item_id；普通菜谱队列与计划条目分别去重，不再将同菜谱不同餐次合并。餐单入队快照保存 plannedServings、计划日期和已缩放原料，队列读取优先保留计划原料；烹饪页面读取实际队列版本、默认计划产出份量并沿用原料和计划日期。取消后的再次入队使用新条目版本生成内部幂等键。

验证：服务端完整 309 项通过，SQLite 新增三份/六枚蛋快照及相同菜谱两餐独立队列断言；PostgreSQL w37_integration_8 完整测试通过，新增两份同菜谱计划的独立队列和份量恢复，临时实例已停止。全仓静态检查、数据库 schema 检查和 Web export 通过。SQLite 计数增加来自迁移入口的一次 schema exec，已同步基线，未在服务层引入数据库调用。旧版已合并队列的迁移恢复、多目标成品分配和活动餐单替换重校验仍未完成；尚未创建 PR、合并或分配 26w37b。

2026-09-09 #194 旧队列升级：迁移 62 / 0004 对活动队列的旧 queued 餐次关联回填 source_plan_item_id；同一队列多餐次时按稳定 ID 保留一项，其余恢复 planned 并清空队列关联、提升版本。保留原队列烹饪状态，恢复被保留餐次的原料及日期快照，同时刷新队列版本以拒绝旧页面提交。此步骤不影响库存或饮食记录。

验证：新增 SQLite 迁移回归覆盖共享队列拆分、六枚蛋快照、正在烹饪状态保留、迁移重跑幂等、拆开餐次独立入队与唯一约束。PostgreSQL w37_integration_9 先用临时旧结构运行真实 0004 SQL 验证相同行为，再完成全套迁移和仓储测试；实例已停止。服务端 310 项、server TSC、schema 检查通过。尚未覆盖真实旧设备升级后的交互；多目标成品分配、活动计划替换、#191 明确项自动接受/未知日期贯通及部署设备验收继续待办。

2026-09-09 #191 未知日期保存：库存创建/更新/读取契约允许 expiration_date 空字符串，明确表示未知，复用现有非空文本列而不写虚构日期；OpenAPI 同步。库存扫描和助手扫描保存不再强制日期，日期输入标注未知可留空，清空日期的字段依据标 unknown。库存、上下文、推荐和扣减仓储排序将未知日期放在有日期批次之后。现有日期徽标显示日期未知；临期范围查询不会纳入空日期。

SQLite API 回归覆盖粗略一袋、日期空值原样保存、克数仍未知、扣减不足以确认足量、非法文本日期仍拒绝和临期范围排除。完整服务端 311 项、全仓静态检查、契约兼容检查、客户端日期 6 项通过。PostgreSQL w37_integration_11 完整测试通过，验证未知日期与粗略数量原样读回，临期范围不误报；临时实例已停止。Web export 及补充 server TSC 通过。明确项自动接受、重复照片歧义与日期依据后续生命周期仍待完成，未关闭 #191。

2026-09-09 #191 服务端接受判定：新增 GET /inventory/scan-jobs/:jobId/review，从已鉴权的已完成 inventory_scan 任务读取持久化 vision 结果，结合现有可用库存判断。inventory-scan-v1 同时检查来源任务、置信度、名称歧义、同批同名/已有批次重叠、明确数量表达和存放位置，返回 automatic/review 及原因；保留识别数量 estimated、缺失日期 unknown。判定接口不写库存，不能把此状态理解为已经入库或人工确认。

验证：五项混合规则回归含明确数量、粗略歧义、位置未知、名称歧义、已有库存重叠；另验证同批同名全部待确认、缺少任务依据拒绝。API 验证服务端任务来源、稳定项目 ID、跨用户 404、未完成 409、无隐式库存写入。完整服务端 313 项与 server TSC 通过。自动接受专用事务、接受类型持久化和客户端自动保存/结果提示尚未接通，#191 仍未完成。

2026-09-09 #191 自动接受服务端入库：新增 POST /inventory/scan-jobs/:jobId/accept，仅接收任务身份，重读服务端判定，将明确项以 confirmed=false 交给现有事务入库，持久化 acceptance=automatic、acceptance_rule=inventory-scan-v1；人工批量入口继续要求 confirmed=true。未知日期保持空，识别依据不升级成人工事实。同任务固定幂等键复用原结果；事务内复查可用同名库存并保留已保存来源项目，PostgreSQL 自动入库额外按用户加锁防止两个任务并发重叠。批次载荷只写实际接受项目，保持结果索引对应。

验证：完整服务端 313 项通过，API 新增并发接受只一次、历史/批次明确自动接受与 confirmed=false。PostgreSQL w37_integration_12 完整通过，新增不同任务同名自动入库并发仅新增一项和审计类型验证；临时库已停止。server TSC 通过。SQLite 基线同步事务内复查查询，仍位于仓储。客户端扫描完成自动调用、已保存/待确认展示、自动项撤销及更完整的跨照片匹配仍待完成。

2026-09-09 #191 客户端自动接受衔接：自动入库响应增加 savedSourceItemIds，重试返回原保存项目的稳定标识。助手扫描完成后调用接受接口，列出已保存名称，仅把剩余项放入确认卡；全部自动保存时显示已保存。库存页恢复/打开任务同样接受明确项后排除已保存 ID，并刷新库存。扫描任务后续人工补充统一 image 来源，保持与自动入库相同来源身份，避免两个入口因 receipt/image 差异重复添加。

验证：服务端 313 项通过，API 新增并发接受两次返回同一 savedSourceItemIds 的断言；全仓静态检查和 Web export 通过。尚未进行真实相机/设备交互验收；完整自动入库撤销、已人工保存剩余项的再次恢复展示、重叠照片识别仍需继续。

2026-09-09 #191 已人工确认项目恢复：SQLite/PostgreSQL 仓储新增按用户及扫描任务读取全部入库批次的方法，复用来源项目身份恢复。接受接口在幂等自动批次后合并任务的人工确认结果，返回完整已保存项目 ID，不再按名称匹配。用户确认时改名不影响原始身份，重开任务也不会把该项重新列为待确认。

验证：API 覆盖先自动保存一项、手工改名保存第二项、重开仍两项且 ID 完整；服务端 313 项、server TSC 通过。PostgreSQL w37_integration_13 全套通过，额外验证各任务来源 ID 隔离和实际保存项目恢复；临时库已停止。SQLite 基线同步新增仓储读取。扫描批量撤销和真实设备断网恢复验收继续待办。

2026-09-09 #191 扫描整批撤销服务端：新增 POST /inventory/scan-jobs/:jobId/undo，校验任务归属后，从批次来源身份恢复本次新增项，逐项检查原版本。在单事务中软删除并提升版本，记录 intake-undo 幂等日志；任何已删除/消耗/修改项导致整批回滚，返回库存冲突并提示手动纠正当前数量。重复撤销跳过已有日志，不覆盖后续状态。PostgreSQL 与入库共用来源锁，并按库存 ID 顺序锁行。

验证：SQLite API 覆盖两项撤销、重复撤销零变更、撤销后接受重放不重新插入；冲突测试第二项修改后，第一项删除也回滚且无撤销日志。PostgreSQL w37_integration_14 全套通过，新增并发重复撤销仅一次、版本冲突整批回滚；临时实例已停止。服务端 314 项、server TSC 通过。SQLite 基线同步仓储事务读写。客户端撤销入口、撤销后恢复状态展示及真实设备验收仍待完成。

2026-09-09 #191 客户端撤销：助手确认卡支持撤销本次入库，包括已有自动接受项而尚有待确认项的情况；失败展示服务端冲突原因并保留卡片。库存页保存结果提示也可撤销并刷新库存。接受响应查询持久化撤销日志，返回 undoneSourceItemIds，展示已撤销并排除原库存快照，仍保留来源 ID 防止重新添加。助手卡增加 undone 状态，重新打开不会显示为已入库。

验证：API 撤销后重开返回零个有效保存项、两个撤销来源 ID，库存未恢复；PostgreSQL w37_integration_15 全套通过并覆盖撤销日志回读，实例已停止。服务端 314 项、全仓静态检查、Web export 通过。SQLite 基线同步仓储撤销记录读取。真实设备点击/网络恢复验收、跨照片重叠处理及其他里程碑待办仍未完成。

2026-09-09 #191 相同照片重传：识别任务入口按解码后的图片字节生成 inventory-photo SHA-256 幂等键；data URI 和裸 base64 的相同图片共享键。SQLite/PostgreSQL 对这一类库存扫描键允许复用超过普通 15 分钟窗口的活动/已完成任务，仍按用户隔离，失败任务不复用。重新上传同一照片得到原任务及原项目身份，沿用既有入库/撤销记录；入口返回已有完成状态，不再固定声称 queued 或未去重。

验证：315 项服务端测试通过，新增字节身份、两天前完成任务复用、跨用户隔离、失败重试；PostgreSQL w37_integration_16 全套通过，含并发复用及跨用户验证，临时库已停止。server TSC 通过。本项仅覆盖完全相同图片字节，裁剪/重编码/不同角度的近似图片仍依靠批次重叠待确认机制，真实照片与设备验收仍待完成。

2026-09-09 #193 过期库存预算：普通推荐、制作草案及替换预算排除当前日期前明确到期的原料；推荐评分提升 rules-2026-09-09.2 使旧快照失效。库存扣减预览以应用时区日期排除已过期批次，当天到期仍保留，未知日期不改写。底层数量预算接受显式参考日期，人工直接扣减/丢弃操作仍可处理历史库存。

验证：服务端 317 项与 server TSC 通过，新增过期库存不 full、不增加覆盖率、制作预算无过期扣减、当天到期与未知日期边界。既有 FEFO API 测试改用相对未来到期日期，继续验证早到期批次优先扣减。此轮为共享业务逻辑修改，未重跑 PostgreSQL 环境；最近一次完整 PostgreSQL 结果为 w37_integration_16。未来餐次的存放/复热条件与完整时间可行性仍待验收，不能据此关闭 #193。
