# 食光烙记 Beta 收口 TODO

> 更新于 2026-08-09。当前阶段是 **可信 Beta 收口**，不是继续扩充页面和 AI 能力。本文只把经过代码检查、自动化或真机验收确认的事项标为完成；“已经实现”不等于“已经在候选包中验证”。

> 本轮范围（2026-08-09）：只完成仓库内与双端本地体验收口，不执行 staging 部署、候选包生成或外部用户内测。主机与设备就绪不等于已经完成发布验收，相关项仍保持未勾选。

## 当前产品目标

先把一条核心闭环做真、做稳：

```text
录入家庭食材
→ 识别临期或可用食材
→ 获得真正能做的菜谱
→ 完成烹饪并扣减库存
→ 形成饮食记录
```

北极星指标：**每周完成至少一次上述完整闭环的用户数**。在这条链路被真实用户反复使用之前，暂缓新增 AI 入口、社区玩法和复杂健康指标。

产品阶段与量化门槛见 [产品路线图](docs/product-roadmap.md)，安装包验收步骤见 [真机验收与小范围内测清单](docs/device-beta-checklist.md)。

## 已验证的工程基线

- [x] client、server、admin 的 TypeScript/Lint 校验通过
- [x] 22 个测试文件、102 条自动化测试通过：client 46、server 52、admin 4
- [x] CI 使用冻结依赖，执行三端校验、测试、构建和生产依赖审计
- [x] 服务端核心库存、饮食、健康、权限和用户隔离已有集成测试
- [x] 仓库已有 Android 内部分发预览配置和候选 APK
- [ ] [#107](https://github.com/AdlinZ/DietDigiDose/issues/107) iOS 与 Android 真机完成同一版候选包的核心闭环验收
- [ ] [#106](https://github.com/AdlinZ/DietDigiDose/issues/106) 全新数据库、升级数据库和备份恢复均在 staging 完成演练

## P0：外部内测前必须完成

任何一项未完成，都只允许受控开发调试，不应邀请外部用户或录入真实健康数据。

### 1. 身份、隐私与会话安全

- [x] 将登录标识（邮箱/手机号）与公开昵称拆开，迁移已有账号；社区、评论和菜谱不得把登录标识当作作者名公开
- [x] 明确 AI 会保存完整对话和健康上下文的实际行为，补充首次告知、用户同意、可配置的第三方处理方说明、90 天默认保留期限及自助导出/删除路径
- [x] 原生端将登录 Token 从 AsyncStorage 迁移到安全存储；Web 使用标签页级 `sessionStorage`，退出时清除
- [x] 统一密码策略；注册、修改密码、管理员重置与客户端文案统一为至少 6 位且同时包含字母和数字

### 2. 核心闭环真实性

- [x] 统一 AI、首页和采购页的采购清单 key、数据结构与用户隔离，确保“AI 加入采购清单”后正式采购页可见
- [x] 采购页“一键入库”改为调用服务端库存 API；成功后库存页能立即读取，不再只写本地 AsyncStorage
- [x] 修复或移除首页不存在的 `/recipe-generator` 路由
- [x] 完成烹饪、库存扣减和饮食记录采用可重试且幂等的服务端事务，避免半完成状态和重复扣减
- [x] 删除或明确标记所有伪结果：小票识别不生成默认条目，采购清单不自动填示例，固定天气、假同步状态和假清缓存已移除
- [x] 演示用户、社区内容和高互动数据只在 `ENABLE_DEMO_SEED=1` 时生成，生产与 staging 默认关闭
- [x] 菜谱增加可信、营养估算、待复核质量状态；固定分类营养兜底、异常食材解析和不合理时长退出公开列表与推荐
- [x] v29 迁移回填现有菜谱；管理端支持质量筛选、问题提示、设为可信或待复核并记录审核人、时间与原因
- [x] 做饭模式只接收 `recipeId` 并读取最新菜谱；登录使用内部白名单 `returnTo` 续接原页面，不自动重放写操作

### 3. 数据库与发布环境

- [x] 为 `user_custom_foods.status` 增加版本化迁移，并通过空数据库迁移集成测试验证
- [ ] [#105](https://github.com/AdlinZ/DietDigiDose/issues/105) 部署独立、可持久化、仅通过 HTTPS 访问的 staging；验证 `DATABASE_PATH`、CORS、JWT、管理员初始密码和反向代理设置
- [x] 移除候选配置中的明文 HTTP 地址与不安全网络开关；EAS 候选/生产构建缺少 HTTPS API 时直接失败
- [x] 补齐 iOS bundle identifier、构建号与 EAS preview/production 双端配置；候选构建缺少 HTTPS API 时直接失败
- [x] 独立 `preview-http` 与 simulator profile 才允许显式 HTTP，并由配置回归测试锁定正式 preview/production profile 的 HTTPS 约束；HTTP 包仅用于受控测试账号
- [ ] [#107](https://github.com/AdlinZ/DietDigiDose/issues/107) 基于同一提交和同一 staging 后端生成 iOS/Android 双端候选包
- [ ] [#106](https://github.com/AdlinZ/DietDigiDose/issues/106) 完成备份、隔离恢复、健康检查、登录、库存读取和管理统计演练，记录恢复时间与回滚步骤

## P1：5～10 人封闭内测前完成

### 稳定性与异常恢复

- [x] 为 AI、食品数据和推送等外部请求设置统一服务端超时，客户端保留可理解的失败与重试状态
- [x] 扫描任务使用服务端持久化状态；启动时回收超过 15 分钟的 `queued/processing` 任务并提示重新上传
- [x] 受保护页面使用统一登录守卫；未登录、401、空数据与网络错误必须是不同状态
- [x] 首页、我的和库存等聚合请求拆分错误边界，辅助接口失败不应清空整页数据
- [x] 修复一次性路由参数清理，避免返回页面后重复打开弹窗或重复执行动作
- [x] 对登录、匿名外部查询和高成本 AI 接口使用可跨实例共享的限流/配额策略

### 候选包与质量保障

- [x] 为“采购清单 → 服务端库存”和“烹饪 → 扣减 → 饮食记录”的事务、幂等重试和用户隔离补集成测试
- [x] 增加注册、登录、库存、菜谱、烹饪的端到端冒烟测试
- [ ] 在 iOS 与 Android 真机验证权限拒绝、相机/相册、键盘、安全区、前后台、弱网和升级安装
- [x] 接入错误监控与核心漏斗事件；事件使用匿名标识，不采集不必要的健康内容或完整 AI 文本
- [x] 给主分支启用保护规则，并把 CI 设为合并必需检查
- [x] 移除安装脚本中的动态依赖安装/npm 回退，使冻结安装可复现
- [x] 统一根包、客户端和服务端的版本号来源与发布标记

## P2：验证留存后再投入

- [x] 将库存、AI、首页等超大页面拆出可测试的数据 hooks、筛选器、消息列表和弹窗模块，并为库存筛选/排序补单元测试
- [x] 将品牌色、字号、间距、圆角和交互状态收敛到 `client/global.css` 的语义 design tokens，并迁移核心页面与共享组件
- [x] 为 Tab、认证表单、核心按钮和动态弹窗补无障碍角色、状态、实时播报及 44pt 触控区
- [x] 共享 `Screen` 根据导航焦点隐藏非活动页面的无障碍树，并为浮动 Tab Bar 预留底部内容空间
- [ ] 在 iOS/Android 真机验证系统字体放大、VoiceOver 与 TalkBack 阅读顺序
- [x] 为社区、菜谱和管理端用户/食谱/社区列表补不透明游标分页，并覆盖连续页无重复测试
- [x] 新增认证、限流、MIME/魔数/大小校验的社区对象存储上传路径，新发布内容禁止把 Base64 图片写入数据库
- [ ] [#108](https://github.com/AdlinZ/DietDigiDose/issues/108) 在 staging 创建受控媒体 bucket，并迁移已有内联社区图片后核验访问与删除策略
- [ ] 只有当单机 SQLite 的并发、备份窗口或部署方式成为实测瓶颈时，再迁移 Postgres 和任务队列
- [ ] 根据真实漏斗决定优化方向，不以页面数量或 AI 功能数量作为阶段成果

## Beta 发布门槛

- [ ] P0 为零，所有 P1 发布例外均有负责人、截止时间和可接受风险说明
- [ ] 同一提交、同一 staging 后端的 iOS/Android 候选包通过完整真机清单
- [x] `pnpm -w lint:all`、`pnpm -w test:all` 和三端本地构建通过；`pnpm -w audit:prod` 仅保留 `CVE-2025-71329`、`CVE-2025-71330` 两项 Expo/Metro 构建链临时例外
- [ ] 每次 Expo/Metro 升级及每次 Beta 候选验收前复核上述两项 `image-size` 例外；上游提供修复后立即移除，审计结果不得描述为“零漏洞”
- [ ] 备份可恢复、错误可定位、核心漏斗可观察、数据删除请求可执行
- [ ] 先邀请 5～10 位用户连续使用 7 天，不录入无法承受泄露风险的真实敏感信息
- [ ] 以人数而非小样本百分比做首轮判断：10 人中至少 8 人完成激活、6 人录入 3 种以上食材、4 人完成一次完整闭环、3 人在第 7 天回访或再次完成核心动作

## 推荐执行顺序

1. 先修身份泄露、采购/库存断链、伪结果、数据库迁移和 HTTPS 五类 P0。
2. 建立可持久化 staging，完成空库、升级库、恢复库三类演练。
3. 生成同版本双端候选包，按真机清单完成内部验收。
4. 接入最小核心漏斗后，开展 5～10 人、7 天封闭内测。
5. 只针对最大流失点安排下一轮开发；没有留存信号时，不扩大功能面或基础设施。
