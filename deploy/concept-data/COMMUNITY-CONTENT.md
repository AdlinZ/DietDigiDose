# 平台社区内容与可导入数据包 · 2026-09-15

## 榜单补充更新

用户指出榜单为空后，已补充 3 条 `category=榜单` 的平台数据榜单（帖子 317–319）：8 道主要营养可估算菜谱的原料项数榜、20 道家常菜原料覆盖榜、18 条明确厨具关系的出场榜。每条说明样本、排序和统计口径，不伪造热度。原先的榜单页面只查询本分类，不会自动纳入寻味帖子。

最新完整包为 `artifacts/base-data/system-data-2026-09-15.2.zip`，SHA256 `5bc9345bf6e232e34a0c6cbd4091d55f1a7f1ecce3f2e930f847431c19db3f30`。社区共 19 条平台帖子、12 条平台答复。本次保留前 16 条内容及活动日期，仅新增 3 条；不同版本中内容摘要相同的稳定 ID 不重复导入，内容变化仍拒绝静默覆盖。

4 项导入内容测试、可重复 ZIP 构建、独立 PostgreSQL 回滚/演练/升级/重复导入测试通过。正式榜单推荐接口逐条核对 3 条正文一致。个人数据摘要保持一致。备份与报告在 `/opt/dietdigidose/backups/community-ranking-20260915/`。以下保留第一版历史记录。

已按用户确认的“活动、问答等社区内容”范围发布，使用目标库已有管理员 `admin`，不创建虚构用户或专家身份。

## 线上结果

- 4 个平台线上活动（帖子 301–304）：2 个进行中、2 个预告。首次启用时间为 2026-09-15 16:46:26（上海）。
- 12 个平台常见问题（帖子 305–316），各有一条明确标注的“平台参考答复”；问题保持开放，未伪造采纳或专家审核。
- 活动说明包含参与方法、记录模板与期限；不设收费、奖品或排名。没有导入点赞、报名或虚构互动。
- 原 100 条寻味帖子及 6 条评论保留；新增后共 116 条帖子、18 条评论。
- 正式导入前后账号、个人库存、收藏和个人厨具摘要一致。
- 未修改服务和网页程序，不需要重新打包客户端。数据实验室仍关闭。

## 可导入包

本地：`artifacts/base-data/system-data-2026-09-15.1.zip`（约 2.45 MiB）。

SHA256：`77f958b0ef5d2315f0585dab791edbdce011673588b5cbd5dd825a7c76ed681a`。

包含 1,079 个食材概念入口、385 个菜品入口/389 种做法、261 项厨具、营养补充及本次 16 条平台帖子/12 条答复。不是个人数据库备份，原有用户内容、账号和互动记录不导出。原 100 条演示寻味帖子也不混入基础包。

构建：`python datasets/base-data/build_system_package.py`，输出目录必须不存在。`community_content.py` 保存平台原创内容。包内 README 给出完整导入方式。

导入到已完成当前项目 PostgreSQL 初始化/迁移的系统：设置 `DATABASE_URL`，多个管理员时指定 `COMMUNITY_AUTHOR_ID`；在解压目录安装依赖后运行 `pnpm check` 演练、`pnpm import` 提交。三个阶段通过保存点在同一外层事务中执行；结果报告的阶段完成状态不等于外层事务已提交，以顶层 mode 为准。

`platform_content_imports` 保存稳定内容 ID、摘要及首次活动日期，`system_data_package_imports` 保存完整包版本。重复导入不会重发帖子或顺延活动，也不会重置已存在的基础目录。活动日期可通过首次导入时的 `COMMUNITY_ACTIVATION_AT` 调整。

## 验证与运维

### 2026-09-15 社区空列表缓存修复

- 社区帖子列表停止使用应用持久缓存，避免旧空结果遮住后来发布的榜单；空状态增加“重新加载”按钮。
- 回归测试覆盖“持久缓存为空、服务端已有新帖子”的场景；客户端相关 23 项测试、客户端 lint 和 Web export 通过。
- Web 发布目录：`/opt/dietdigidose/releases/community-feed-cache-20260915`。仅更新 Web 服务，保留旧静态资源及原发布目录以支持回退。
- 已在生产浏览器刷新并点击“榜单”，DOM 和截图确认显示“共 3 个榜单”及厨具、原料覆盖、原料项数三份内容。

- 11 项内容/社区针对性测试通过。
- 重建 ZIP 字节完全一致，包内 24 个文件校验通过。
- 两个独立 PostgreSQL 验证库分别覆盖空基础目录和已有完整目录。故意在最后一阶段指定不存在的管理员，验证所有业务记录回滚；演练无业务写入；提交成功；重复导入无变化。
- 空目录导入得到 1,079 食材、389 做法、261 厨具、16 帖子、12 答复；目标数据库数字 ID 自动重映射。
- 线上活动和问答列表逐条检查通过；12 条参考答复、管理员身份、活动时间、未采纳状态核对通过。
- 未执行新的页面视觉验证，采用既有页面与线上接口验证。

服务器数据包：`/opt/dietdigidose/releases/system-data-2026-09-15.1.zip` 和同名解压目录。

备份与验证报告：`/opt/dietdigidose/backups/community-content-20260915/`，包含 `database.before.dump`、`fresh-verification.json`、`existing-verification.json`、`production-import.json`、`production-api.json`。

若需撤下，只软删除 `platform_content_imports` 关联的本批帖子，保留导入映射和后续真实评论/互动。不要覆盖整个数据库或删除已有社区内容。新一轮活动使用新逻辑 ID，不复用本批已结束活动。
