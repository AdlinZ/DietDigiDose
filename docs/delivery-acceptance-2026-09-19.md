# 已有代码交付验收（2026-09-19）

后续进展：本批内容已完成服务器与网页版部署，新增 AI 路径未通过完整真实提供商验收，已关闭新主助手开关。详见[部署验收记录](deployment-acceptance-2026-09-19.md)。下文保留部署前的本地验收时间点与证据。

## 结论与范围

**本地代码验收通过，发布验收尚未完成。** 本次检查包含 main 基线 `90f7889cc0bc96c1a1faa6532d678da37df19ada` 和开始时已有的 115 个已修改／未跟踪文件，以及本次修复的测试隔离配置。不是只验证已提交的 main，也不代表这些工作区改动已经部署。

重点验收 #222 反馈、#220 计时、#221 已登录修改密码、#179 提醒操作与归因、#66 厨具功能约束、#218 管理端解析，以及 #224–#230 的已有 AI 实现；其余既有模块纳入全量回归。没有扩展实现 #219 更新、#223 绑定／换绑、#221 自助重设或 #186 家庭来源关联。

环境：macOS arm64，Node **22.23.2**，pnpm **10.18.0**；SQLite 临时数据库，独立 PostgreSQL **17.11** 临时实例。CI 使用 PostgreSQL 16，且有 Linux / Windows 检查，本地通过不能替代这些平台的最终 CI。浏览器使用本次构建的客户端／管理端静态产物、真实本地 Express API 与合成账号。AI 使用本地 OpenAI 兼容模拟提供商，没有调用真实模型、短信或 Expo 推送。

## 本次发现并修复

首次全量测试和 SQLite 恢复演练错误继承本机 `.env` 中的 PostgreSQL 驱动，尝试连接 `127.0.0.1:5432`，报 `ECONNREFUSED`。这违反了这些用例只使用各自临时 SQLite 的预期。

修复：在 `agentConversation.test.ts`、`feedbackFlow.test.ts`、`app.test.ts`、`milestone35.test.ts`，以及 `database-rehearsal.ts` 的子进程环境中明确设置 `DATABASE_DRIVER=sqlite`、`DATABASE_URL=""`。保留原临时数据库路径，没有修改开发环境配置，也没有放宽断言。修复后全量测试、覆盖率、恢复演练和两轮稳定性检查均通过。

## 自动化与构建结果

日志位于 `artifacts/acceptance-2026-09-19/`，该目录按仓库规则不提交。

| 检查 | 本次结果 | 主要证据 |
| --- | --- | --- |
| 三端静态检查 | 客户端 TSC / ESLint、服务端 TSC、管理端 Oxlint 通过；管理端 TSC 随构建通过；修复后服务端 TSC 再次通过 | `lint.log`、`lint-server-fixed.log`、`build-admin.log` |
| 全量测试 | 客户端 65 套 / 250 项，服务端 520 项通过 / 1 项条件跳过，管理端 6 套 / 21 项；发布、staging smoke、上游检查规则另有 17 项通过 | `tests-fixed.log` |
| 条件跳过的 PostgreSQL 队列测试 | 单独提供独立数据库运行，1 项通过、0 跳过 | `postgres-queue.log` |
| 覆盖率 | 客户端语句 57.72%、分支 54.62%、函数 45.78%、行 60.03%；服务端行 88.54%、分支 81.34%、函数 89.35%；全部达到原门槛 | `coverage.log` 的客户端部分、`coverage-server-fixed.log` |
| 两轮稳定性 | 两轮 `test:all` 均退出 0；约 45.6 秒、45.0 秒 | `stability.log` |
| 共享契约与 OpenAPI | 8 项通过，生成物一致，向后兼容检查通过 | 本轮工具输出 |
| 架构与数据库边界 | 模块边界通过；SQLite 保持 72 个既有适配／基础设施文件；PostgreSQL 115 表、184 外键，Drizzle 检查通过 | 本轮工具输出 |
| SQLite 空库／升级／恢复／回退 | 完整演练通过，包括恢复后的 HTTP 业务验证 | `sqlite-rehearsal-fixed.log` |
| PostgreSQL 集成 | 迁移、仓库、事务、并发、备份恢复及恢复后 HTTP 运行时通过；包括新反馈和提醒归因 | `postgres.log` |
| 基础数据厨具修复 | PostgreSQL 独立测试通过，保留人工审核映射及原子回滚 | `base-data-import.log` |
| 数据包测试 | 107 项中 84 项通过、23 项历史重建用例按原条件跳过；当前冻结包与营养重算测试通过 | `datasets.log` |
| 发布声明与国内外配置 | 工具链、版本元数据、两种 provider profile 的 Expo 公共配置检查通过；未分配新快照 | `providers.log`、本轮工具输出 |
| 三端构建与预算 | Expo Web export、Express、管理端 TypeScript / Vite 构建通过，原预算不变 | `build-*.log`、`build-budgets-final.log` |
| 测试隔离登记与差异格式 | quarantine 检查无 focused / 未登记 skip，`git diff --check` 通过 | 本轮工具输出 |
| 生产依赖安全审计 | **未完成**：npm registry TLS 连接失败 `ECONNRESET`，curl 独立复核同样失败，不能认定零新增漏洞 | `audit.log`、`registry-connectivity.log` |

`coverage.log` 保留修复前失败的服务端结果；最终服务端结论以 `coverage-server-fixed.log` 为准。同样，`tests.log` 和 `sqlite-rehearsal.log` 保留原始失败证据，不应引用为最终结果。

未通过网络重新下载全部历史数据输入；23 项跳过不能计为通过，也不能以本次数据包测试代替许可证、来源与云端内容审核。

## 浏览器 → API → 数据验收

使用本地 `http://localhost:8080` 客户端、`http://localhost:5173` 管理端与 `127.0.0.1:9090` API。agent-browser CLI 不在本机，安装时 registry 网络失败，改用已连接的 Computer Use 浏览器完成操作、截图和控制台检查。浏览器实际读取的业务结果再用临时 SQLite 独立核对，摘要保存为 `ui-business-evidence.json`。

| 流程 | 本次可复核结果 |
| --- | --- |
| 首页与登录 | 客户端和管理端生产构建可见、登录正常，读取本地 API，未见空白页或错误覆盖层。 |
| #222 用户提交 | 表单提交后直接进入反馈 #1；后端返回 201，页面显示“已收到”。 |
| #222 管理处理 | 管理员公开回复并改为“待用户补充”；用户收到站内通知，点击返回同一反馈。 |
| #222 内部备注 | 管理端保存内部标记，用户详情只显示公开回复，没有泄露内部备注。 |
| #222 用户补充 | 用户在原反馈内补充，用户／管理端状态均为“处理中”；数据库只有同一条反馈，version=4。 |
| #220 计时 | 2:00 开始倒计时，朗读开关切换后保留 1:53；刷新后按经过时间恢复到 1:34；暂停并切换到建议 3 分钟的下一步时仍保留 1:27，没有重置。 |
| #227–#228 追问接续 | “安排备餐”立即出现运行中任务卡和取消入口；模拟模型追问“为几个人？”；底部回复“两个人”后完成原 run。数据库仅一个对应 run，刷新后最终答复与任务仍保留。 |
| #229 批准编辑 | “记录喝水300毫升”进入待批准；业务表单改为500后批准；任务继续运行并完成。数据库恰好一个 executed 动作、一条 water_ml=500 记录。 |
| #226 配置展示 | 管理端展示主助手／规划／图片／转写最终模型、来源与调用预算，并明确真实能力尚未验证。 |
| #179 库存提醒 | 合成提醒展示准确的500g库存，点击“整批已使用”先展示影响并要求再次确认；确认后库存关闭、提醒已处理、投递取消，归因为一次 inventory_used。 |
| #179 归因展示 | 管理端显示候选1、已处理1、确认使用1，实际开始烹饪0；数据库饮食记录为0，没有把处理库存误记为饮食或烹饪。 |
| #221 修改密码 | 页面显示当前／新／确认密码及重新登录说明；提交、校验、旧会话失效由组件和 HTTP 自动化覆盖。本轮没有在浏览器手动提交密码变更。 |

上述页面检查未观察到浏览器控制台 error。模拟模型用于验证状态与写入链路，不能证明真实模型语义效果、首字延迟或线上提速。提醒候选由隔离夹具构造，不代表真实扫描吞吐和推送已验收。

## Issue 的交付判断

| 范围 | 本次可接受的部分 | 仍需完成 |
| --- | --- | --- |
| #222 | 用户／管理端反馈闭环、隔离与事务、本地双数据库回归 | 部署后目标环境冒烟 |
| #201–#205、#184 | 既有修复纳入本次全量回归并通过 | 提交后的 CI、目标包回归；没有仅因 issue 开放就判定未实现 |
| #189–#199 | 既有库存、待吃餐、计划、偏好、家庭逻辑纳入接口与双数据库回归 | 完整真实数据与设备场景；本次未逐项宣称整个需求全部验收 |
| #220 | 计时恢复、隔离、通知调度自动化与 Web 页面流程 | Android/iOS 锁屏、后台、通知权限及系统强制停止 |
| #221 | 已登录修改密码部分 | 自助重设仍未实现，整个 issue 不可关闭 |
| #179 | 类型化操作、库存结果／真实开始归因、双数据库事务和管理统计 | 真实 Expo 推送、设备及生产规模吞吐；默认主动开关未开启 |
| #66 | 设备功能与同设备条件约束自动化通过 | 目录语义、历史映射审核、真实设备说明和完整覆盖 |
| #218 | 管理端 JSON 文本／原生数组兼容的模型测试与构建通过 | 修复部署、真实云端食谱补全审核发布、Android 刷新；本次未改云端记录 |
| #224–#230 | 已有任务、历史、预算、安全与批准链路本地回归通过 | 真提供商能力矩阵、改前／改后性能、真实旧 checkpoint 与原生交互 |
| #106 | SQLite 与 PostgreSQL 本地演练通过 | 目标部署的独立备份／恢复演练，CI PostgreSQL16验证 |
| #178、#187 | 当前数据包结构和冻结产物的自动化检查通过 | 来源／许可／语义审核与正式发布，不等同于基础数据v1已交付 |
| #186 | 已有个人闭环统计纳入自动化回归 | 家庭来源关联仍有代码缺口；不以未知数据冒充0或已支持 |
| #17、#62、#63、#95、#183 | 对应已有组件／服务逻辑纳入全量回归 | 评论键盘、持续录音、授权音色、ONNX性能及原生音频真机验收 |
| #105、#107、#108、#171、#185 | 仓库内已有配置／脚本测试与构建通过 | staging、媒体迁移、双端同提交候选包、签名连续和覆盖安装 |
| #98 | 上游检查规则测试通过 | 联网生产依赖审计与当前上游状态复核，本次没有更新漏洞状态 |
| #96 | 本轮模块边界和数据库架构检查通过 | Epic 按各子项及部署适配证据收口，不凭静态检查关闭 |
| #219、#223 | 不在“已有完整实现”的范围 | 应用内更新、手机号绑定／换绑等代码缺口继续保留 |

## 发布前余项

1. 在可访问 npm registry 的环境完成 `pnpm -w audit:prod`，处理任何新增告警。
2. 将最终工作区内容作为明确提交交付，运行同提交 Linux / Windows CI 与 PostgreSQL16集成；本次工作区文件指纹另存 `source-manifest.json`。
3. 部署到指定验收环境，确认数据库迁移和服务端／客户端版本一致，再进行目标环境冒烟。
4. 按各 issue 要求完成真实模型、短信／绑定、设备通知、录音／音色、签名覆盖安装及云端内容审核。当前本机没有可用 adb，`xcrun devicectl` 也不可用。

本次未发布或部署、未生成APK、未分配打包编号、未发送真实短信或系统推送、未关闭远端issue。验收用服务及独立PostgreSQL实例在收尾时停止。
