# 全部未关闭 issue 实施记录

基线：865987c；范围为 2026-09-12 拉取的 34 个 open issue。完整快照在本地 artifacts/issue-implementation/open-issues.json。未经验证的事项不标为完成，不自动关闭 GitHub issue。

## 本轮已实现

- #201：统一账号重试键、旧键迁移及退出清理，账号代次失效，退出等待在途写入/删除；正常缓存清理保留待办。
- #202：请求序号阻止旧 GET 回写，按版本合并且保留已吃完记录的版本，重试成功刷新当前餐食。
- #203：关联删除必须明确选择撤销误记/仅删摄入；SQLite v63 / PostgreSQL 0005 保存独立纠正凭据，事务恢复余量但不返还原料；重复撤销去重，后续变更拒绝覆盖。
- #204：制作发送前持久化完整请求；重启精确恢复原幂等键、份量、日期、库存版本；存储失败不发送，未知响应不替换请求，成功后清理。
- #205：份量统一六位小数，最小 0.000001 份，已有 0.0005 余量可以明确食用/丢弃。

验证：pnpm test:all 通过（服务端 320 项，管理端 16 项；客户端全套通过）；contracts:check、lint:all、database:schema:check、architecture:check 通过。PostgreSQL 临时独立数据库全套集成通过，覆盖精度并发、纠正失败回滚、重复撤销与后续变更。SQLite adapter 与迁移新增访问已更新边界基线，无业务层新增直接 SQL。使用本机 Node 25.2.1，仓库声明的发布工具链为 Node 22.23.2，发布构建另行验证。

## 已有实现复验

#183/#185：语音与构建配置 23 项通过；#184：媒体清理 4 项通过，含 25 个失败任务后的有效任务与恢复。相关真机录音、APK 并存和覆盖升级验收仍未完成。

## 范围清单

| Issue | 标题 | 状态 |
| --- | --- | --- |
| #205 | [P2] 份量精度与最小消耗量不一致，产生无法清空的待吃余量 | 本轮实现和自动回归通过 |
| #204 | [P1] 非队列烹饪完成请求在重启后丢失幂等身份，可能重复记账 | 本轮实现和自动回归通过 |
| #203 | [P1][data-integrity] 删除待吃餐生成的饮食记录未处理关联食用事件 | 本轮实现和自动回归通过 |
| #202 | [P2][ui] 待吃餐幂等重试返回旧快照导致界面余量倒退 | 本轮实现和自动回归通过 |
| #201 | [P1][privacy] 待吃餐重试记录未纳入账号退出清理 | 本轮实现和自动回归通过 |
| #199 | feat(households): 将个人周计划扩展为家庭共餐和独立食用记录 | 待逐项核对并完成 |
| #198 | feat(planning): 每日与业务事件触发的可恢复局部重规划 | 待逐项核对并完成 |
| #197 | feat(preferences): 从内部行为学习可解释、可纠正的个人偏好 | 待逐项核对并完成 |
| #196 | feat(planning): 生成个人滚动七日计划及合并采购建议 | 预览/预算/草案实现并回归通过；关联重算待补 |
| #195 | feat(meal-plans): 建立自动修改边界与可审阅变更记录 | 餐次及计划级保护与审阅已回归；后续 worker 接入继续审计 |
| #194 | [P1][feat] 将方案局部替换、制作分配和食用更新贯通到真实记录 | 待逐项核对并完成 |
| #193 | [P1][feat] 按待吃餐、库存份量和总耗时生成一套多餐方案 | 待逐项核对并完成 |
| #192 | [P1][feat] 扩展常用备餐偏好并支持一次性语音覆盖 | 待逐项核对并完成 |
| #191 | [P1][feat] 明确识别项自动入库，模糊项确认，支持撤销 | 待逐项核对并完成 |
| #190 | [P1][feat] 分离制作完成、待吃餐与实际食用 | 待逐项核对并完成 |
| #189 | [P1][fix] Agent 库存操作复用结构化数量与部分消耗规则 | 待逐项核对并完成 |
| #187 | [P1][data] 整理并发布版本化基础数据包 v1 | 待逐项核对并完成 |
| #186 | [P2][metrics] 建立每周核心闭环用户数的统计与验收 | 待逐项核对并完成 |
| #185 | [P2][release] 修复示例环境变量覆盖 preview 隔离包名 | 已有代码，复验通过；设备证据待补 |
| #184 | [P2][storage] 避免旧域名媒体清理任务阻塞后续批次 | 已有代码，复验通过；设备证据待补 |
| #183 | [P1][voice] 修复静音切换并发与断网时麦克风继续录音 | 已有代码，复验通过；设备证据待补 |
| #179 | feat: 主动食物决策——临期抢救与用餐窗口提醒 | 待逐项核对并完成 |
| #178 | data: 中国食品洁净室数据集 v1（CC0） | 待逐项核对并完成 |
| #171 | [P1][Release] HTTPS preview 持久签名与覆盖升级验收 | 待逐项核对并完成 |
| #108 | [P1][storage] 配置 staging 媒体 bucket 并迁移历史内联图片 | 待逐项核对并完成 |
| #107 | [P0][release] 生成同提交双端候选包并完成核心闭环真机验收 | 待逐项核对并完成 |
| #106 | [P0][data] 完成空库、升级库与备份恢复演练 | 待逐项核对并完成 |
| #105 | [P0][release] 部署独立 HTTPS staging 并验证持久化配置 | 待逐项核对并完成 |
| #98 | [P1][security][upstream-blocked] 跟踪 image-size 构建链拒绝服务漏洞 | 待逐项核对并完成 |
| #96 | [架构治理][Epic] 收敛为可维护的 TypeScript 模块化单体并建立国内外适配基线 | 待逐项核对并完成 |
| #95 | [语音包] 收口最小语音管理闭环并接入内部 ONNX 测试音色 | 待逐项核对并完成 |
| #66 | feat(kitchenware): 扩充厨具目录及菜谱能力映射 | 待逐项核对并完成 |
| #63 | feat(voice): 支持本地个人音色包与三级语音降级 | 待逐项核对并完成 |
| #62 | feat(cooking): 支持可打断的实时双向语音烹饪助手 | 待逐项核对并完成 |

## 工作区边界

开始时已有 server/src/modules/aiToolData/service.ts、server/test/aiToolDataModule.test.ts、docs/base-data-rc7-import.md、server/scripts/import-base-data-runtime.mjs 及 artifacts 改动，已保留，尚未作为本轮成果提交。无新打包编号分配。

## #195 计划变更保护（2026-09-12）

新增 SQLite v64 / PostgreSQL 0006：餐次确认事实、变更历史与稳定建议指纹。当前 active 旧计划的餐次在升级时保守标为已确认，显式激活草案也保存确认。餐次修改统一在仓储事务内决策：未确认应用；已确认或关联采购 checked 只存建议；已入队/制作/完成保留。加入购物清单本身不等于购买。历史保留原因、来源、原版本、应用版本、前后差异、应用时间及恢复元数据。

界面提供确认、审阅、接受/拒绝和恢复。接受并发只应用一次；拒绝后同一未变化建议返回原拒绝结果；确认、采购版本、队列状态变化使旧建议/恢复冲突；无实际字段变化不产生新版本或日志。保护范围目前为餐次修改入口；整个计划的归档/元数据变更、Agent 计划级操作与后续 worker 接入仍需继续审计，不能把 #195 全范围宣称验收完成。

验证：客户端整套 160 项、服务端 322 项、管理端 16 项通过；新增审阅组件另有 2 项交互回归通过。PostgreSQL 独立空库全套（含并发接受、拒绝去重、采购后恢复冲突、制作保护）通过；SQLite 旧队列升级夹具扩充真实字段后验证进度与确认状态保留。Expo Web export 成功，未分配构建编号。最终恢复审计调整后再次执行定向 API、PostgreSQL 与全仓静态检查。

下一步：完成 #195 计划级边界审计，再推进 #196 七日规划。已有推荐模块完整查询库存，但 buildCookingDraft 仍按当前日期过滤库存、整套一次制作计时，allocatePreparedMeals 无可用期限字段；周规划需要跨日预算、保留现有安排、合并采购来源与明确不确定性，不能直接循环七次既有单餐查询。#197–#199、#186 与数据/部署/设备相关问题均未完成。

当前工作分支 codex/all-open-issues，第一批已提交 43f0b95。无推送、PR、合并或 issue 关闭。本地临时 PostgreSQL 数据目录 /tmp/dietdigidose-issues-pg.ZfvImf/data，端口 55439；测试必须新建空库，不能对已有业务库运行迁移集成。日志在 artifacts/issue-implementation/。

## #195 计划级保护与 #196 七日预览（第二批）

计划级 update/archive 与 Agent 更新、撤销入口现在在原事务内检查餐次保护。已确认/采购/制作安排不得通过取消计划、缩小日期范围或更换约束绕过保护，单独改标题仍允许。SQLite API 和 PostgreSQL 集成覆盖直接入口与 Agent 撤销，全部通过。

新增七日只读规划 API 和客户端入口：查询完整库存、既有安排、活跃草案的待吃分配及购物清单，先预留原安排的原料和待吃份量，再填空缺；按目标日期过滤已知过期库存，七日共享 FEFO 数量预算，统一可换算单位但不合并不可换算规格。缺口保留餐次来源，用户购物项保持不变。每餐单独检查制作时间；数据未知会明确提示待核对或未满足，未擅自认定存放/携带/复热适用。草案保存复用编号，账号切换丢弃旧响应；周草案激活检查已占用餐次，防止预览后新增安排被重复覆盖。

验证：客户端 162 项、服务端 327 项、管理端 16 项通过；新增 UI 覆盖跨账号旧响应和保存重试身份；API 验证第 17 项库存参与、只读无扣减、过时激活冲突；算法验证十枚鸡蛋需求/四枚库存/六枚缺口、跨日到期、待吃份量预留、未知原料与耗时限制。独立空 PostgreSQL 全套通过，含新 planningState 查询的账号隔离、现有安排保留及不改库存。lint:all、contracts:check、architecture:check、database:schema:check、SQLite 边界更新和 Expo Web export 通过，未分配打包编号。

仍未完成的 #196 验收：周草案局部换菜尚沿用单次方案的原料重算，需要按目标日期重新预算并扣除草案外原安排占用；采购完成/取消后的关联缺口更新需贯通。待吃餐没有已核实可用期限字段，当前明确提示未知；不得把提示等同于已验证可执行。周激活 advisory lock 已串行化激活入口，Agent 新建/移动餐次等并发入口还需统一审计。#197/#198/#199 与其他清单中未验收事项保持未完成。继续从这些缺口推进，不关闭总目标或 issue。

## #196 周草案局部换菜重算

抽取一次性只读库存预算器供首次周规划和换菜共用。周草案换菜重新查询当前账号、日期范围内的已有安排，预留原安排后按日期和餐次顺序预算新增餐次，已知过期原料不会用于未来餐次。原安排用量未知时不再声称新增需求已被该库存覆盖。其余新做菜和待吃分配保持原值。合并采购缺口及来源写入草案契约，保存/恢复后可见；恢复的周草案隐藏两餐重新生成表单，提供七日重新核对入口，早餐/加餐标签正确显示。

新增验证覆盖：换菜后旧计划两枚鸡蛋占用、跨日到期、其他餐次不变；取消旧餐次及采购入库后重新计算缺口，手工购物项不变；服务级确认按当前账号和七日期间重新查询占用。17 项推荐/周规划模块、4 项相关 API、客户端 162 项通过，lint:all、contracts:check、Web export 通过；无数据库模式或仓储 SQL 变更，未重复运行 PostgreSQL 全套。

仍需继续：#195/#196 并发餐次写入入口统一审计、待吃期限与复热条件完整模型；#197–#199 及范围表其他事项。上述取消/采购回归证明主动重新核对结果正确，自动事件触发更新属于 #198，尚未实现，不混同验收。

## #195/#196 PostgreSQL 并发计划写入

把原先仅周草案激活持有的用户级事务锁抽为共享函数，所有 mealPlans 事务入口，以及 Agent 执行/撤销入口，均在读取或加行锁前取得同一把锁。周激活读取占用情况到写入餐次的区间因此不会被 Agent 新建或餐次移动穿插；用户间仍使用不同锁。SQLite 原事务已串行化写入，无额外改动。此锁保证并发检查一致性，不禁止用户通过其他正常入口显式安排同餐多道菜。

独立 PostgreSQL 空库全套集成通过。新增实际锁等待回归：持锁事务写入新的同餐安排时，同时启动周激活与 Agent 执行；查询 pg_locks 确认两者都在等待；提交后周激活读取新占用并返回 version_conflict，不插入重复餐次。服务端静态检查、模块边界检查通过。仍未把 #197–#199、数据/部署/设备验收或全目标标为完成。

## #197 跳过证据口径第一步

修正旧行为：任意一次 skip 扣 30 分，变为只有近 30 天至少三条不同幂等事件、reason=dislike 且 scope=long_term 才扣 8 分，不排除菜谱；明确收藏优先，不叠加该推测惩罚。无原因历史、no_time、too_much、本次 scope、撤销、未来或过期证据均不计入口味判断。SQLite 与 PostgreSQL 查询同一纯规则，兼容两种日期/JSON 格式。评分版本升级 rules-2026-09-12.1，避免复用旧规则游标，并在结果中解释降权原因。

验证：服务端全套 333 项通过，包含更新旧的一次跳过断言、SQLite 三条明确证据回归；PostgreSQL 独立空库全套通过，分别验证没时间/份量过大/不喜欢以及撤销和账号隔离。静态与架构检查通过，SQLite 边界检查更新无新增越层 SQL。

#197 仍未完成：当前只是事件证据和评分纠偏；没有新增用户原因选择界面、完整偏好列表/纠正/暂停学习控制，也未关联制作/食用/纠错的所有业务证据。下一步需要持久化学习控制和删除截止证据，配合可解释偏好入口；不得将 metadata 的 withdrawn 测试等同于已交付用户撤销入口。

## #197 偏好查看、纠正与学习控制

SQLite v65 / PostgreSQL 0007 新增用户学习设置，外键随账号删除。存储 enabled、按菜谱的明确 dislike/neutral 覆盖和版本。neutral 是最小屏蔽记录，持续阻止旧证据再生；此实现也抑制该菜后续推测，只有用户新的明确设置才能覆盖。设置更新使用事务和版本冲突检测，多个设备不会静默覆盖。

新增鉴权 GET/PATCH preferences 和“系统记住了什么”页面（餐单页入口），显示结论来源、时间和证据，不复制聊天或健康数据。用户可暂停/恢复、确认结论、纠正/删除。暂停时停用推测排序；反馈 SQL 落库时根据当时设置添加 learningPaused，恢复不纳入暂停期反馈。明确设置仍有效并优先于旧收藏；菜谱详情读取限制为当前公开可用记录，不能借任意 recipeId 读取不可用标题。

验证：服务端全套 334 项、客户端 164 项通过；SQLite API 覆盖旧事件重放、版本冲突、暂停和账号隔离，UI 覆盖删除成功后更新、账号切换丢弃旧响应。独立 PostgreSQL 空库全套通过，新增并发设置只有一个生效、暂停标记和恢复后 neutral 屏蔽。lint:all、contracts:check、schema:check、architecture:check、SQLite 边界和 Expo Web export 通过。最终权限收紧后定向 API 与静态检查再次通过。

#197 尚未全验收：需要前端反馈原因采集、内部明确对话/识别确认/换菜/制作与实际食用/剩余和丢弃等可信业务证据接入、撤销后衍生结果重算。现有用户控制和三次 dislike 规则不能冒充完整行为学习。#198/#199 与数据/发布等事项仍继续推进。

## #197 菜谱详情反馈入口

新增菜谱详情反馈组件：今天没时间(no_time)、份量太大(too_much)作为 session 事件；长期不喜欢直接写为明确口味，不要求用户重复表达三次。三个选项不会混为同一种厌恶，明确口味可转到偏好管理页纠正或删除。临时反馈重试复用事件幂等键；明确口味失败重试沿用原版本，遇到并发变化不能重新读取版本后悄悄覆盖。切换账号或菜谱时失效旧请求，读取设置后再次检查账号才允许写入。

客户端全套 168 项、静态检查、Web export 通过；新增四项覆盖两种临时原因、明确偏好的原版本重试、账号切换阻止旧写入。本轮只改客户端，未重复服务端或数据库测试。#197 剩余为其他可信业务证据（对话、识别、换菜、制作、食用、丢弃、纠错）的关联和可回放推导，不能把原因按钮视为全部完成。

## #197 制作/食用/丢弃/纠错证据关联

偏好接口现从完整近30天业务查询获取制作批次与实际食用/丢弃事件（近期纠正的更早事件也纳入），关联独立摄入纠正凭据。稳定 production/event 标识去重，随真实数据库结果重读；撤销食用或删除摄入使该食用事实 valid=false，缺失关联摄入也不能作为有效证据。账号过滤贯穿 JOIN，不复制营养值或原始对话。页面展示事实和纠错说明。

这里仍是观察事实，不是喜好推断：制作不等于食用、单次食用不等于喜欢、丢弃原因未知，实际制作耗时没有可靠采集时明确不提供。未因增加事实展示而擅自启用新的排序规则。

验证：服务端 336 项、客户端 168 项、PostgreSQL 独立空库集成、contracts 与架构/静态检查及 Web export 通过。新增纯规则覆盖去重和两种纠正；SQLite API 与 PostgreSQL 均验证真实纠正链使食用证据失效、丢弃仍是独立事实，账号隔离有效。#197 其余证据来源和逐步推导仍未完成。

## #197 识别确认与换菜证据

近30天事实增加 inventory_intake 生成的库存日志和已应用/已恢复餐次变更。库存区分 manual/automatic acceptance，并关联同一库存项的识别撤销日志和删除状态；自动入库不冒充用户明确表达。餐次建议只有真实应用后进入行为事实，reverted 标为失效。库存数量使用原单位展示，不误标为餐食份量。各来源有独立稳定标识，仍不把买过、拍过或换过直接推断为喜欢/厌恶。

验证：服务端 338 项、客户端 168 项、PostgreSQL 全套通过，含真实有效/已撤销识别入库及恢复餐次变更的正反断言；静态、契约、架构、SQLite 边界与 Web export 通过。此批同时提交前一批遗漏暂存的 SQLite 边界更新。#197 仍需明确对话来源、实际用时采集和完整推导；#198/#199 及其他未验收问题保持范围不变。

## #197 用户报告的实际制作用时

SQLite v66 / PostgreSQL 0008 增加 nullable reported_cooking_minutes（1–1440 整分钟）。完成界面可自愿填写，不从步骤倒计时、菜谱估时或请求耗时生成；共享契约明确此来源限制。字段随制作事务和原始幂等完成请求保存，重试不能把原用时改成新的值；偏好事实注明“用户报告”，留空及升级历史保持未知。切换菜谱/队列/账号清空未提交输入，待确认制作期间不可修改字段。

验证：服务端 340 项、客户端 168 项、PostgreSQL 全套、静态/契约/数据库模式/架构检查与 Web export 通过。API 覆盖非法值、正常保存、不同用时重试返回原值和未知；PG 检查实际列值；旧队列升级夹具补齐其声明已存在的 prepared_meals 表并验证原记录新增字段为 NULL，未改变迁移跳过规则。

此项是用户报告，不是自动计时精度验证；尚未提供制作后用时纠正入口，也未用于估时学习。#197 仍有对话证据和完整推导缺口，整个 issue 清单未完成。

## #197 制作用时纠正

待吃餐页可修改或清空 reported_cooking_minutes；提供已吃完/丢弃制作记录的展开入口，避免余量为零后无法纠正。复用版本化 reschedule 事件与账号内持久化幂等重试，调整不改计划日期、份量和摄入。禁止把用时修改夹在 eat/discard 内。每次事件保留结果快照，可核对纠正历史，后续事实读取使用当前值。

验证：服务端 340 项、客户端 169 项、PostgreSQL 全套通过；并发同键只产生一次更新、清空、旧版本冲突、错误操作类型拒绝、零余量历史界面清空均覆盖。保留原有待吃餐旧响应/旧快照回归，新增界面提交测试实际经过共享 schema。该页改为直接导入 schema，解决 Jest 无实验 VM 模块时无法执行动态导入的问题。静态、契约、架构、SQLite 边界检查与 Web export 通过。

重新读取 GitHub open issue：仍34项，与原范围一致，无新增或关闭。对话证据、完整学习及 #198/#199、其他数据和发布验收仍继续，不关闭总目标。

## #192/#197 对话长期意图否定与问句

审计发现原 hasPermanentPreferenceIntent 只要出现“长期/默认/记住”就可能通过，错误包含“不是长期，只是今天”和“要不要记住”。现按语句处理，否定保存、引用示例、疑问句与明确仅本次范围不构成长久写入意图；独立的肯定长期语句仍有效，例如“今天两个人。以后默认一个人”。保留原高风险操作确认和 payload persistent 限制。

定向测试覆盖否定、引用、举例、问句及混合临时/长期语句，服务端全套341项和静态检查通过。此规则仅是长期动作入口的文本判断，不等同于完整语言理解或对话来源模型；尚需继续关联原操作证据及学习推导。无客户端、数据库变动，不重复相关构建。

## #197 对话长期设置来源

偏好事实关联 assistant 来源的已执行/已撤销 update_kitchen_preferences 操作，按操作 before/result 的差异识别实际改动，并与当前档案逐项比对。请求级条件、未执行提案和无实际差异操作不进入长期来源；后续档案纠正使旧依据失效。仅返回稳定操作标识、时间、变更类别及有效性，不复制原始聊天或健康字段值。

服务端342项、静态/契约/架构与SQLite边界检查通过。SQLite API验证对话设置执行后有效、手动改档案后失效；PostgreSQL独立空库全套补充同样正反场景并通过（夹具显式创建所需档案行）。未改变客户端渲染逻辑或数据库模式，未重复导出。当前已关联多类事实，但完整学习规则与长期/本次口味条件仍需继续审计，#197和其他未验收事项未关闭。

## #192/#197 本次不吃辣与长期口味隔离

新增 avoid_spicy 明确条件，长期档案、Agent本次提取、方案本次选择共用契约；本次覆盖不写回档案，恢复草案显示原有效条件，切换账号/上下文清除临时选择。候选过滤已知辣原料、明确辣标签与原料缺失菜谱，其他候选仍提示核对辣度。待吃批次无可靠辣度属性时不自动用于要求不吃辣的本次方案，明确说明原因；不改变过敏等独立限制。评分版本升为 rules-2026-09-12.2。

服务端344项、客户端169项、静态/契约/架构检查和Web export通过；扩展定向19项验证临时true/false与长期值互不覆写、AI上下文区分stored/effective、已知辣原料/标签/未知原料过滤、七日待吃分配不猜辣度。无数据库写入规则或模式变化，未重复PG全套。#197完整学习规则和其他issue仍待完成。

## #198 worker 每次运行租约与过期结果保护

每日维护接入前审计发现，同一 workerId 原可重复获取尚未到期的租约；超时仅 Promise.race 返回失败，没有向任务传递取消。现每次运行用独立 runId 持租约，SQLite/PG 均禁止未到期重入；任务上下文提供 signal、assertActive 与 leaseOwnerId，超时、失租约或运行结束后上下文失效。记录运行结果的 UPDATE 内同时校验租约所有者、未到期与 running 状态，避免先检查后被接管仍记为完成；旧任务释放只匹配自己的租约。

服务端349项、静态检查、架构、SQLite边界与PG schema检查通过；独立空库PG全套通过。新增同进程并发、过期/被接管、超时后迟到返回、上下文复用和结果落库竞态回归；两种真实数据库分别验证过期结果拒绝与旧所有者不能释放新租约。没有客户端或数据库模式变更，无需重做Web导出。

这仍是 #198 前置基础：取消属于协作机制，不能强制阻止不使用上下文的业务副作用；局部重规划最终必须在业务写事务内一起校验租约和输入版本。用户每日时间/时区、持久化变化事件、合并/恢复/有限重试、局部计算及通知尚未接入；#198和总目标保持未完成。

## #198 每日检查时间与持久化设置

新增 planMaintenance 模块、共享设置契约和鉴权 GET/PATCH /api/v1/plan-maintenance/settings。未设置用户默认关闭；开启必须由用户提供有效时区与 HH:mm 时间，不替用户选默认检查点。设置保存下一次 UTC 时间和本地日期，关闭清除待运行时间、保留原选择；按用户隔离并以 version 拒绝旧页面/并发覆盖。SQLite67、PG0009新增设置表及到期索引，保留不可由请求修改的上次完成日期。

时间规则独立于服务器时区：今日未完成且检查点已过时返回今日到期时间，重启可以补查；停机多日合并为当前日期检查；重复夏令时取首个时刻、跳过时刻取当天其后首个有效分钟、完全跳过日期无任务。改时区后不重复已完成本地日期。定向测试覆盖上海跨日、纽约夏令时、尼泊尔45分钟偏移、Lord Howe半小时夏令时、Apia跳日、无效设置和重启。

验证：服务端356项、PG独立空库全套、契约/服务端静态/架构/SQLite边界/schema检查通过；两种数据库覆盖持久化读取、并发设置仅一方成功及关闭清除下次时间。API还覆盖鉴权、账户隔离和输入缺失拒绝。PG模式为98表152外键。

剩余 #198：设置界面、worker真实调度、事务业务事件、任务合并/恢复/有限重试、局部结果的租约与输入版本原子校验以及变更通知。当前保存设置不代表worker已执行检查；不对外宣称每日维护功能已经完成。

## #198 业务事务内持久化维护事件

SQLite68/PG0010新增 plan_maintenance_events，按用户/事件类型/原始操作去重，保存待处理状态与索引。公共入库插入路径覆盖手动、购物清单、识别批量和Agent入库；制作（含旧完成入口）、食用、丢弃、改期/保留和摄入纠错在原业务事务内追加事件。事件仅保存主体、关联库存/餐次ID与版本，消费方必须重新读取当前事实；制作同时食用合并于制作事件，不重复通知。事件独立于worker进程存活，不在提交后靠内存回调投递。

服务端356项、PG独立空库全套、服务端静态/架构/SQLite边界/schema检查通过。SQLite回归覆盖重复请求与并发冲突只留下有效事件、跨账号无事件、事件写入故障回滚制作及库存；Agent组合操作在入库后失败也回滚事件。PG验证并发食用去重，以及外层事务中事件仅本连接可见、回滚后制作和事件均消失。更新旧Agent内存夹具补足新表，没有绕过生产事件写入。

剩余：此提交只持久化事件，未将processed_at提前置为完成。需要继续实现到期/事件任务合并、租约与版本保护的局部计算、有限重试恢复、设置界面及通知；库存后续修改/撤销的补充变化路径还应随影响追踪审计。#198和其他未验收issue保持未完成。

## #198 事件任务合并、领取与有限恢复

SQLite69/PG0011新增维护任务与事件成员表，事件只能归属一个任务。入队读取已提交且超过30秒等待窗口的未分配事件，同用户尚未开始的任务合并；已尝试任务成员冻结，后续事件另建任务。领取按用户排他，每次生成新租约令牌；失败按30/60秒退避，最多三次；崩溃后的过期任务可接管，第三次过期转失败。迟到失败请求必须同时匹配当前用户、任务、次数、令牌和有效期限。尚无成功应用入口，事件不会因入队/失败而标为已处理。

worker注册 plan-maintenance-dispatch，复用运行租约和历史，实际执行事件入队；日志phase明确为event_dispatch，不能把该阶段完成解释成计划已更新。保留现有worker轮询配置，低延迟事件处理和用户每日到期检查仍待后续接入。

验证：服务端358项、服务端静态/架构/SQLite边界/schema及PG独立空库全套通过。共享行为断言在两种真实数据库验证分批合并、重复入队无效、同账号不并发、不同账号可领取、重新实例化后恢复、旧令牌拒绝、退避与第三次失败/过期终止、事件始终未提前处理。SQLite额外执行真实runWorkerCycle验证重复dispatch只首次入队，并检查队列仍queued、事件未完成。当前模式101表156外键。

下一步仍需局部规划计算与#195决策的原子应用、输入版本/租约校验、每日到期任务、变更通知和用户设置界面。任务队列基础通过不等同#198整体完成；所有未验收issue仍在总范围内。

## #198 经#195决策的批量原子应用入口

将原mealPlans更新逻辑提取为可加入调用方事务的同步SQLite入口和PG PoolClient入口，原手动接口继续复用。维护队列增加applyChanges：同时校验当前任务用户/次数/令牌/数据库实际到期时间，以及目标活动计划版本、餐次版本和计划日期范围；逐项经原#195规则记录applied/pending/blocked。整批成功后才在同一事务内确认所属事件并完成任务；任一版本冲突或最终租约失效，撤销整批变更与日志。PG在同一事务中持有计划advisory lock、任务行锁和目标计划/餐次行锁。

验证：服务端358项、PG独立空库全套、服务端静态/架构/SQLite边界通过。共享双库回归在同一批次验证未确认直接应用、已确认与已采购先建议、制作中保持不动、未受影响餐次不变；第二项冲突撤销第一项及变更日志，旧任务令牌和错误计划版本/越界日期拒绝，成功仅确认本任务事件，重复旧令牌不产生新记录。无模式/客户端变化。

这是应用端入口，尚未自动从事件推导changes；worker仍只dispatch。计算器还必须校验库存、待吃餐、偏好等完整输入版本/指纹，空变更只有在确认无可行动结果后才能交给此入口。每日调度、影响范围计算、通知及设置界面仍待实现；不据此宣称#198或全部issue完成。

## #198 关联餐次影响范围读取

新增maintenanceScope，复用库存分配器的食材名称匹配规则，根据入库/制作消耗的库存定位依赖原料的未来餐次；待吃餐事件通过保存的allocations和executionItems定位未来目标及关联制作项。排除其他账号、已删除/取消计划、过去和已完成/跳过餐次；已确认餐次仍保留在范围内，交由#195产生建议。缺失库存、缺失待吃餐、未知事件、损坏原料以及新增未分配的待吃餐返回核对事项，不扩成自动整周重排，也不冒充无影响。

两种队列仓库增加scope读取入口：校验用户/租约/次数，事件成员从数据库关联读取，不信任调用参数eventIds；SQLite事务和PG只读repeatable-read取得一致范围输入。尚未调用生成器或自动应用。

验证：服务端364项、服务端静态/架构/SQLite边界及PG独立空库全套通过。六项定向测试覆盖同名分配规则、账户与日期/状态边界、确认餐次保留、待吃目标精确关联、未知事实显式检查、去重；共享双库断言验证实际任务成员、伪造成员参数不扩大读取、跨账号与旧令牌拒绝。没有模式或客户端变更。

下一步：将范围内餐次交给局部数量/菜谱重算，并为计算使用的完整输入增加原子版本/指纹保护，再连接worker处理与每日任务。通知和用户设置界面仍未完成；#198及总目标未完成。

## #198 计算输入快照与提交时校验

队列新增inputs读取入口，返回账户库存/数量依据、待吃餐、计划/餐次、采购、制作队列、档案、厨具、学习设置、收藏、跳过证据和摄入记录的一致快照与稳定指纹。自动纳入当前餐次所用菜谱，计算器可以额外声明候选菜谱ID；新增替换菜谱必须在已捕获的输入中。applyChanges现在强制要求该快照指纹，不能只凭目标餐次版本提交。

SQLite在同一写事务内重读并验证。PG应用事务先持有计划/任务锁，再锁用户父记录（阻止经外键新增输入）及现有输入行、所用菜谱行；只在短暂验证和应用阶段持锁，计算阶段为只读repeatable-read。核对过相关表的user_id外键均指向users。新增候选菜谱未自动加入已选候选集，后续计算器应明确其候选集合。

验证：服务端364项、服务端静态/架构/SQLite边界和PG独立空库全套通过。双库共享测试验证重复读取指纹稳定、计算后新增库存导致旧结果被拒绝且无日志/确认副作用，重取快照后可正常应用。PG额外在输入锁持有期间，用另一真实连接验证新增和修改库存均被锁阻止，提交结束后写入可成功。未改变数据库模式或客户端。

仍需将局部计算器只读快照作为唯一计算来源（不能另外读取未纳入快照的数据），接入worker实际处理、每日到期任务、通知和设置界面。输入保护基础不代表完整自动重规划或全部issue已完成。

## #198 局部数量重算与统一快照评估

新增recalculateMaintenanceQuantities：先保留未受影响与受保护餐次的固定占用，再让受影响可调整餐次按日期共用一次性库存账本，返回covered/missing/unknown及每项原料的需求/覆盖/缺口。保留期限与数量证据规则，估算值不当作确定库存；未知用量会阻止后续重复承诺库存。固定占用的预览在账本中保留一次，避免确认/制作中餐次被重复扣算。已完成/跳过餐次不再占用未来原料；变化的待吃目标仍明确核对份量、期限和复热条件。

输入快照现在同时捕获实际任务事件，evaluateMaintenanceJob从同一份快照完成影响范围与数量评估，避免先读取范围、再另读库存造成时间错位。保持纯计算，不写库存、不记录食用，不调用模型。

验证：服务端369项、服务端静态/架构/SQLite边界及PG独立空库全套通过。五项定向测试覆盖固定优先、同库存不复用、过期/估算值、未知用量传播、保护餐次不双扣、完成后释放；共享双库断言验证评估与输入快照指纹一致、过期旧任务不评估。此次更改无数据库模式和客户端改动。

仍需替换菜谱约束与份量同步（不能仅按库存覆盖就换菜）、诊断结果持久化和通知、worker实际处理、每日到期与设置界面。当前为局部数量诊断，不是完整局部重规划，#198及全部issue保持未完成。

## #195/#198 换菜保留计划份量

发现原applyItemChange直接复制新菜谱原料，会把份量不同的周计划执行项改成菜谱默认量。现以executionItems的servings和新菜谱serving_size缩放原料，同步执行分配recipeId/title/recipeYield/demands，保留targetMealId与计划份数；恢复原菜谱走同一路径。缺产量、非结构化用量或缩放后无法以六位小数表示的数量拒绝覆盖，服务层给出MEAL_PLAN_QUANTITY_UNKNOWN。没有份量分配的旧餐次保留原有行为。

换菜后标记executionNeedsRevalidation，因为当前只校准执行原料，不能把旧草案的整体时间/采购预算冒充重新核算完成；后续局部生成器仍需处理这些检查。执行元数据随餐次写事务提交，仍由餐次版本保护，不额外改变计划元数据版本规则。

验证：服务端372项、服务端静态/架构/SQLite边界以及PG独立空库全套通过。纯计算覆盖未知产量/用量与不修改输入；双库实际updateItem/reviewChange验证1.5份计划替换为4份200g菜谱后为75g，恢复1份100g原菜谱后为150g，计划份数/目标关联与执行需求一致。无模式/客户端变更。

这解决自动替换的份量前置缺口，尚未完成替换候选的完整约束选择、结果通知、实际worker处理、每日任务与设置界面。总目标仍未完成。

## #198 快照内厨具需求与替代规则

维护输入新增厨具目录、能力、目录能力映射、替代关系和菜谱厨具需求五类共享规则。新增只读快照仓库，复用现有KitchenwareService进行目录别名解析和需求评估，不额外查询实时数据、不开放写操作。输入缺规则表时拒绝评估。evaluateMaintenanceJob返回已捕获菜谱的compatibility供后续候选筛选使用。

规则变化纳入指纹。PG在最终校验/应用短事务内对五类低频变更规则表持SHARE锁，阻止新增规则逃过单行校验；只读计算阶段不持这些锁。目录顺序沿用各数据库的ORDER BY category,name，需求按角色/id排序。

验证：服务端375项、服务端静态/架构/SQLite边界及PG独立空库全套通过。定向测试覆盖别名、账户隔离、维修状态、必需能力、禁止/等价替代、缺规则表拒绝、规则变更指纹变化；PG并发测试追加真实规则表写入在提交期间被阻止。无数据库模式或客户端变化。

下一步仍为候选约束选择与实际worker处理，条件替代的使用条件、整体时间与采购预算需在选择时核对，不能仅凭库存充足或单个能力匹配就自动换菜。通知、每日任务和设置界面仍待完成；总目标未完成。

## #198 受影响餐次的替换候选选择

维护评估捕获全部已批准且不需复核的候选菜谱，复用现有档案/过敏/偏好排序与快照内厨具评估。仅针对受影响且原料不足或不再满足约束的餐次尝试替换；每次候选都重新核算完整受影响集合，保留固定占用、已覆盖餐次和先前候选的原料承诺。确认餐次只生成建议，并额外保留原安排占用；制作中的餐次不替换。

候选按计划份数缩放，需要明确原料、产量和准备/烹饪时间，并核对同餐次菜谱累计时间。条件厨具替代、明确价格/携带/辣度要求无法核实时保留原安排并输出检查事项；收尾时间仍标记待核对。结果目前为只读候选，尚未连接worker提交。

验证：服务端380项、服务端静态检查、架构/SQLite边界与PG独立空库集成全套通过。五项定向测试覆盖共享库存不重复承诺、过敏与厨具限制、制作中固定占用、份数缩放、未知时间与预算、原安排已覆盖不改动；双库队列评估使用包含候选的同一输入指纹。无模式或客户端变更。

仍需实际worker处理、诊断持久化/通知、每日任务与设置界面，以及整体执行预算复核。#198和全部issue目标仍未完成。

## #198 后台处理与诊断原子落库

新增plan-maintenance-process任务，默认在dispatch之后运行；每批领取有限任务，使用只读统一快照完成范围、数量和替换评估，随后通过已有输入指纹/餐次版本/租约事务校验应用。输入冲突进入最多三次的重算路径，失效租约不写入新任务，计算期间取消不会继续提交。提交已开始后的业务事务仍依赖独立任务租约进行原子保护。

结果与餐次变更、事件确认同事务保存到result_json，含规则版本、输入指纹、当地日期、变更前数量诊断、检查事项和零模型调用/费用。用户维护时区纳入输入快照；未设置时沿用Asia/Shanghai。运维文档补充独立30秒维护worker配置，默认综合worker仍按原一小时轮询。

验证：服务端384项全套通过；新增4项处理器测试涵盖当地日期、输入冲突重试、失效租约、取消和计算失败；真实SQLite worker验证dispatch→process→持久诊断→重复不处理。补充双库诊断原子性断言后105项相关测试、PG独立空库全套与服务端静态检查通过；架构检查通过。无数据库模式或客户端变化。

每日任务、用户可见通知/失败反馈和设置界面仍待接入，库存修改/撤销事件需要补齐；整体执行时间/采购预算仍需进一步复核。#198及全部issue目标仍未完成。

## #198 每日检查持久调度

两种队列仓库新增enqueueDaily，worker dispatch先扫描已启用且到期的设置，再生成daily_check事件并推进下一次时间。事务内完成事件插入和调度游标更新；以账户/事件类型/当地日期唯一键防重复，PG使用行锁SKIP LOCKED支持并发扫描。停机漏过旧日期只检查当天，不重放过期计划。原有夏令时重复/跳过时刻规则继续沿用。

每日事件检查全部当前及未来的有效计划餐次和待吃分配，仍由既有变更保护策略决定直接改动/建议/保留。关闭开关后，已排队每日事件记录跳过；业务事件照常处理。last_completed_local_date仅随成功应用事务推进，入队不冒充完成；设置版本和输入指纹防止计算期间时区/开关变化被忽略。

验证：服务端385项、静态/架构/SQLite边界及PG独立空库集成全套通过。共享双库测试覆盖并发只生成一次、停机日期合并、重新读取仓库不重复、入队不更新完成日、成功提交推进完成日、时间回退不重复和关闭不派发。范围测试验证每日全范围、过去/已完成/其他账户排除、关闭跳过。无数据库模式或客户端变化。

设置界面、用户可见结果与通知、失败反馈和库存修改/撤销事件仍待完成，整体执行预算复核仍有待核对项。#198及全部issue目标保持未完成。

## #198 每日检查设置界面

餐次计划页新增每日计划检查入口，路由仅导出screens实现并使用Screen容器。用户可启停每日检查、明确填写24小时制时间和IANA时区，或主动采用设备时区。首次不预选时间，提交前复用共享输入校验；展示已保存状态、服务器保存的下次实际当地执行时间和最近完成日，避免夏令时跳时后仍显示原始钟点。

响应新增共享运行时校验；账户切换立即隔离旧设置，读取/保存结果用账户和请求序号保护。保存冲突或网络结果不确定时要求重新读取核对，不能用旧版本继续提交；关闭只发送enabled=false和version，保留服务端时间设置。页面说明关闭每日检查不关闭业务事件检查。

验证：客户端50套172项测试通过，新增3项覆盖启用前显式时间/时区校验、关闭请求及冲突后刷新、旧账户保存延迟响应隔离。全仓静态、合约、架构检查和Expo Web导出通过；导出不分配打包编号。未改动数据库或服务端业务逻辑，未完成原生设备视觉验收。

用户可见的维护结果/通知和失败反馈、库存修改/撤销事件以及整体执行预算复核仍待完成。#198及全部issue目标仍未完成。

## #198 用户可见检查结果与失败状态

新增鉴权GET /api/v1/plan-maintenance/runs，两种数据库按当前账户返回最近20次任务摘要。展示排队/运行/完成/失败、尝试次数和重试时间，以及提交时的已调整/待接受建议/受保护保留计数和待核对事项。API只读取白名单字段，不返回内部错误、租约令牌或完整输入快照。

每日检查设置页新增结果入口。结果页按账户重新挂载并拒绝过期读取响应；支持刷新、空态/错误态和跳转餐次计划。明确计数为检查当时的结果，接受建议后的当前状态以餐次计划为准；失败不会显示为已调整。

验证：服务端385项、客户端51套174项、PG独立空库集成、全仓静态/合约/架构/SQLite边界检查通过。双库共享断言验证变更类别计数、诊断内容和账户隔离；HTTP验证登录要求；客户端新增失败展示和切换账户的延迟响应测试。调整账户挂载后定向测试与Expo Web导出再次通过，无打包编号分配。

目前提供主动打开查看的结果页，尚未完成消息中心/推送通知与失败后的用户重试操作。部分诊断仍需替换为更直观的餐次描述；库存修改/撤销事件和整体执行预算复核也未完成。#198及全部issue目标继续保持未完成。

## #198 库存修改、删除与识别撤销事件

两种库存仓库在修改、软删除和识别批次撤销的同一事务中写inventory_changed事件。修改包含新版本与原食材名称，范围发现同时匹配旧名称和当前名称，避免改名后丢失原有餐次依赖；软删除仍能从保留行查找原料关联。版本冲突不产生事件，重复撤销由既有幂等保护跳过，整批撤销失败同时回滚所有事件。Agent调用共用修改事务入口，沿用此触发。

验证：服务端386项、服务端静态/架构/SQLite边界及PG独立空库全套通过。新增范围测试覆盖改名两侧依赖与删除后关联；SQLite和PG断言验证撤销事件数量、重复不新增、批次冲突无部分事件，PG另验证修改/删除事件和旧食材名称。无数据库模式/客户端变化。

独立库存扣减路径仍需补审（制作/生产已有对应业务事件），结果描述、用户重试、通知和整体执行预算复核仍未完成。#198及全部issue目标保持未完成。

## #198 库存扣减事件闭环

SQLite实际扣减函数与PG共用扣减事务函数现在按成功扣减项写inventory_changed事件，携带库存ID、新版本和consume原因。覆盖手动扣减、Agent共用路径以及制作扣减；制作另有生产/完成事件，队列按账户合并后范围去重，不重复承诺库存。事件键沿用业务请求幂等键和条目身份，重放请求不新增事件。

验证：服务端386项全套通过，新增断言后的101项应用测试、PG独立空库全套、服务端静态/架构检查通过。SQLite验证两项扣减各发一条、重放不重复，以及第二项失败回滚第一项库存和事件；PG验证重复请求只保留一条、后续条目版本冲突使前项及事件全部回滚。无模式、客户端或发布编号变化。

通知、失败后用户主动重试、面向用户的餐次诊断描述以及整体执行预算复核仍待完成。#198及全部issue目标未完成。

## #198 消息中心提醒

维护worker在处理后扫描完成/最终失败任务，将有调整、建议、待核对事项或最终失败的结果写入现有消息中心。无变化、无核对事项的完成记录保持安静。提醒和任务result_json中的notificationRecorded标记同事务提交；PG锁任务行并SKIP LOCKED，SQLite写事务序列化；已处理的静默结果同样标记，避免阻塞后续批次。进程重启后继续扫描未处理的终态任务，不依赖内存回调。

消息中心识别plan_maintenance类型，点击打开检查结果；待处理提醒可单独标记已处理，明确不接受或改动餐次安排。消息只包含摘要计数，不包含内部异常、输入快照或饮食详情。

验证：服务端386项、客户端174项、PG独立空库全套、全仓静态及架构/SQLite边界通过。共享双库断言验证并发只生成一次、失败任务提醒、静默完成不发消息、重复扫描不新增。客户端静态与Web导出验证新入口和提醒操作，无发布编号分配。

目前为站内消息，设备推送投递尚未接入；用户主动重试、餐次诊断描述和整体执行预算复核仍待完成。#198及全部issue目标未完成。

## #198 验收对照：小调整静默与重复事项去重

重新对照issue原文，修正上一阶段“任何调整都提醒”的偏差：只包含已自动应用变更的成功结果现在只记日志，不生成消息。待接受/受保护决策与核对事项按稳定内容生成去重键；不同任务的同一组事项不重复提醒，新的建议ID或检查内容会生成新提醒。PG对提醒发布使用短事务专用advisory锁，防止不同任务行并发为同账户同一内容重复创建；SQLite沿用写事务。最终失败仍按任务只提醒一次。

每次成功计算的diagnostics新增evaluationDurationMs（计算与提交前校验耗时，不冒充整个数据库提交耗时）；任务整体运行耗时仍在worker运行记录。

验证：服务端389项、服务端静态/架构/SQLite边界与PG独立空库全套通过。三项定向测试覆盖小调整静默、事项顺序/重复不改变去重键、新建议触发新键、失败不泄漏内部错误；双库共享回归将同一完成结果复制为不同任务，验证仍只一条消息。无模式或客户端改动。

用户主动重试并非#198最小验收的显式要求，后续优先处理实际验收缺口：整体执行预算、直观的影响范围说明以及设备/并发路径验收。已有有限自动重试和失败状态不等于全部目标完成；#198及全部issue仍未完成。

## #196/#198 维护换菜的分批时间核算

发现维护候选选择只为每道菜累加一次准备/烹饪时间，没有按计划份量和菜谱产量增加批次；已提取maintenanceSessionTime，按ceil(计划份量/菜谱产量)累加整餐顺序制作时间，包含同餐其他有效安排，排除已完成/跳过和其他日期餐次。已知小计超过用户预算时不选该候选。未知产量、准备时间或非法时间值不能当作确定的零耗时。

结果说明给出餐次日期、分批已知小计和用户预算；仍明确收尾与设备容量未核实，不声称已证明总耗时可行。既有executionNeedsRevalidation继续保留。

验证：服务端392项、服务端静态/架构与PG独立空库全套通过。新增3项测试验证3份一份产量需3批（18分钟不能进入12分钟预算）、整餐45分钟超过40分钟预算、完成/其他日期排除、未知产量/准备时间和非有限时间拒绝。无数据库模式或客户端改动。

仍需完整执行预算与真实设备条件的数据闭环，以及面向用户的影响范围说明和验收；本次仅修复已知分批漏算。#198及全部issue目标保持未完成。

## #198 直观的餐次影响说明

维护处理在提交诊断前，用同一输入快照中的所属计划/餐次将内部编号转换为日期、中文餐别和菜名；待吃目标也使用保存的餐次标识。只解析当前账户数据，无法关联的编号退化为“相关餐次”，不拿其他账户的标题补齐。说明随检查结果保存，后续改名不改变过去记录的含义。

对未解决的可调整餐次和待接受建议，补充已知原料缺口（如鸡蛋缺2个）；不把不确定数量写成确定缺口。已直接替换解决的旧缺口不再列为待核对项，受保护制作安排也不新增未经验证的缺料结论。

验证：服务端395项、服务端静态/架构及PG独立空库全套通过。三项定向测试覆盖名称/待吃餐别说明、实际缺口、直接替换与建议的区别、未知数量不编造、账户隔离。无模式或客户端变化；已有历史记录不追溯改写。

执行预算中的收尾/设备容量仍缺可靠依据，完整场景和设备验收仍待完成。#198及全部issue目标保持未完成。

## #198 无行动结果的说明与提醒隔离

检查到关闭每日检查后的排队事件原先将“已关闭，跳过”作为checks保存，进而被消息中心当成需要确认的事项。现将范围诊断中的info单独保存在notes，结果页作为说明显示，不参与提醒判断；只有真正checks仍展示为待核对。共享响应默认notes为空，兼容已有无notes记录。

没有受影响餐次或待吃目标时，数量评估直接返回范围检查与说明，不再遍历无关固定安排并把其旧数据缺口误报成这次事件的影响。新生产但未分配等真实范围检查仍保留。

验证：服务端397项、客户端175项、PG独立空库全套、全仓静态/合约/架构和Web导出通过。新增测试验证跳过事件不扫描无关的不完整计划、说明不产生提醒、结果页说明不标为待核对。无模式或发布编号变化。

完整执行条件、设备验收与其余issue仍未完成；总目标保持进行中。

## #198 双数据库完整维护链路验收

新增共享集成场景：在真实数据库中准备原料不足的餐次、可覆盖的候选菜谱和另一道未受影响的已确认餐次，写入库存变化事件后运行enqueueEvents→processMaintenanceJobs。没有替换计算或仓库的测试替身，实际完成快照读取、范围发现、候选筛选、份量/时间核算、输入校验和事务应用。

SQLite与PG均验证目标餐次实际换菜且版本增加一次，未受影响的已确认餐次整行不变，事件确认并只生成一条变更记录；再次派发/执行不重复处理。场景只在隔离测试数据库中清理测试队列。

验证：102项应用测试、PG独立空库全套以及服务端静态检查通过。此次为验收覆盖，不修改产品逻辑。远端gh issue list重新核对仍为34个开放issue，与初始清单一致，无新增或外部关闭；未对远端issue状态作更改。

这补充了局部自动维护的真实执行证据，不代替完整约束与设备验收，也不证明其余issue完成；总目标继续进行。

## #196 双数据库跨日滚动预览验收

新增真实数据库场景：原计划覆盖12日至18日，滚动预览请求13日至19日，六个重叠餐次保留（包含已确认、关联已采购、排队和制作状态），仅第19日生成新做方案。七份现有原料覆盖六个已有安排加一份新方案，过去12日不再占用本次需求；保留完整用量来源。

SQLite通过鉴权HTTP weekly-plan接口、PG通过真实RecommendationsService及仓库运行，未替换计算逻辑。重复预览的餐次与采购缺口一致，数据库原餐次整行及库存数量不变。

验证：103项应用测试、PG独立空库全套和服务端静态检查通过。初次测试用错采购列名后已按真实checked字段修正并重新通过两库验收。本次增加验收证据，不改变产品行为。

这证明显式跨日起始日期的滚动预览；不声称已实现无需用户确认的自动延长计划，规划窗口外已承诺资源的保护还需专项审查。完整约束/设备验收及其他issue仍未完成，总目标继续进行。

## #196 七日窗口外的既有资源承诺

修复planningState只读取本周安排的问题：两库现读取起始日之后的所有既有餐次和有效计划，用于保留库存与待吃分配；窗口外的安排仍不出现在本周槽位。数量账本新增采购范围约束，预留会扣除资源，但窗口外需求不会混入本周采购汇总或来源。过去日期不参与新周预留。

新增草案保存shoppingWindow（明确七日日期范围），换菜重算沿用该范围；共享校验要求范围为七天且包含方案餐次。旧草案没有该字段时沿用可用餐次日期范围回退，旧草案边界信息不足仍需重生成预览，不能声称已有完整历史窗口。

验证：服务端400项全套通过；补充范围校验及调整不同候选下的断言后112项相关测试通过。客户端175项、PG独立空库全套、静态/合约/架构/SQLite边界通过。真实两库场景增加第八天之后已确认承诺，验证新餐次不使用其预留库存，本周采购来源不包含该未来餐次；纯规则测试验证所需7份、可用2份、缺5份且不混入未来2份需求。不同数据库可选不同缺料候选，断言检查资源与范围不变量。

无数据库模式变更。完整执行条件、旧草案范围补全及设备验收等仍待完成；全部issue目标保持进行中。

## #196 旧草案采购窗口恢复

取消用旧草案“待补做餐次最早/最晚日期”直接充当采购窗口的回退，因为保留餐次可能被省略。现结合原weeklyShopping来源中的既有餐次日期，仅在能确定完整七天时恢复窗口，并将恢复结果写入换菜后的草案。查询允许向前寻找六天的保留来源，但实际资源账本只保留恢复窗口起点以后的安排，避免把前一周用量带进来。

无法唯一确定完整七日范围时返回WEEKLY_WINDOW_UNKNOWN，要求重新生成七日预览，原方案不写入。此处是拒绝不可靠的采购核算，不把缩小范围的结果伪装成完整周预算。

验证：服务端401项全套、PG独立空库全套、服务端静态/架构通过；新增与扩展后的10项周计划测试通过。旧草案只含13日至18日新做餐次时，从保留来源恢复12日至18日；实际换菜后仍计入保留需求（需8/有7/缺1），不计入前一周100份用量；缺来源明确拒绝。无模式或客户端变化。

全部issue仍未完成；继续推进完整约束和其他验收缺口。

## #199 共餐共享忌口的成员权限基础

新增成员自己的共餐偏好 GET/PUT 接口，保存明确填写的过敏原、饮食限制及共享开关，默认不共享，不自动读取个人健康档案。家庭列表仍只返回既有成员字段，管理员不能通过此接口指定其他用户。当前仅完成设置存储与权限边界，尚未将共享字段提供给多人规划或界面。

更新同时校验当前成员记录ID与版本，防止并发覆盖，以及退出后重新加入时旧请求重新开启共享；退出删除成员行，重新加入默认空偏好、不共享。新增SQLite迁移70及PG0012，生成模式及基线同步。

验证：服务端403项全套、PG独立空库全套、全项目静态检查、合约、架构和模式检查通过。两库共同验证默认关闭、成员只访问自己的设置、旧版本拒绝、退出权限失效、重加入不继承以及旧成员ID写入拒绝；HTTP额外验证非成员403和指定其他userId的请求400。历史队列迁移测试夹具补齐既有家庭成员表。

共餐参与者与份量规划、一次制作多人分配、各自实际摄入、客户端入口等仍待实现；#199及全部issue总目标保持进行中。

## #199 个人共餐设置客户端入口

个人页新增共餐忌口入口，读取家庭列表后按家庭分别编辑自己的过敏原、饮食限制和明确授权开关；分行输入去重并遵循共享合约数量/长度限制。未加入家庭时指向库存页家庭共享入口。页面说明当前设置尚不能替代多人配餐前的忌口核对。

请求与响应均使用严格共享合约；共餐设置GET禁用客户端缓存，撤回或成员状态变化后重新读取可获取服务器当前结果。账号与家庭切换卸载旧编辑器，迟到读取/写入不污染当前用户；保存不确定或冲突后清除可编辑状态，要求重新读取。

验证：客户端178项测试、静态检查、架构检查和Expo Web导出通过。新增实际组件测试覆盖明确授权与成员版本、分行去重、不确定写入后重读、切换家庭迟到响应、相同家庭ID下切换账号的迟到保存，以及退出登录清空。未完成原生设备视觉验收；Web导出不分配发布编号。

多人参与者份量、共同制作与各自摄入仍未完成，#199和全部issue目标继续进行。

## #199 共餐授权成员信息读取与展示

新增dining-members接口，以同一SQL语句同时检查请求者当前家庭成员身份并读取成员名单；数据库CASE仅提取授权成员的共餐JSON，不查询个人健康资料。严格区分shared:false（无过敏原/限制字段）和shared:true（显式填写的数组），防止未授权被解释为无忌口。返回成员身份ID与版本供后续规划校验。

设置页展示已授权成员信息，未授权和空白字段均提示本人确认；读取无客户端缓存，保存或重读设置后刷新名单。页面明确名单是读取时的状态，不替代配餐前重新确认；退出者无权读取且不会留在名单。

验证：服务端403项、PG独立空库全套、客户端178项全套通过；随后新增的迟到成员数据跨账号隔离测试与其他3项组件测试通过。全项目静态、合约、架构及Web导出通过。两库共同验证授权数据可读、撤回后字段消失、退出后拒绝读取和名单移除。无数据库迁移。

这完成授权信息的读取展示，不代表多人参与者份量、共同制作或摄入闭环完成；全部issue目标保持进行中。

## #199 共餐参与者份量与约束预览

新增dining-allocation-preview接口及设置页核算入口：明确选择当前成员及各自份量，检查成员身份ID、授权版本与共享状态，汇总所选成员全部已共享过敏原和饮食限制。成员未授权或状态过期时拒绝预览，不把缺少授权视为无忌口。取消参与者后重新核算不包含该成员约束；需求按百万分之一份整数累加，单次总量上限30份，与当前规划范围一致。

响应保留每人的份量，并明确recipeValidationRequired；客户端显示仍需核对菜谱、配料和制作条件。这里只读预览，不保存餐次、不扣库存、不记录摄入；因此不将总制作需求当作个人摄入，也不宣称共同菜已通过全部约束。客户端修改选择或份量清除旧预览，重读成员会重置选择。

验证：服务端404项、PG独立空库全套、客户端180项、全项目静态、合约、架构与Web导出通过。三人各一份保持个人一份、总量三份；临时不参与后总量减少；0.1+0.2+0.000001准确为0.300001。覆盖重复成员拒绝、撤回授权与版本变更拒绝，以及组件选择与请求、取消参与清除旧结果。

尚需将预览接入具体餐次和菜谱硬约束计算、按#195处理受保护餐次，并实现共同制作/家庭待吃/各自实际食用。#199与全部issue目标继续进行。

## #199 具体菜谱的共餐忌口检查

共餐需求预览新增可选recipeId，从菜谱详情入口带入；服务端只读取已审核、未删除且非needs_review菜谱。按每位所选成员的共享信息检查已知过敏原和明确禁食冲突，复用推荐过敏原别名；保留括号内的实际配料内容。已知冲突返回blocked及对应成员/限制，未匹配仍返回needs_review，不把简单文本检查当作全约束证明。

界面呈现选中菜谱名称、冲突成员与待核对项，仍不保存计划、制作、库存或摄入。此处只是具体菜谱核对入口，尚未自动给出配菜/替换、计算制作资源或应用到受保护餐次；任意自然语言限制和复合调料/实际制作条件仍需完善。

验证：服务端405项、客户端180项、PG独立空库全套、服务端/客户端静态、合约、架构与Web导出通过。两库真实菜谱读取触发已共享花生冲突，缺失菜谱拒绝。规则覆盖三人不同限制、括号内花生油和未知限制不认证；组件验证详情传入recipeId、展示冲突、不写入摄入相关接口。

无模式迁移，全部issue目标继续进行。

## #199 共餐原料总需求换算

具体菜谱检查现读取原始serving_size与配料，使用既有recipeDemands将完整可计量配料按共餐总份量缩放一次，保留每位成员份量。响应及界面标明这是总需求，尚未减去库存和已有预留，不直接生成采购或扣库。

菜谱份数缺失/无效、配料项不可计量、缩放后低于六位小数精度或超出支持数值范围时返回unknown和空需求，避免把部分用量显示成完整总量。已知忌口冲突仍保留blocked，即使原料量能换算也不代表可制作。

验证：服务端406项、客户端180项、PG独立空库全套、全项目静态、合约、架构与Web导出通过。三份对两份菜谱的米/水需求分别为300g/450ml；两库真实菜谱产出两份用10ml油，单份预览为5ml；缺份数、适量配料、无限份数和过小数量均不输出伪完整需求。

共餐原料归属与库存预留、具体餐次应用、一次制作及分人食用尚未完成；全部issue目标保持进行中。

## #199 家庭库存并发版本保护

梳理共享制作基础时发现家庭库存已有version列，但编辑既不校验也不递增，移除同样接受旧状态。现编辑与移除要求有效版本，事务内检查当前版本，编辑递增一次；旧版本返回409，缺版本返回400，不允许旧客户端无条件覆盖。SQLite事务、PG行锁保证同版本并发仅一次成功。

PG修改先锁家庭再锁成员资格，沿用退出/解散的锁顺序，将资格保留到事务结束；然后锁食材记录。客户端编辑、拖动位置和删除携带所见版本；拖动成功重新加载版本，错误显示服务端提示。此次未添加家庭份量扣减，也未假称已有共同制作账本。

验证：服务端407项、客户端180项、PG独立空库全套、全项目静态、架构、SQLite边界与Web导出通过。真实PG两成员并发同版本仅一个成功；SQLite HTTP覆盖旧版本编辑/移除409、缺版本400、数量不被覆盖，以及新版本移除成功。无模式迁移；此前测试类型错误修正后重新通过。

共享制作、份量预留与分人实际摄入仍是后续工作，全部issue目标继续进行。

## #199 家庭制作批次与原料扣减事务

新增household_meal_batches（SQLite71/PG0013）及POST家庭meals接口，记录用户明确报告的制作名称、总份量与家庭原料实际扣量。批次保留原料归属、扣前扣后数量快照、创建成员身份和请求；初始剩余等于制作总量，不自动记录任何人的摄入。创建人账号删除使用SET NULL，避免仅因制作人删除账号丢失家庭批次。

成员身份ID、每项库存版本和可计量单位在事务内核对；按库存ID排序锁定，原料扣减、活动日志和产出要么一起提交要么一起回滚。制作编号在家庭中唯一，重复相同请求返回同批次；改变内容或成员身份拒绝重用。沿用库存单位换算，额外拒绝超量及超出存储计量精度的扣减，避免舍入后不减库存却生成产出。

验证：服务端407项全套、PG独立空库全套、全项目静态、合约、架构、边界和模式检查通过；追加两并发相同制作请求后105项应用测试与PG全套再次通过。两库证明后续原料冲突回滚先前扣量、并发只生成一个逻辑批次、重试不重复扣、改量重用编号被拒绝、冒用成员身份被拒绝，个人摄入计数不变。

当前仅支持明确列出的可计量家庭原料，不支持混合个人库存来源；尚无客户端制作入口、批次列表、分人取用、预留或摄入更正。此为共享制作后端基础，#199及全部issue目标继续进行。

## #199 统一余量下的分人实际食用

新增家庭批次列表（最近100条）和批次eat接口，以及食用事件表（SQLite72/PG0014）。请求绑定当前成员身份、批次版本、明确实际份量和食用日期时间；摄入用户固定为登录用户，无代填他人ID字段。事务内扣统一余量、生成该用户摄入并记录幂等食用事件，不再触碰原料。

同版本并发仅一个取用成功；相同食用编号重试不重复摄入/扣余量，编号重用不同内容拒绝。列表只返回批次公共名称/份量/版本，不暴露其他成员食用事件、日期或摄入记录ID。退出后即使持有旧请求也不能读取或重试；个人营养未知时保持null，不用总制作量估算个人摄入。

验证：服务端407项、PG独立空库全套、全项目静态、合约、架构、边界和模式通过。三名真实用户各吃一份、各自产生一条1份记录，总剩余为零；其中两人同版本并发一成功一冲突，失败者刷新版本后成功；不足额请求拒绝、重试不重复、退出后禁止访问。补充逐行比较食用前后原料完全不变后，105项应用测试和PG全套再次通过。

尚待客户端批次/制作/食用入口、份量预留、关联餐次及食用更正。现有个人摄入删除不会自动归还家庭份量，家庭食用专用更正还未实现；本阶段不宣称完整共餐闭环已完成。全部issue目标保持进行中。

## #199 家庭待吃列表与本人食用客户端

共餐设置可进入所选家庭的待吃页，读取最近100个制作批次，明确填写本人实际份量、日期、可留空的时间及餐次。接口响应使用严格共享合约，家庭待吃GET不使用客户端缓存。成功后重读服务器当前余量，不采用幂等重试中可能较旧的批次快照。

发送前将完整请求持久化到账号/家庭隔离的私有键，未确认时禁用新的食用提交，仅重试相同编号。已确定的余量/版本冲突可重读重填；其他不确定结果保留待确认项，用户可先查看个人摄入后明确清除本地待确认项。清除不删除服务器摄入。损坏的待确认数据也必须先核对再清除。

账号、家庭和私有存储代次共同隔离组件；旧会话的读取、写入回包不能覆盖新会话或清理其待确认记录。本地持久化失败不发送食用请求。

验证：客户端185项全套、客户端静态、合约、架构及Web导出通过。新增5项实际组件测试覆盖持久化先于请求、同编号重试/重开恢复、历史返回不恢复旧余量、切换账号与同账号新会话隔离、存储失败不发送。新增会话测试的Jest动态导入限制修正为静态导入后全套通过。未完成原生设备视觉验收。

家庭制作入口、预留和食用更正仍待接入，全部issue目标继续进行。

## #199 家庭制作客户端入口

家庭待吃页可进入“记录已完成的家庭制作”，填写名称和总产出，逐项选择家庭原料、明确实际扣量。数量输入严格匹配完整值与单位，拒绝负数、范围、约数和适量；不默认用完整库存。界面说明仅记录已完成制作、当前不混用个人库存，成功后进入家庭待吃流程再分别记录食用。

请求先按账号/家庭/会话代次持久化，再调用制作接口；重开恢复原编号和每项原料版本，不因重试重新读取版本覆盖原请求。明确的库存拒绝会要求刷新重选；不确定结果只允许原提交重试，或用户核对批次与库存后清除本地待确认项。家庭库存GET也禁用客户端缓存。额外修复制作及食用冲突后本地清理失败时保留待确认项，避免未处理的异步异常。

验证：客户端190项全套、客户端静态、架构与Web导出通过。新增测试覆盖严格数量解析、300g原料/三份产出请求、提交前持久化、重开原请求重试、库存版本冲突清空旧选择、跨账号迟到响应隔离、拒绝后清理失败仍保留重试。正则lint要求修复后重新通过全套。未完成原生设备视觉验收。

目前有家庭制作与本人食用客户端通路，但预留、食用更正、具体餐次及计划保护规则关联、混合库存来源等仍未完成，全部issue目标继续进行。

## #199 家庭实际食用更正与不可重复归还

个人摄入列表新增家庭批次关联字段，原有记录处理入口对家庭记录同样要求选择“撤销误记食用”或“仅删除摄入”。新增家庭更正审计表（SQLite73/PG0015），按原用户/摄入ID保持处理幂等，删除摄入后仍保留处理方式；换方式重试拒绝。

撤销必须仍是原家庭成员身份，且批次版本等于该次食用后的版本；原子归还份量、递增批次版本、写审计并删除个人摄入。已有后续食用等变更时拒绝直接归还，避免覆盖他人取用。仅删除摄入保留食用事实与批次余量，退出成员仍可清理自己记录。两种操作都不返还制作原料，别人不能删除该用户摄入。

验证：服务端407项、客户端190项、PG独立空库全套、全项目静态、架构、边界及模式通过；补充并发同次撤销后105项应用测试和PG全套再次通过。测试覆盖明确模式要求、跨用户拒绝、并发/重复撤销只归还一次、改模式拒绝、原食用重试不复活已删除摄入、后续取用阻止旧撤销、退出后禁止改余量但允许仅删摄入、原料始终不变。

这补齐家庭食用更正，份量预留、计划餐次关联、完整约束及设备验收等仍待完成；全部issue目标继续进行。

## #199 本人预留与统一可取份量

新增按成员身份保存的家庭批次预留（SQLite74/PG0016），只能设置自己的数量，0取消。预留与食用共用批次版本/事务锁，总预留不超过余量；取用只能消耗未预留部分及本人的预留，优先消耗本人预留。食用事件记录实际消耗的预留量，撤销误记同时恢复这部分预留；原料不受影响。

成员退出通过外键级联释放预留，重新加入不继承；批次列表在统一快照中显示总预留、本人预留和本人可取量，不显示其他人的摄入记录。客户端提供预留数量输入（0取消），按所见版本提交；结果不确定时先刷新核对，不自动改版本重试。预留不足导致的食用拒绝也按明确拒绝处理。

验证：服务端407项、客户端191项、PG独立空库全套、全项目静态、合约、架构、边界、模式及Web导出通过。两库覆盖超额预留拒绝、本人可取量、部分食用优先扣预留、撤销恢复预留、同版本并发仅一次成功、取消释放、不能吃他人预留、退出释放/重加入不继承、原料不再扣。组件验证按显示版本设置/取消本人预留且不触发食用接口。

个人餐次扩为共餐的计划关联及#195保护规则、完整菜谱约束与设备验收仍未完成；全部issue目标保持进行中。

## #199 共餐预览关联个人餐次及修改保护

个人餐次详情新增共餐需求预览入口，传入计划/餐次ID及所见版本。后端只读取本人当前有效计划中的餐次，拒绝跨账号、已归档/非有效计划、旧版本及与餐次不符的菜谱；默认用餐次当前菜谱检查所有所选成员忌口与总原料需求。

餐次、采购和队列在数据库一致快照中读取，复用#195修改策略：普通未确认餐次显示允许调整，已确认/采购显示需要审阅建议，已排队/制作/完成显示必须保留。返回显式applied=false；预览不写计划、采购、库存或摄入。客户端随餐次上下文切换重建状态，并显示具体拒绝原因和中文餐别。

验证：服务端407项、客户端192项、PG独立空库全套、全项目静态、合约、架构、边界、模式及Web导出通过。两库新增共同回归覆盖本人餐次查找、菜谱推导、预览后原餐次/摄入不变、跨账号及旧版本拒绝、已采购/确认/排队保护、非有效计划拒绝；组件验证传递所选餐次版本与保护提示。检查中纠正了采购查询引用不存在字段的问题，随后两库全套通过。

本次仅补齐有上下文的预览，并未把参与成员/共餐份量保存到计划，也未改造个人制作完成路径；真正应用/审阅/撤销共餐变更、库存来源与执行衔接、完整约束及设备验收仍待完成。全部issue目标保持进行中。

## #199 家庭实际制作关联来源个人餐次

家庭制作接受可选的本人餐次ID/计划ID/版本，实际提交前重新核对有效计划、归属、版本及执行状态。只允许尚未入队的planned餐次；已确认餐次可记录实际完成，但不会改变其日期、菜谱或规划份量。SQLite75/PG0017新增批次到餐次的可空唯一外键，同一餐次只能产生一个家庭批次。扣家庭原料、保存全额待吃产出、关联来源、完成个人餐次和写维护事件在同一事务中完成。

旧版本、非本人、已入队/完成及库存不足均不留部分结果；同餐次不同制作编号并发只成功一次，同编号重试返回原批次，改来源重试拒绝。个人旧完成入口不能为已完成家庭餐次再生成摄入，重新入队入口也拒绝已完成/跳过餐次。私有制作路径继续在事务内检查已完成状态，防止改入口重复产出。删除来源计划保留家庭批次及余量。

个人餐次详情显示家庭批次关联和家庭待吃入口，制作表单持久化来源版本，切换入口后重试仍使用原提交关联。家庭批次变更清理当前账号的相关计划/摄入缓存；不清理其他账号缓存。制作成功不记录任何人的食用，实际三份仍全部待吃。

验证：服务端407项、客户端194项、PG独立空库全套、全项目静态、合约、架构、边界、模式和Web导出通过。两库测试覆盖不同编号并发、来源归属与版本、入队保护、不足回滚、不改无关餐次、不重复扣库、不自动摄入、个人完成/制作/入队不能再次执行、重复仅一个维护事件、来源删除后余量保留。组件验证重开后保持原餐次提交，缓存测试验证关联资源刷新且账号隔离。新增组件测试最初在同一次act里查找尚未渲染的输入框，拆分交互后全套通过。

此处关联的是已完成的实际制作，尚未持久化“计划哪些人共餐及各自份量”，也未自动扩展采购/制作建议；共餐变更审阅/撤销、完整共同忌口及执行差异、家庭资源参与自动维护与设备验收仍待完成。全部issue目标继续进行。

## #199 保存共餐参与者与份量，复用餐次变更审阅

SQLite76/PG0018为餐次及家庭批次新增共餐分配快照。个人餐次可保存家庭、参与成员身份/共享版本、各人份量及明确核对标记；不复制完整健康档案或忌口正文。保存通过现有餐次变更入口，未确认可应用、已确认/采购生成建议、进入执行保留；接受/拒绝/恢复继续使用版本与保护事实检查。制作关联保存当时的参与者份量，实际产出/实际食用仍独立计量。

每次保存、实际应用/审阅及家庭制作均在事务内核对当前家庭成员、共享授权版本和菜谱；已知共同忌口冲突拒绝，家庭角色不会跳过授权。文本未命中仍需要用户明确核对配料及实际条件，不能据此保证所有限制已满足。更换已有共餐的菜谱不能沿用旧核对，必须重新提交共餐安排。成员授权变化会使旧建议失效，须重新核对后提交。

客户端从有来源餐次的预览单独保存共餐，显示具体前后成员和份量；支持取消安排并沿用建议/保护规则。已保存的共餐餐次拒绝个人完成、个人制作接口、个人入队及原个人采购入口，避免仍按单人路径执行；家庭制作按原来源重新校验，保存后保留参与者快照但不自动记录摄入。

验证：服务端407项全套、追加105项应用回归、客户端196项、PG独立空库全套、全项目静态、合约、架构、边界、模式及Web导出通过。两库共同测试覆盖三人保存、恢复个人安排、已确认建议、授权变化后接受失败、已知过敏冲突拒绝、重提/接受、家庭制作快照、直接个人制作接口拒绝、完成后共餐变更保留；不增加个人摄入。组件测试覆盖明确核对后提交所见版本、建议提示、前后成员姓名与各自份量。

尚未自动修改采购清单或个人原料需求；当前共餐采购须按预览总量核对家庭采购。采购扩量/去重与库存归属、家庭资源参与周计划和自动维护、完整忌口与配菜差异、设备验收等仍待完成，全部issue目标保持进行中。

## #199 共餐原料总需求同步到家庭采购

已保存的共餐可在核对预览后按总份量同步家庭采购，入口明确说明这是原料总需求，不抵扣库存或其他采购项。请求必须匹配已保存参与者/份量、餐次版本及所见菜谱内容指纹；未保存的选择、旧菜谱预览不能直接写入。重复编号核对原需求，避免将另一份预览误当成已同步。

SQLite77/PG0019为家庭采购增加来源餐次、原料键和生成版本。重复同步不重复加行；无人修改且未购买的生成条目可按新预览更新数量，去掉已不需要的生成条目。已购买、入库、删除或手动修改的条目拒绝自动覆盖/重复加购，须先人工核对。家庭采购及入库状态纳入#195餐次保护事实和共餐预览判断，即使餐次未确认，已购买也只能提出建议。当前账号的家庭清单缓存在同步后失效。

验证：服务端408项、追加105项应用回归、客户端197项、PG独立空库全套、全项目静态、合约、架构、边界、模式及Web导出通过。两库验证三人按菜谱yield生成15ml总需求、重复不增行、新预览把原行更新为30ml再恢复15ml、生成版本递增、购买后禁止重写且触发建议、旧菜谱指纹和改请求重试拒绝；摄入与实际制作仍独立。迁移回归补齐旧测试基线遗漏的既有家庭采购表，静态检查补全PostgreSQL执行回放返回类型。

这不是库存抵扣后的净采购量；家庭库存预留/净缺口、多个餐次之间的需求去重、已采购后追加或减少的差额建议、家庭资源自动维护及设备验收等仍待完成。全部issue目标继续进行。

## #199 家庭跨餐次库存核算预览

带来源的共餐预览新增同名家庭库存核算。两库在一致只读快照中读取该家庭所有有效、尚未完成/跳过的共餐安排及家庭库存，优先处理已确认、采购或进入执行的安排，再按日期与稳定ID顺序分配；每份库存只计入一次，按各餐日期检查保质期并优先使用临期批次。支持g/kg、ml/l换算，不混用不同计量类别，也不把相似名称当成同一种原料。

返回仅包含当前餐次的覆盖量、缺口与其他餐次数量，不公开其他成员完整个人计划、摄入或健康资料。未知份量/配料、无效日期、无法换算的同名库存会标记待核对，缺口使用null而不是确定补买数字。来源版本或菜谱指纹变化要求刷新；核算不扣库、不建立预留、不改写采购清单。

验证：服务端411项、客户端197项、PG独立空库全套、全项目静态、合约、架构、边界、模式和Web导出通过。两库共同回归覆盖另一位成员的餐次竞争库存、按日期和保护优先级分配、未来餐次无法使用过期批次、未知竞争需求不输出确定缺口、核算后库存不变。纯逻辑补充g/kg换算、每批仅分配一次、无效保质日期及未知量测试；界面展示覆盖/缺口并明确不是最终补买量。

当前核算尚未抵扣采购清单和家庭待吃餐，也未建立可持久化的库存预留。采购按钮仍按明确标示的原料总需求同步，不能将本次缺口直接视为最终净采购。实际净缺口同步、并发预留/释放、跨餐次采购去重及设备验收等仍待完成，全部issue目标保持进行中。

## #199 区分库存覆盖与清单已安排量

家庭核算现在在同一只读快照内读取尚未入库、未删除的采购清单，分别返回实际库存覆盖/库存缺口、清单已列入量及尚未安排量。清单数量代表采购意图，不代表已经入库或可食用。普通无来源采购按共餐优先级和合适保质期分配一次，有来源条目仅用于原餐次，不自动挪用。

已转库存的采购不再抵扣，避免与生成的库存重复计算。未知数量/单位、已购条目缺少日期、失去对应餐次的来源采购及来源采购超过原餐库存缺口时标记待核对，尚未安排量为null；不凭不完整信息自动加购。页面明确区分三种量，现有采购按钮仍使用总需求模式。

验证：服务端415项、客户端197项、PG独立空库全套、全项目静态、合约、架构、边界、模式和Web导出通过。两库验证来源采购覆盖15ml缺口、标记已入库后只按5ml库存计入且不再抵扣原采购；纯逻辑覆盖跨餐次普通采购只用一次、来源限制、已删除/已入库排除、临期采购优先、未知和富余来源采购需要核对。客户端验证库存与采购数值分开展示。

此阶段仍为核算预览，不产生持久库存/采购预留，也未按差额自动改写采购或考虑待吃餐。持久预留、并发释放、可审阅的净采购差额同步和设备验收等继续推进，全部issue目标保持进行中。

## #199 可审阅的净采购差额同步

家庭共餐预览新增当前餐次自动采购项的原数量/调整后数量。计算排除本餐尚未修改的来源采购，抵扣跨餐次分配后的实际库存和其他清单数量，避免把原总需求再次当成外部覆盖量。零差额移除本餐生成项；其他成员的手动采购和其他餐次来源项保持独立。已购买、已入库、删除或人工修改的来源采购，以及未知分配量，均要求先核对。

用户明确核对后可应用净采购调整。预览绑定完整相关库存、共餐安排、菜谱和清单快照及版本；写入事务重新读取并比较指纹，变化则409，不覆盖旧结果。SQLite使用同一事务，PostgreSQL净额操作使用SERIALIZABLE并将序列化冲突转换为可重试冲突。原请求重试返回已保存结果，不新增条目或再次递增版本。预览与写入共用各数据库的快照读取器；对客户端仅返回本餐调整项和不透明指纹，不公开其他个人餐次明细。总需求同步仍是明确标注的独立选项。

验证：服务端416项、客户端198项通过；独立空PostgreSQL数据库全套通过，包含净额并发重复提交。两库回归15ml来源需求/5ml库存调整为10ml、库存版本变化拒绝旧预览且不改清单、重复和不同模式复用请求保护、库存数量不变。纯逻辑覆盖300g来源需求/100g库存/50g其他采购生成150g、原来源排除、无须补买时移除、来源人工修改/购买/删除/入库保护及稳定指纹。客户端覆盖数量审阅、显式确认和不确定写入重试绑定同一快照。全项目静态、合约、架构、模式检查及Expo Web导出通过，未分配打包编号。

范围澄清：家庭待吃成品的持久留份与释放已在前阶段实现；此净额操作不新增原料硬预留，也不把成品折算回原料。库存后来消耗、已购后增减差额建议及家庭资源自动维护仍需后续处理。设备验收与其他issue尚未完成，全部issue目标保持进行中。

## #197 偏好解释与排序证据一致、显示真实余量

重新读取GitHub仍有34个open issue。本轮核对#197的可信事实及可解释要求，统一偏好排序和列表解释的证据筛选：仅使用近30天明确长期dislike的skip事件、同一幂等身份去重、排除暂停/撤销/临时原因，并为一次格式化固定同一时钟。数据库目前已过滤skip且保证幂等唯一；共享纯规则进一步避免解释函数与排序函数在边界输入及过期时刻发生口径差异，不改变三次信号门槛或扩大推断。

补充此前未展示的剩余事实：两库读取近期制作批次当前remaining_servings及updated_at，偏好页面显示当前待吃余量和更新时间，包括零余量。仍将制作、食用、丢弃和当前剩余区分开；余量本身不推断喜欢、厌恶或份量过大。缺失余量/更新时间的旧输入不伪造值，纠正后重新查询真实余量。

验证：服务端418项、客户端199项、独立空PostgreSQL全套通过。纯规则验证解释与排序使用同一组事件、重复/非skip/临时/撤销/暂停排除、顺序无关及证据到期；两库真实业务回归验证纠正后的余量与数据库一致，客户端覆盖零余量与更新时间展示。全项目静态、合约、架构、SQLite边界检查和Web导出通过。无数据库模式变更，未分配打包编号。

此轮未扩大口味推断为“吃过/买过就喜欢”。#197其他条目仍需结合真实入口逐项核验；其余规划、家庭、数据和发布issue继续推进，全部issue目标未完成。

## #197 对话中的明确长期菜谱口味

新增update_recipe_preference高风险动作，将用户明确要求长期记住的菜谱dislike或neutral纠正写入现有学习设置，而不是另建一份口味档案。OperationsAgent仅在长期意图门槛通过后提案；本次条件过滤掉。服务端读取并绑定提案生成时的偏好版本，不允许模型自行选择后续版本。菜谱必须当前公开可用，payload严格限制scope/recipeId/version/value，不能夹带健康硬约束修改。

SQLite/PG在原Agent执行事务内锁定/核对偏好版本，保存明确结论、更新时间和来源操作ID，保留暂停学习状态。重复执行返回原操作结果；用户在偏好页纠正或删除后，重放旧操作不会恢复旧结论。明确结论列表显示可追溯的已确认操作来源。沿用现有高风险审批流程；未新增自动写入或把食材购买推断为口味。

验证：服务端420项、独立空PostgreSQL全套、全项目静态、合约和架构检查通过。双库共享回归覆盖幂等、来源、旧版本拒绝、管理页纠正后旧操作重放、账号隔离以及同版本并发仅一次生效。策略测试覆盖高风险分类、拒绝本次scope、缺少版本、未知value及额外健康字段。最后收紧提案版本为服务端读取值后服务端静态再次通过。测试账号长度和越权文案断言按现有规则修正，未弱化业务校验。无数据库模式或客户端变动；SQLite边界基线更新。

本批补齐已识别菜谱的明确长期口味入口，不声称任意自然语言食材偏好、真实模型和设备全流程已经验收。全部issue目标继续进行，尚未推送或关闭issue。

## #105/#107 以当前制作/食用模型验证staging烟测

审计发布门槛发现deploy/staging-smoke.mjs仍只走兼容旧接口，将烹饪完成直接写摄入且未验证重试和份量。现录入三种合成食材，结构化部分扣减一项，制作两份但暂不食用，再实际食用半份、丢弃剩余一份半。重复制作、重复食用及后续重放旧制作请求都必须保持同一事件和当前余量；其他两项库存不变、原料只扣一次、实际摄入只有一条，未知营养不得猜测。公开菜谱目录仍单独检查可读，不借任意菜谱营养冒充测试餐营养。

保留注册令牌用于登录失败后的账号清理；失败报告增加cleanup结果，无法清理时不再静默声称已删除。HTTP例外仅允许显式启用的本机回环演练，其他候选地址强制HTTPS。新增脚本测试纳入test:unit/test:all，并更新部署文档。

验证：服务端421项、最终应用API回归107项、脚本失败清理/清理失败/拒绝非回环HTTP共3项通过；同一CLI在SQLite和独立空PostgreSQL真实HTTP API下通过，测试账号最终不存在。PostgreSQL全套也通过；其前序仓储夹具留下的模拟在途Agent任务在启动HTTP runtime前置为cancelled，避免恢复器误启动模型，生产恢复逻辑不变。服务端静态与脚本语法检查通过，无数据库模式或客户端变动。

外部证据（2026-09-12本机核对）：仓库STAGING_BACKEND_BASE_URL已设置为https://dietdigidose.top；本机curl的HTTPS握手失败、HTTP返回空响应，不能据此证明外部服务完全不可用，也不能证明staging验收通过。android-preview Environment存在四项preview签名Secret及摘要变量，不再按仓库级列表误判其缺失；未读取密钥值。已向用户询问现有运维入口和真机资源，期间继续本地实现。尚未运行外网写入烟测、部署、生成双端候选或真机验收，#105/#107等外部门槛和全部issue目标仍未完成。

## #106 PostgreSQL在线备份与隔离恢复工具

新增db:postgres:backup/inspect/restore及编译后postgres-backup入口。源/目标连接分别通过DATABASE_BACKUP_URL、DATABASE_RESTORE_URL传入；密码不放进进程参数，子进程诊断脱敏。备份要求负责人和完整候选SHA，使用pg_export_snapshot让custom archive和表计数共享同一在线快照。manifest保存字节数、SHA-256、源版本、迁移版本、快照时间和耗时；目录0700、文件0600，拒绝覆盖已有备份，缺失manifest或校验不符不视为有效备份。

恢复只接受没有已有关系对象的隔离目标，不提供clean/覆盖开关。先核对归档，再以pg_restore单事务/失败即退出恢复并核对所有表计数；不会删除源数据库或原备份。文档明确先恢复完整archive、不预先重复建表，媒体需单独备份，应用验证后才安排连接切换；归档恢复耗时不冒充完整RTO，RPO需要真实故障时间。CI固定Ubuntu24.04与PostgreSQL16客户端，避免漂移到不匹配的dump/restore主版本。

验证：独立PostgreSQL全套通过，新增两个临时库演练覆盖小数份量、用户归属、外键、源库后续修改、已有备份拒绝、非空目标拒绝、损坏归档拒绝及权限检查。服务端静态、构建和架构检查通过。另用编译后的CLI实际备份/inspect/恢复当前完整测试库（110表，migration77，Drizzle序号20），全部表计数一致；归档396259字节，SHA256为059fb1f9c1c79bc51240bba4e55932bd476a648122dfaef4d19ad94f8c733e7f。本地备份134ms、恢复及计数265ms，运行于PostgreSQL17.11，仅为本机测试数据结果。备份位于/tmp受限目录，未放入Git。

这补齐可执行的PostgreSQL工具，不等同于已完成真实staging空库/上一候选升级、线上备份、恢复后登录与媒体访问或实际RPO/RTO验收。外部资源信息仍待用户补充，其他本地可实现工作继续，全部issue目标保持进行中。

## #106 恢复后的真实HTTP业务验证与失败回滚

PostgreSQL集成演练进一步覆盖完整应用模式：在源API注册两个临时账户、赋予测试管理员角色、录入分属两人的库存、制作并实际食用一份，再在线备份；恢复到新数据库后启动独立API进程，以原密码重新登录。检查相同用户ID、库存小数值、待吃余量、摄入唯一性、管理员汇总及媒体引用。普通账号不得看到他人的库存/摄入或读取管理员统计。媒体只验证数据库引用保持一致，未声称对象存储文件已恢复。

源进程与恢复进程严格分开，不绕过既有checkpointer禁止运行中切换数据库的保护。额外在备份后把源库存改为99，恢复API仍读到快照中的2，证明没有误连源库。全部演练在临时隔离数据库进行，不连接外部staging。

失败恢复回归设置一个目标原有函数，与归档函数冲突，让pg_restore在创建新schema后失败；验证单事务回滚新schema/表，原函数仍保持旧结果。消除冲突后同一备份正常恢复，函数和数据均恢复到源快照。此测试直接覆盖命令失败，不只检查损坏文件被预检拒绝。

验证：独立PostgreSQL全套通过，报告增加postgresRecoveryHttpVerified；补充源/恢复快照区别后定向真实HTTP演练通过。服务端静态检查通过。首次尝试同进程重建runtime被既有保护拒绝，已改为独立进程；未放宽运行时检查。无产品代码、客户端或模式变更，不重复无关构建。真实staging、上一发布候选升级及实际RPO/RTO仍缺外部证据，全部issue目标继续进行。

## #171 preview证书寿命检查与已有签名证据

新增公开证书检查脚本，在CI恢复持久preview keystore后、构建前核对固定摘要、与release摘要隔离、当前有效且剩余不少于365天。keytool通过环境变量读取密码，只导出公开证书；不自动更换签名。原APK实际签名/包名/版本检查继续保留。三项证书规则测试加入test:release，包含未来、过期、临期、错误pin及误用release证书；公开测试证书无私钥，不代表实际preview证书。

读取两次成功GitHub构建34490933958和34143161394，日志均为HTTPS preview、包名com.dietdigidose.app.preview、签名摘要0c7b482180d86c3022a5295bdcfc02065b599ff4d5d96186149c52f07596590c，证明确有跨构建持久签名证据。完整引用与边界见docs/preview-signing-verification.md。不能把它当成当前开发提交的构建或真机覆盖升级证据。

验证：test:release共7项通过，客户端静态通过；未改应用代码，不重复Web导出。当前机器adb不在PATH，devicectl不可用，设备条件仍不足。未读取签名私钥、修改Secret、触发新构建或分配快照。实际证书的新增CI检查结果及真机覆盖安装仍待完成，全部issue目标保持进行中。

### PR #206 首轮 CI 与修复（2026-09-12）

- 已推送 `codex/all-open-issues` 并创建草稿 PR https://github.com/AdlinZ/DietDigiDose/pull/206；未关闭 issue，未合并或发布。
- 首轮 CI `34655862044` 的 PostgreSQL 16 集成、全套 Linux 测试、覆盖率和稳定性检查通过。主验证在 SQLite 演练失败：旧夹具只支持逆转迁移 59–62；Windows 首个共餐设置页面测试超过默认 5 秒。
- 补齐仅用于临时空白演练库的迁移 63–77 逆转夹具，保持未知迁移拒绝运行。不是生产数据库降级功能。重新本地执行演练，58→77 升级、备份恢复、回退与恢复后业务检查通过。
- 共餐设置测试首次加载 React Native 组件允许 15 秒，保留所有业务断言和后续测试默认时限；本地 9 项通过。Windows 结果等待新一轮 CI，不以本地结果替代。

### PR #206 第二轮 CI 与 Web 分包（2026-09-12）

- CI `34656459050` 已通过 Windows 全套测试、Linux 测试与覆盖率、PostgreSQL 16 集成、58→77 SQLite 升级/恢复演练和三端构建；稳定性任务成功。两平台最终都在包体积检查失败：Web 单文件 4,444,892 / 4,447,904 bytes，超过原 4,100,000 限额。
- Expo Router 插件仅启用 Web 的 `asyncRoutes`，原生平台保持同步加载。保留原预算；本地 Web 导出最大脚本 1,234,114 bytes、全部 JS 4,478,558 bytes、总量 6,287,914 bytes，全部通过现有预算。依据 https://docs.expo.dev/router/web/async-routes/；该能力仍为 Expo alpha，需继续关注 Web 路由加载回归。
- 本地浏览器验证导出静态站点：首页可见→点击全局搜索→搜索页可见→刷新搜索页仍可见，无浏览器脚本错误。未连接可用后端，未将此检查描述为完整业务验收。客户端静态校验通过；生产依赖审计仅报告仓库既有忽略项（2 high），无新增未忽略项。

### #186 推荐选择的持久化来源证据（2026-09-12）

- 新推荐快照保存 FEFO 实际匹配到的个人库存 ID、版本、分配数量与单位；只包含计算时的可用批次，不把过期或数量不明的库存当成已匹配。它是匹配证据，不是已扣库存或已完成闭环的证明。
- 推荐反馈校验当前用户的原请求、评分版本及快照中的菜谱成员关系，拒绝把后来新增或未推荐菜谱挂到原请求。来源读取独立于 24 小时分页有效期，允许保留先前周的选择来源。
- view/queue/start 事件中的 `selectionEvidence` 只由服务端快照产生；客户端同名 metadata 被丢弃。历史快照无库存证据时明确保存 null，独立于推荐请求的反馈不能伪造来源。
- 同一反馈幂等键的请求、菜谱、事件类型、版本、展示入口必须一致；普通重试和并发写入赢家都校验，避免另一条反馈复用先前的事件 ID。
- 服务端 423 项测试通过，包含真实 SQLite HTTP 的伪造来源、未推荐菜谱及键冲突检查；完整 PostgreSQL 集成通过（含并发重试与持久化来源），服务端静态校验、契约和架构检查通过。
- #186 尚未完成：还需要把这份选择证据贯穿到队列/制作/实际摄入，核对真实入库来源、环境与账号分类，补周统计重算、纠错、权限和管理端展示。没有用本次来源补充直接报告“周闭环用户数”。

### #186 推荐来源贯穿队列与制作（2026-09-12）

- 首页与库存推荐进入详情页时携带推荐请求 ID；详情页加入队列（含登录后返回）继续携带。服务端只接受当前用户原请求中确实存在的菜谱，将最小来源证据写入队列快照；独立选菜保持 null，已有队列不被后来来源覆盖。
- 制作成功事务将队列来源复制进成品 `result_json.selection_evidence`，与既有库存扣减结果、制作 ID 一起持久化。不新增客户端可写的证据字段，删除队列后制作重试仍返回原来源。
- SQLite HTTP 与 PostgreSQL 集成均验证推荐→队列→制作→删除队列→重试的来源保持，以及只制作不产生饮食记录；不将这种未食用、可能未扣原料的夹具计为有效闭环。
- 服务端 424 项、客户端 199 项测试，完整 PostgreSQL 集成、三端 lint、契约/架构检查、Web 导出均通过。此前 `bf4dcac` CI 的测试类型错误（读取 unknown.features）已在最终代码中修复并重新执行三端 lint；前一段记录中的 lint 是新增该断言前的结果，不代表该提交 CI 通过。
- `f44cf4b` 的 CI `34657004293` 已全部成功，证明 Web 分包在 Linux/Windows 均满足原预算，并通过数据库演练、稳定性与依赖审计。后续来源改动仍由新的 CI 验证。
- 周聚合、入库真实性分类、纠错重算、共享场景和管理端展示仍未完成，#186 保持开放。

### #186 周统计判定与缺失数据口径（2026-09-12）

- 新增 `coreLoopMetric` 纯业务事实判定：目标环境/真实用户分类、同一菜谱选择、已确认库存、实际数量扣减和本人有效摄入必须关联；家庭库存只能归属同一家庭，不能给所有成员计数。
- 按 Shanghai/IANA 周边界与首次有效摄入的服务端提交时间归周。制作 ID 去重、用户独立去重；撤销后重新选择首条存续摄入，支持跨周变化。历史来源不足返回 partial/null 和已核实下限，而非伪造 0。
- 7 组固定数据用例通过（包含多个排除原因），已纳入服务端单元命令。详见 `docs/core-loop-metric.md`；真实仓库投影、分类配置、管理员接口和展示尚未接线，未声明 #186 完成。
- 修复 CI `34657664564` 的 SQLite 清单差异：新增查询仅位于已有 cookingQueue/dietRecords 仓库适配器，每处新增 1 次访问，均有 PostgreSQL 对应实现；无域服务直连或新直连文件，清单仍为 72 文件。同步清单后边界校验通过。
- 本轮最终全量服务端 431 项测试通过，服务端 tsc、架构检查和 SQLite 边界检查通过。纯统计算法尚无数据库写入，因此不把纯函数重复调用当作“统计写入失败恢复”验收。

### #186 真实业务回算、管理员配置与周指标页面（2026-09-12）

- SQLite/PostgreSQL 仓库在一致快照内关联推荐来源、同次制作的库存日志及存续摄入，供管理员按上海自然周重算。独立埋点写入失败不影响业务事实回算；制作重试不重复计数，账号分类及摄入撤销会改变重算结果。
- 新增默认关闭、显式绑定 CORE_LOOP_ENVIRONMENT 的采集配置；恢复到不同环境后旧覆盖范围失效。账号默认 unknown，分类使用 CAS 并与审计同事务提交。管理员核对采用稳定数据库用户标识，仅返回最小制作/用户/原因信息，普通用户接口访问返回 403。
- SQLite 迁移推进至 78，PostgreSQL 新增 0020_black_siren；基线 107 表、170 外键。SQLite 58→78 升级/回滚演练与完整 PostgreSQL 集成通过。新增 SQL 仍限于已有仓库适配器，边界清单保持 72 文件。
- PostgreSQL 实测发现微秒库存时间与 SQLite 秒级选择时间比较会误判，现统一秒级比较；投影用例覆盖时间、浮点扣减对账、错误日志关联与撤销记录。
- 管理页增加周范围、覆盖状态、已核实下限、小样本提示、受控明细及账号分类。隔离本地合成数据库通过浏览器验证未采集/周中启用显示、1 人 1 次下限和分类读取；不作为真实内测人数或生产验收证据。
- 最终服务端 434 项、管理端 16 项测试通过；三端 lint、契约、架构、数据库基线/边界检查及服务端/管理端构建通过，构建预算未放宽。完整 PostgreSQL 集成日志为 core-loop-final-postgres.log，退出 0；SQLite 演练为 core-loop-rehearsal.log，退出 0。
- 前一提交 e72cbeb 的 CI 34658134603 三项任务已全部成功；本轮改动需由后续提交 CI 单独验证。
- #186 仍开放：当前已核实链路限于个人手动入库→库存推荐→队列→制作扣减→本人摄入。扫描/AI 等入库、家庭共餐及历史来源仍需补证据，缺失保留未知，未伪造完整统计。其他 issue 的真实 staging、设备、媒体及数据治理验收也尚未完成。

### #186 确认识别结果与购物入库的证据闭环（2026-09-12）

- 读取入库日志中服务端保存的 acceptance，确认图片/小票等统一入库路径现在可关联周闭环。只提取确认状态，不把完整识别 metadata 或原始内容带入统计。未经用户确认的自动接受、缺少确认标记的旧 AI 来源保持未知；不能因为 source=ai 就计入，也不再一概漏掉用户确认过的识别结果。
- 修复购物清单导入缺失创建日志：两种数据库均复用现有事务内创建方法。导入项目、创建日志和幂等响应共同提交，重复导入不重复生成日志。
- SQLite HTTP 参数化验收覆盖手动、图片确认、小票确认、购物导入四条真实链路及其制作重试、分类排除和撤销。投影测试明确排除 automatic/缺失/未知 acceptance。服务端最终 438 项测试和 tsc 通过。
- 完整 PostgreSQL 集成通过（core-loop-intake-postgres.log，退出 0），包含用户确认图片入库的完整周指标回算，以及并发购物导入只产生一条创建日志。数据库结构未变，SQLite 边界校验仍为 72 文件。
- 本次扩大了个人确认入库覆盖；家庭共餐来源与未保存必要证据的历史路径尚未补齐，#186 保持开放。上一提交 36949ca 的 CI 34659698903 此时仍在运行，不把本地验证替代 CI 结论。

### #186 共餐待核对批次去重（2026-09-12）

- 核对家庭制作与共餐计划代码后，确认目前只有实际制作扣减快照，没有可持久追溯的家庭库存匹配选菜证据；继续保留未知，不通过菜名或时间拼接闭环。
- 修复同一家庭制作的多条本人摄入被投影为多条待核对制作的问题：按制作批次聚合，保留全部摄入证据，未知条目只计一次；不同批次仍分别核对。
- 11 项周判定/投影测试及服务端 tsc 通过。新用例验证同一批两次摄入只产生 1 条未知、0 次已核实闭环，另一批不被合并。此改动没有把家庭路径标记为已支持。

### #66 厨具模糊映射与条件替代纠偏（2026-09-12）

- 复核发现 0.72 置信度的部分名称匹配被当作正式设备保存，且条件替代直接满足必需厨具，违背低置信度人工审核与安全限制要求。新建/修改仅对精确标准名称或别名保存 catalog_id，部分匹配保留原名称并进入待审核。旧无 catalog_id 项也不能通过部分名称获取设备能力。
- 实时模块与旧兼容评估器均仅将 equivalent 替代认定为满足。conditional 继续返回替代设备、时间/效果影响及安全说明，但保持必需约束未满足，不将未核实条件当作已通过。计划维护快照复用同一判定，新增对应回归。
- 推荐规则推进至 rules-2026-09-12.3，旧规则游标不再继续分页。SQLite HTTP、PostgreSQL 兼容查询和模块用例已更新以验证条件替代不自动放行。
- 服务端 440 项测试、tsc、SQLite 边界检查及完整 PostgreSQL 集成通过；版本调整后推荐模块测试另行通过。PostgreSQL 日志 kitchenware-safety-postgres.log 退出 0。无数据库结构变更。
- #66 尚未全验收：容量/热源等限制的结构化条件核验、旧错误映射治理与完整目录覆盖仍需继续审计。未将这次纠偏等同于所有条件替代已可自动执行。
- #186 主体提交 36949ca 的 CI 34659698903 已全部成功；后续提交分别由各自 CI 验证。

### #66 能力匹配不得绕过设备替代限制（2026-09-12）

- 修复在线厨具评估先匹配通用能力、从而绕过特定设备 forbidden/conditional 关系的问题。具备相同能力并不使这些设备关系失效；能力候选先排除禁止或条件未核实的替代设备，再判定必需条件。equivalent 仍可满足。
- 计划维护快照补齐替代关系读取并复用实时判定，不访问当前数据库。模块覆盖 forbidden/conditional/equivalent 三种关系；快照覆盖具备 steam 能力却仍需核对条件的设备；SQLite HTTP/PostgreSQL 真实兼容查询均给菜谱同时标注具体设备和 bake 能力，确认不能旁路。
- 推荐规则版本推进 rules-2026-09-12.4。441 项服务端测试、tsc、架构/SQLite 边界检查及完整 PostgreSQL 集成通过（kitchenware-capability-postgres.log，退出 0）。无数据库结构或 SQL 清单变更。
- #66 的完整属性与条件执行模型尚未验收，不将此限制优先级修复宣称为整个 issue 完成。#186 确认识别/购物入库提交 6755fb3 的 CI 34659909989 已成功。

### #66 目录审核状态参与能力判断（2026-09-12）

- 已保存的 catalog_id 不再绕过目录治理：实时服务只接受当前可信目录中的拥有设备；菜谱引用已撤回目录时也不能改走能力或替代关系自动满足要求。无目录映射的准确别名仍按可信目录解析。
- 新快照回归验证：可信设备可满足 steam 能力，设备撤回审核后不满足，设备恢复但菜谱要求目录被撤回时仍不满足。目录审核状态在既有快照指纹内，因此规则数据变化可被重规划识别。
- 服务端 442 项测试和 tsc 通过；推荐规则更新为 rules-2026-09-12.5，版本调整后推荐模块测试通过。没有改变仓库 SQL 或数据库结构，本次未重复 PostgreSQL 迁移演练。
- 本次只修复已撤回目录继续生效的问题；属性条件执行与历史错误映射治理仍未完成。共餐去重提交 a4a90b9 的 CI 34660012955 已全部成功。

### #98 上游修复与精确例外复核（2026-09-12）

- 通过 GitHub 两个 Advisory 页面及仓库只读检查脚本重新查询 npm 官方 registry：latest 仍为 2.0.2，GHSA-w3rx-r6r6-pgpr / GHSA-5p2g-fcmc-qvqq 的 patchedVersion 均为 null，受影响范围均为 <=2.0.2。未确认可用修复，不升级到虚构版本或删除例外。
- `pnpm -w audit:prod` 退出 0，但明确输出 2 high (2 ignored)，并非零漏洞；上游检查的 5 项既有测试通过。只读检查没有发布 issue 通知，输出存于 image-size-upstream-current.json。
- #98 的上游修复、去除例外及 Android/iOS 资源构建验收尚未完成；其他 issue 仍有可继续处理的代码工作，整个目标不标记阻塞。

### #66 菜谱入口的低置信度映射保持待审（2026-09-12）

- 发现菜谱投稿、管理编辑与旧导入写入器仍使用 0.7 接受阈值，部分名称匹配 0.72 会直接生效。现仅将精确名称/别名解析结果写为确认设备；管理员编辑不再从模糊设备猜测继承能力，明确填写的能力仍保留。
- 管理仓库的待审核判断同步阈值；保留原词和置信度，供后续审核。导入写入器新增断言，0.72 猜测只能进入 unresolved/review，不能落为正式要求。投稿及管理员模块用例注入实际低置信度解析结果，确认 catalog_id/capability 不被猜测补齐。
- 全量服务端 443 项测试、tsc 通过；最终调整的三个模块定向 10 项测试通过。无数据库结构、SQL 文本或接口字段变更，本次未重复 PostgreSQL 迁移演练。
- 复核当前管理端未找到完整的厨具映射审核处理入口，仍需实现；本次没有把“进入待审核表”视为“人工审核闭环完成”。#66 保持开放。

### #66 厨具映射审核接口与管理入口（2026-09-12）

- 新增受管理员保护的 GET/POST `/api/v1/admin/kitchenware/mapping-reviews`（提交路径带词条 ID），管理页 `/admin/kitchenware-mapping-reviews`。按 pending/approved/rejected 读取，单页最多 200 条；待审处理后刷新继续。
- 批准必须明确选择可信标准设备，并提示这是全局别名。原词与其他标准名称/别名冲突时拒绝；若来源为菜谱，只在当前仍使用该词条的 required/optional 角色中补映射。过期或删除来源不进行猜测修复，可明确拒绝审核。别名、来源要求、审核状态和审计同事务提交。
- 提交包含服务器生成的内容指纹；已处理或内容变化返回 409。PostgreSQL 按来源菜谱→审核行→目录顺序锁定，避免和菜谱写入反向加锁；并发批准仅一次成功。不将来源用户编号当作厨具项目编号批量改写资产。
- SQLite HTTP 验证普通用户读写 403、过期指纹拒绝、批准后真实别名/菜谱要求/审计、重试不重复、来源失效时不写别名及显式拒绝。PostgreSQL 完整集成通过（mapping-review-postgres2.log，退出 0）；首轮失败是 PG 测试夹具把大写 PG 原词当作标准化名称，已按实际标准化规则修正。
- 最终服务端 445 项测试、三端 lint、管理端/服务端构建、架构/SQL 边界及原构建预算通过。SQL 新增仅在两个既有 adminKitchenware 仓库，边界仍为 72 文件，无数据库结构变更。
- 本地隔离合成库通过浏览器核对登录、侧栏入口、待审显示、标准设备选项与布局；窄表格改为横向滚动。原生确认框阻塞预览工具后改为页面内二次确认，但浏览器批准按钮全流程尚未确认成功，不能将 API 通过等同于该项 UI 验收通过。临时 API/Vite 服务已停止。
- 前五批提交对应 CI 34660200992、34660350380、34660450489、34660564870、34660699921 均已成功；本轮审核入口需由后续 CI 单独验证。#66 的属性条件模型、旧错误映射治理与完整验收仍未完成。

### #66 审核页面批准与拒绝实测（2026-09-12）

- 重新启动隔离本地预览后，使用浏览器实际完成选择“烤箱”→批准为别名→确认全局别名→待审列表刷新→已批准列表出现原词；另一合成未知词完成拒绝并在已拒绝列表出现。页面内确认无需原生浏览器弹窗，本轮已补齐上一轮按钮交互验收缺口。
- 对隔离测试库核对：只有批准词进入烤箱别名，拒绝词没有进入；两条审核状态与两条审计均正确持久化。未操作真实数据。没有代码修改，因此本轮不重复全量构建和数据库迁移测试。
- 81c1ccf 已在本地提交，但推送时 GitHub 登录失效；本轮 `gh auth status` 仍报告 token invalid，尚未推送审核入口。已请用户自行重新登录，不索取令牌；其他本地工作可继续，整个目标保持进行中。

### #66 区分指定设备与纯能力要求（2026-09-12）

- 复核旧迁移发现菜谱要求可同时带设备 ID 和能力代码。旧 OR 判断会把明确指定设备降级为任意同能力设备，即使没有经过治理的替代关系。明确 contract 注释：catalogId 非空表示指定设备，能力为附加描述；catalogId 为空才是纯能力要求。
- 实时服务与旧兼容评估器统一语义：指定设备仅本体或 equivalent 可满足，conditional 保留待核对；纯能力仍按用户已登记的可信设备能力匹配。计划快照复用同一服务，不为用户推断未登记设备。
- 新回归覆盖同能力但无替代关系仍不满足指定设备，而改为纯能力后可满足；既有禁用、条件替代、目录撤回与别名用例继续通过。服务端 446 项、tsc、SQL 边界与架构检查通过。推荐版本 rules-2026-09-12.6，数据库结构/SQL 未改变，本轮未重复 PostgreSQL 演练。
- 容量/热源等属性条件的持久化与执行仍未补齐，#66 不标记完成。GitHub 认证尚待恢复，本轮提交继续保留本地。

### #66 用户厨具规格录入与持久化（2026-09-12）

- 复用已有 attributes_json 字段，新增共享严格 contract：容量（毫升）、直径（厘米）、兼容热源（燃气/电磁炉/电热）。未填写或 null 表示未核对，空热源列表表示明确不适用列出的热源；不从名称猜测规格。
- 用户厨具创建、修改、列表、保养响应返回结构化 attributes；SQLite/PostgreSQL 同步保存。旧客户端省略 attributes 时保留已有值，显式传入对象替换规格，可用 null 清除已核对信息。校验拒绝非正数、超范围、重复/未知热源和额外属性。
- 库存编辑器支持读取与录入规格，独立表单 hook 保持库存页面既有架构行数限制。检查 mutation/API 链路直接序列化完整 payload，没有丢弃新增属性。
- 服务端全量 447 项、客户端 199 项、共享 contract 8 项及 OpenAPI 检查通过；三端 lint、服务端构建、Expo Web export、架构与 72 文件 SQL 边界通过。原预算通过，客户端 JavaScript 4,482,623 字节。首轮客户端架构限制失败已通过提取表单修复，测试断言的类型错误也已修复。
- PostgreSQL 使用独立空库 issue66_attributes_20260912a 完整迁移/运行时/备份恢复演练通过，覆盖规格写入、省略保留和显式清空；无 schema 变更。日志 kitchenware-attributes-postgres.log。规格表单尚未进行浏览器/真机交互验收，容量和热源约束也尚未接入推荐条件执行，本次不宣称 #66 完成。
- GitHub 登录仍失效，本批提交保留本地，尚未进入远端 PR/CI。

### #66 能力条件执行与快照隔离（2026-09-12）

- 新增共享 kitchenwareCapabilityConstraintsSchema，定义 minCapacityMl、minDiameterCm、heatSource，条件须在同一件已登记设备上全部满足。未知/格式错误/未识别条件不授予能力，显式未知规格不能替代核对；空条件保留既有无额外规格要求行为。
- 实时 KitchenwareService 的纯能力要求改为读取完整目录能力关系，按拥有设备的 attributes_json 核对，不再只聚合能力代码。restricted/未知 safety_level 不自动授予能力；normal/caution 仍需通过其条件。指定设备与条件替代的既有语义保留，不把能力条件视为任意替代授权。
- 计划快照复用服务的同一条件执行器。新增测试发现 inputSnapshot 返回原数据引用，后续修改会使数据脱离创建时指纹；现深拷贝捕获数据，验证后续规格变化不会修改既有快照。
- 覆盖两设备不能拼凑规格、阈值边界、未知/不兼容热源、畸形条件、restricted 能力及 SQLite JSON 文本。SQLite HTTP 真实保存→兼容性响应验证，PostgreSQL 完整运行时验证 JSONB 条件/规格及不匹配阻断；独立空库 issue66_constraints_20260912a 完整迁移/恢复演练退出 0。
- 服务端全量 450 项、三端 lint、共享 contract/OpenAPI、架构/SQL 边界、服务端构建通过。无数据库 schema 或生产 SQL 变更；推荐版本 rules-2026-09-12.7。日志 kitchenware-constraints-tests.log、kitchenware-constraints-lint.log、kitchenware-constraints-postgres.log。
- 本批完成现有条件字段的执行基础，尚未补齐目录条件的管理编辑、历史错误映射治理、电器功能属性和条件替代完整验收。旧未被运行时引用的同步 evaluateKitchenwareRequirements 辅助函数也未统一此条件逻辑，后续需治理。#66 仍开放，GitHub 认证尚待恢复，提交仅本地。

### #66 管理员能力条件编辑（2026-09-12）

- 官方目录新增“能力条件”面板，独立读取并编辑标准能力、最低容量、最小直径、所需热源。修改经页面内二次确认，说明影响全局推荐；旧未知条件显示原值并阻止静默覆盖，须人工核对后移除/重新登记。目录基础信息编辑不改写能力关系。
- 管理员 GET/PUT /api/v1/admin/kitchenware/catalog/:id/capabilities 使用共享严格条件验证和内容指纹。未知能力、重复能力、无效条件拒绝；过期编辑返回 409。SQLite 事务和 PostgreSQL 目录行锁保护读取/替换关系/审计，审计保存 before/after，空数组可明确移除能力。
- SQLite HTTP 回归验证普通用户读写 403、非法输入不变更、实际保存、过期编辑、审计和清空。PostgreSQL 并发同指纹两次提交仅一次成功、另一次 409；未知能力不改变现有内容，审计一次。
- 旧 /tmp PostgreSQL 实例目录已不存在，连接拒绝后新建隔离实例 artifacts/issue-implementation/pg-adminconditions/data（127.0.0.1:55439），独立空库 issue66_adminconditions_20260912a 完整迁移/运行时/备份恢复演练成功。未复用失败迁移库。
- 服务端全量 451 项、三端 lint、管理端/服务端构建、架构检查和原构建预算通过。SQL 边界仍 72 文件；新增访问仅在已有 adminKitchenware SQLite repository（24→32），具备 PostgreSQL 对等实现，更新计数基线。没有数据库结构变化。
- 新面板尚未完成浏览器实际交互验收，不能以构建替代 UI 验收。条件替代、功能属性、历史映射治理等仍待处理；#66 不标记完成。本批本地提交，GitHub 认证此前失效尚无恢复证据。

### #66 单一能力判断路径及管理面板实测（2026-09-12）

- 仓库全局查证 evaluateKitchenwareRequirements 无调用方后，删除旧同步判断实现，避免保留绕过规格与安全条件的替代路径。移除已无生产调用的 capabilityCodesForCatalogIds 接口、SQLite/PG 实现和快照适配，测试夹具同步清理；现行服务继续读取完整能力条件。
- SQL 基线只减少旧访问，文件数仍为 72。服务端 451 项测试、tsc、架构/数据库边界检查通过；未改 schema，也未修改仍在运行的数据库 SQL，本轮不重复迁移演练。
- 使用隔离 mapping-review-qa.db 和本地服务，通过浏览器实际登录、打开平底锅能力条件、录入容量 3000 毫升/直径 28 厘米/电磁炉、核对修改、确认保存，面板关闭后重新打开显示三个已保存值。本轮补齐 7132126 新面板的基本保存/回读交互验收，未操作真实用户数据。
- 临时浏览器页已关闭，API/Vite 测试进程已停止。#66 其他未完成项仍按前述记录保留，不因本轮基础交互通过而关闭 issue。

### #179 修正“处理提醒”误写整组库存（2026-09-12）

- 对照原 issue 发现 notifications 的 complete 在 SQLite/PostgreSQL 中仍将关联整组 inventory_items.is_available 改为 false，不符合提醒完成与实际库存结果分离的要求。已移除此库存写入；complete 只更新提醒已读/完成状态，实际使用或丢弃需通过库存业务操作。
- 客户端按钮改为“处理提醒”，确认文案明确只结束提醒，并引导在库存记录实际使用/丢弃。服务端对已经 completed 的重复 complete 返回成功且不追加完成事件，PostgreSQL 仍使用行锁避免并发重复归因。
- SQLite HTTP 回归用两项合并临期食材，处理及重放后逐字段比较库存全部内容不变，提醒未读数归零且完成事件仅一条。PostgreSQL 完整集成验证库存仍可用、重放事件一次，独立空库 issue179_reminder_20260912a 迁移/备份恢复/运行时演练通过。
- 服务端全量 451 项通过；最后补强的整组库存用例单独通过。三端 lint、架构、72 文件 SQL 边界通过，SQLite 基线减少已删除的直接库存更新计数。无 schema 变化。
- 原始 #179 的主动候选模型、机会生成器、确定性策略、可恢复投递与完整类型化操作/归因尚未实现，不能把本次语义修复视为全部 issue 完成。未宣称客户端本轮浏览器/真机验证；GitHub 认证恢复前提交继续保留本地。

### #179 确定性干预策略基础（2026-09-12）

- 新增 interventions/policy.ts 纯决策模块，输入包含明确时钟、机会窗口、用户授权、时区/安静时段、设备授权、额度预留记录、数据观察时间、推荐质量、冷却/稍后提醒及晚餐任务状态。输出仅 push/inbox_only/suppressed，并带策略版本、原因、决策时间、本地日期和推送优先级。
- 策略版本 interventions-2026-09-12.1：推荐质量至少 0.7、数据年龄不超过 24 小时；临期机会剩余不超过 6 小时可 high，否则 normal；紧急不绕过安静时段。每日额度 0–3，冷却 60–10080 分钟。等起止静音表示未设置静音窗口，起点包含、终点不包含。以上是首期明确默认规则，后续接线须保留版本与事实输入供回放。
- 未开启、重复来源、失效/未到窗口、低质量、陈旧或未来数据、没有可用临期库存、晚餐已有安排/烹饪/明确不做饭、冷却及稍后提醒均阻断。额度不足/无推送授权设备/安静时段仅站内。额度计算按用户本地日统计已预留记录，包含待投递预留；上层仍必须在事务中原子预留，纯函数本身不保证并发安全。
- 六项策略回归覆盖确定性与输入不变、授权和业务条件、跨午夜及日间安静窗口、夏令时重复小时、上海零点额度、无设备、冷却/稍后提醒边界和优先级。全量服务端 456 项通过后补充第六项边界回归，六项定向通过；tsc、架构、72 文件数据库边界通过。加入 test:unit 门禁。
- 尚未接入运行时，不会自动发送通知。本批是完整主动流水线的策略实现；候选持久化/事务预留、机会生成、worker 投递、客户端类型化操作与归因仍为未完成项，#179 继续开放。无数据库或客户端修改，不重复迁移/UI 验收。

### #179 干预持久化模型与复合外键（2026-09-12）

- SQLite 79 / PostgreSQL 0021 新增 proactive_intervention_preferences、proactive_interventions、proactive_intervention_actions、proactive_intervention_outcomes。所有干预偏好默认关闭；候选默认 candidate/none，保存原始候选、策略输入/版本/原因、窗口、站内通知引用、预留额度时间、投递尝试/租约/不确定状态，为后续事务调度提供权威字段。
- 用户+source_key 唯一防止重复候选；用户+操作幂等键唯一；干预+结果类型+来源唯一。操作和归因使用 (intervention_id,user_id) 复合外键，禁止串到其他用户候选；无效状态/操作/时间窗口由 CHECK 拒绝。此处仅建立模型，不宣称已有投递恢复行为。
- 生成检查发现原 Drizzle 生成器把复合外键按列拆开，且同列多外键时省略引用。现按 SQLite foreign key id 分组，复合关系生成 foreignKey，单列引用保留。已重新生成本轮未发布迁移；实际 PG 跨用户操作/归因写入返回 23503，来源及操作重试返回 23505，验证生成语义正确。
- SQLite 用例验证默认关闭、跨用户限制、来源/操作/归因去重及非法状态；服务端全量 458 项、tsc、schema/Drizzle 一致性、架构检查通过。PostgreSQL 独立空库 issue179_schema_20260912a 完整迁移/备份恢复/HTTP 运行时演练通过，111 表、179 个 manifest 外键列记录（包含复合关系各列）。
- SQL 边界仍 72 文件，仅迁移文件新增一次 schema exec，更新计数。新表尚未接到 repository/API/worker，当前不会自动发送干预；原 #179 完整流水线仍待继续实现。本批保留本地提交。

### #179 用户干预偏好接口（2026-09-12）

- 新增共享严格偏好 contract：总开关、临期/晚餐分别授权、IANA 时区、安静起止、晚餐时间与提前量、每日额度和冷却。GET/PUT /api/v1/notifications/intervention-preferences 接入现有鉴权通知模块；无记录返回关闭状态/version=0，写入需匹配 version，过期返回 409。
- SQLite 事务、PG 用户行锁保证首次创建和后续 CAS。关闭总开关或一种类型时同事务取消匹配 pending 投递，保留原额度预留记录；sending 阶段不能撤回已发网络请求，后续 worker 必须在发送前重读授权并遵循相同锁顺序，此处不宣称已完成该阶段。
- SQLite HTTP 验证未登录 401、非法时区/时钟/额度/类型和冒充用户字段 400、账号隔离、默认关闭、启用回读、过期版本以及关闭类型取消 pending。全量 459 项中 458 通过，新增用例因自动拼接用户名超长失败，缩短夹具名称后该用例定向通过（issue179-preferences-targeted.log）；未改生产逻辑掩盖注册约束。
- PG 独立空库 issue179_preferences_20260912a 完整集成/迁移/备份恢复演练通过，包含同版本并发仅一次成功与关闭取消。三端 lint、共享 contract/OpenAPI、架构与 SQL 边界通过；仍 72 文件，新增 SQL 位于既有 notifications 仓库且 PG 对等，更新计数。
- 尚无客户端设置表单；机会生成、决策原子预留、worker 投递与归因仍待接线。当前不会产生智能推送，#179 保持开放。新增提交仅本地。

### #179 客户端干预偏好设置入口（2026-09-12）

- 设置页增加“主动食物提醒偏好”，独立 route-only app/intervention-settings.tsx 指向 Screen 容器页面。支持总开关、两类授权、时区、静音窗口、晚餐时间/提前量、每日额度和冷却设置；明确提示当前服务仍在准备，只保存偏好，尚不发送智能提醒。
- 新类型化 API 在请求和响应边界复用共享 schema；保存带版本号，409 后锁定本页保存并提示重新加载。加载/保存的 generation 隔离账号切换和离开页面后的旧结果；加载失败可重试，保存失败保留本页输入。数字框可暂时留空编辑，保存时不把空值静默转换为每日额度 0。
- 客户端 199 项测试通过，最终类型/ESLint、Expo Web export、架构与原构建预算通过；Web JavaScript 4,491,378 字节，仍在 4,500,000 上限内，后续功能需关注余量。无服务端/schema变更，不重复 PostgreSQL 演练。
- 新页面尚未浏览器/真机交互验收，已有单元测试不能代替实际表单验收。调度、原子额度预留、机会生成、投递和归因仍待实现，#179 保持开放。本批提交仅本地。

### #179 两类机会生成器（2026-09-12）

- 新增纯机会生成器，接收已授权偏好、账号库存快照和经过现有推荐引擎硬约束的候选。临期按今日至今日+3日包含边界合并本人可用、未删除、有正余量的食材；排除过期/无效日期，要求至少一条高质量菜谱分配到关联库存。
- 晚餐机会在当前本地日设置时间前的提前窗口内产生，需要开启晚餐授权且无已安排/进行中/今日不做饭状态。两类建议均采用固定文案、原因、失效标签及类型化操作清单，不嵌入食材名称或健康内容到锁屏摘要。
- 来源键按类型+用户+本地日稳定，同日重复扫描、顺序变化和重启不改变候选 ID。菜谱质量>=0.7、硬约束通过、按质量/ID稳定排序去重最多三项。临期机会截止最早到期食材次日本地午夜，复用已有时区 occurrence 处理夏令时，不用固定 24 小时替代本地日。
- 四项回归覆盖账号/可用性/余量/日期过滤、稳定身份与输入不变、推荐关联/质量/硬约束、晚餐窗口/授权/已安排及 23 小时夏令时日期。服务端全量 463 项、tsc、架构通过；最后补强输入不变断言后四项定向通过，加入 test:unit。
- 仍是待接线的生成器，尚未从 repository 读取实际快照或调用推荐引擎，也未持久化候选。凌晨晚餐的提前窗口可能跨前一日，当前按本地今日生成，跨日目标日及对应计划状态仍需完善。原子预留、worker投递、客户端卡片与结果归因继续未完成，不宣称 #179 完成。

### #179 跨午夜晚餐窗口与远端同步恢复（2026-09-12）

- 晚餐生成改为核对今日/次日的目标用餐日，来源键、localDate、截止说明均绑定实际用餐日。输入改为日期限定的计划/不做饭状态；缺失或重复冲突的日期证据不推断为“没有计划”。正在烹饪仍整体阻断。
- 回归验证 23:45 为次日 00:30 生成机会，跨午夜 00:15 保留同一候选 ID，目标日期明确不做饭或缺少状态时不生成。五项生成器测试、服务端全量 464 项、tsc、服务端构建及原预算通过，无 schema/生产SQL变化。
- gh auth status 已确认认证恢复，fetch 无分支分歧；13 个积压本地提交 81c1ccf..682da27 已推送到既有 draft PR #206。远端开放 issue 重新读取仍为 34 个，无新增 >205 的 issue。后续提交/CI 另行核对，未将远端同步当作全部验收完成。

### #179 原子决策与额度预留（2026-09-12）

- 在现有 notifications 仓库新增 reserveIntervention，输入为内部生成器候选和服务器观察事实；读取数据库当前偏好、活跃设备、最近七日决策/预留，调用统一策略，在同事务内建立候选、完整回放输入、决策版本/原因、站内通知与 pending 投递预留。
- SQLite 事务与 PG 用户行锁保证来源去重、额度检查及写入原子性；PG 与偏好修改使用同一用户锁。重复 user/source_key 直接返回已有记录，不改写历史策略、不重复建立通知，也不额外消耗额度。额度统计包含已预留的待发送任务；七日历史覆盖最大冷却与本地每日额度范围。
- 两数据库共用回归断言：同来源并发返回相同候选/通知、冷却抑制、两小时后额度不足仅站内、关闭取消 pending、新来源受未授权抑制。SQLite/PG 均确认只建立两条站内通知。历史 push 决策在 feature flag 关闭的重放中仍保留原结论，发送器必须依据当前状态复核而非重放历史决策发送。
- 服务端全量 465 项、tsc、架构通过；PG 独立空库 issue179_reservation_20260912a 完整迁移/恢复/运行时演练通过。SQL 仍在现有 repository、PG 对等，边界保持 72 文件并更新计数。无 schema 变化。
- 本批仓库方法尚未由 worker 调用，未产生真实智能通知；投递租约、发送前复核、实际机会数据读取和推荐调用、操作与归因仍待实现。先前 head 2b2a234 CI 34676878544 本轮复核仍 in_progress，不宣称通过。

### #179 投递领取、过期租约及 CI 演练补齐（2026-09-12）

- 新增 claimIntervention/finishIntervention 仓库方法。领取前读取最新开关、类型授权、有效期、安静时段与活跃设备；不符合条件取消 pending，失效候选标 expired。领取设两分钟租约并增加尝试计数，完成须持有未过期 owner，accepted 才标 sent。
- sending 租约过期转 uncertain，不再自动领取重发，避免进程在 Expo 已收请求后崩溃造成重复通知。这是显式不确定结果，不能宣称 exactly-once 外部投递或保证所有不确定任务最终送达；后续 sender/worker 需支持该状态的观测与处理。
- SQLite/PG 共用用例覆盖并发仅一次领取、错误 owner 拒绝、一次完成、过期后旧 owner 拒绝、不确定任务不重发、功能关闭取消及过期候选。服务端 465 项、tsc/架构通过；PG 空库 issue179_delivery_20260912a 完整集成/迁移/恢复演练通过。SQL仅既有通知仓库，72 文件边界计数更新。
- 前一推送 session35246 最终因 HTTP2 framing error 退出128；确认终止后使用 HTTP/1.1 重试，5d6684f 已成功推送。CI 34676878544（2b2a234）verify失败，stability/windows成功，失败原因是数据库演练未支持迁移79回退夹具。
- 已为仅隔离演练使用的旧版本夹具添加79反向删除顺序（outcomes/actions先于候选/偏好），扩展支持版本列表；不作为生产 downgrade。运行 pnpm --dir server db:rehearse -- --owner=issue179-validation 退出0，完整 fresh/upgrade/rollback旅程通过，日志 issue179-rehearsal.log。
- 当前仅实现仓库投递状态机，尚未接入worker与实际Expo请求，也未完成结果归因，#179仍开放。本轮修复需远端CI重新验证。

### #179 实际发送与 worker 接线（2026-09-12）

- notifications worker 现会有界扫描待发送用户，并清理发送租约超时记录；SQLite/PG 提供同语义查询。实际发送调用既有 Expo 通道和 ticket/receipt 归档，保留策略的 normal/high 优先级，载荷包含候选与站内通知 ID。旧通知默认 high 行为保留。
- PROACTIVE_INTERVENTIONS_ENABLED 必须精确为 1 才领取发送，默认关闭并补充环境示例。领取仍读取最新授权、安静时间和有效期。所有设备批次共用 60 秒请求截止，低于两分钟领取租约；每批请求前检查 worker 所有权、取消信号和功能开关。
- 全部有效 ticket 接受才 accepted；全部明确 error 为 failed；空响应、缺失/混合 ticket、网络或归档异常为 uncertain。完成写入仍校验领取 owner/租约；worker 单独记录 accepted/failed/uncertain，不能把不确定发送算成功。不确定任务不自动重发，未作外部 exactly-once 保证。
- 定向测试用模拟 fetch 验证普通优先级、载荷关联、成功/拒绝/空响应/断连归档、功能关闭和租约失效阻断网络；没有发送真实 Expo 推送。两数据库共用断言增加 pending 扫描、有效 sending 排除及过期 sending 纳入。
- 服务端全量 466 项、client/server/admin 静态校验、架构边界和 SQL 冻结检查通过；PG 空库 issue179_sender_20260912a 完整迁移/备份恢复/运行时集成通过。仅现有通知仓库增加 SQL，72 文件边界计数更新。前一 head 7c0b647 的 CI 34677309285 已确认 success。
- 尚未接通实际库存/推荐快照的机会扫描、类型化卡片操作和结果归因。当前 sender 能处理已原子预留的候选，不会自行生成智能通知；#179 仍未完成，不关闭 issue。

### #179 真实机会扫描与推荐快照接线（2026-09-12）

- worker 的通知服务注入现有 RecommendationsService；扫描显式启用的账号并分页读取偏好、本人有效库存、现有计划与活跃烹饪队列，运行机会生成器，再调用原子预留。feature flag 关闭时不扫描；单账号读取/生成失败计入 worker 失败指标，其他账号继续，取消/租约失效则停止。
- 推荐仍经过原引擎质量/权限/过敏/时间/厨具硬约束，并要求已知数量足够、无缺料与结构化原料；从 FEFO allocation 提取库存关联。排名分数按原引擎正向最高90分归一化，作为确定性质量阈值输入，不宣称为成功概率。未新增模型调用。
- 推荐计算支持显式本地日期；新入口把账号时区日期用于过期库存筛选及当天营养记录，其他调用保留原默认日期。快照记录读取开始时间，完成后重新检查窗口及策略新鲜度。
- 晚餐状态兼容存储中的中文“晚餐”及英文值、current/saved 草稿（含仅分配备餐、不新做饭的安排）、带时间的队列以及跨日不做饭日期。正在准备/烹饪阻断；未注明日期的活跃队列或无法解析的活跃草稿不能证明未安排，保守阻断。
- 回归发现并修复偏好 DTO version 导致严格生成器拒绝、PG 新偏好布尔语义实际是整数列的比较问题。测试夹具改用规范库存单位 piece，验证实际推荐引擎产出而非伪造候选。两数据库共用分页用户/队列查询断言。
- 最终全量服务端470项、client/server/admin静态校验、架构与72文件SQL冻结检查通过。PG首次空库扫描用例因整数/布尔比较失败；修复后独立空库 issue179_scan_20260912b 完整迁移/恢复/运行时集成通过。前一 head 61e356d 的 CI 34677845779 success。
- 仍保持默认关闭；类型化客户端操作、结果归因与监控/回滚完整验收尚未完成。当前扫描分页从头开始，仍需完善大账号量下的持久扫描进度和任务隔离，避免长扫描影响已有待投递任务；默认每小时 worker 周期也需要适配最短15分钟晚餐窗口。此提交不是 #179 完成声明。

### #179 扫描/投递任务隔离与部署频率（2026-09-12）

- 从旧 notifications 任务拆出 intervention-scan 与 intervention-delivery，各自使用现有 worker 任务租约、取消、运行结果和错误监控；任务名称集中定义并供 CLI、管理查询复用。默认任务列表先处理待投递记录，扫描失败不会阻止后续单独任务执行。
- staging compose 增加两个独立干预进程，分别运行单项任务，50秒超时/两分钟租约/完成后一分钟间隔；原 worker 明确保留通知、媒体清理和计划维护，每小时运行。代码在选择任何干预任务时默认一分钟，否则保留一小时；显式 WORKER_INTERVAL_MS 仍优先。
- 运行手册补充新任务查询、环境重载方式以及关闭行为：feature flag 默认0，关闭后停止新扫描并取消 pending 投递，保留站内/历史数据，已提交 Expo 请求无法撤回。未启用或部署真实服务，类型化操作未完成前仍要求保持关闭。
- 任务回归覆盖扫描抛错后投递照常执行、普通提醒不会被误调用、发送失败/不确定不算成功及默认频率；HTTP 管理查询验证两种任务筛选可读取持久化记录。服务器全量471项、三端静态校验、架构/SQL冻结通过；compose 使用示例环境 config --quiet 通过。此批无生产 SQL/schema变化，未重跑PG全量迁移。
- 持久扫描游标、大账号量时效、客户端操作和结果归因仍未完成；独立进程解决扫描阻塞投递，不代表已验证所有账号一分钟内扫描完毕。前一 head 8a274bf CI 34678284104 复核仍 in_progress，未提前宣称成功。

### #179 持久扫描游标与重启续扫（2026-09-12）

- SQLite迁移80、PG0022新增专用单行扫描游标，after_user_id不设用户外键，删除账号不会破坏扫描位置。每批最多25账号，逐账号处理结束后推进，末尾回绕0；生成失败计入失败并推进，任务取消/租约失效则停止，尚未提交位置可在重启后重扫，候选来源唯一约束继续避免重复预留。
- 写游标校验预期旧位置与当前 intervention-scan 租约。SQLite同写事务检查，PG锁租约行并在更新时再次检查clock_timestamp有效期，旧owner或陈旧位置不能覆盖新的进度。feature flag关闭时不读取/推进扫描。
- 两数据库共享测试覆盖首次位置、未获租约拒绝、正常推进、CAS冲突、接管后旧owner拒绝、回绕；服务层模拟30账号验证一次25个、重建service后接续最后5个，并拒绝失去所有权的检查点。新增80回退夹具，未改生产downgrade策略。
- 全量服务端472项、三端静态校验、架构及SQL冻结通过；PG独立空库 issue179_cursor_20260912a 完整迁移/恢复/运行时集成通过，db:rehearse --owner=issue179-cursor全旅程退出0。基线112业务表/179外键列记录，Drizzle116表；SQL仍在既有仓库/迁移文件，边界72文件计数更新。
- 前一提交8d7de70的两次push均以网络超时128终止，gh API确认远端仍8a274bf；不存在仍在运行的旧推送，本批可与前批共同同步。尚未验证大账号量吞吐或单账号长读取恢复，通知操作与结果归因仍待实现，不关闭#179或全部issue目标。

### #179 单账号读取超时与取消恢复（2026-09-12）

- 将偏好/推荐/队列快照包在独立10秒异步读取截止中，截止前后检查取消信号；超时记失败并保存该账号位置，随即结束本批，下一轮从后续账号开始，避免同批不断积累迟到读取。下次完整回绕仍会重试该账号。
- 读取段仅返回数据，候选预留在截止成功返回后执行；超时或 worker 取消后的迟到结果不再写入。worker 取消/失去租约仍抛出，不推进游标。手册明确此机制不取消数据库已执行语句、不打断同步计算，资源限制与容量验证仍待完成。
- 新回归覆盖慢账号超时后续扫、迟到的有效临期/推荐结果不生成候选、父任务取消不推进游标或触发后续计算。服务端全量474项、三端静态校验、架构/冻结检查通过；补强迟到有效结果断言后9项通知定向测试通过。此批无SQL/schema变化，未重复PG演练。
- 普通git推送本轮再次以连接github.com:443超时失败；使用GitHub Git Data API同步既有两个已验证提交，脚本逐blob/tree/commit核对本地SHA，最终ref只允许fast-forward并复核远端未变化。写本文时脚本仍在运行，尚未声称远端同步成功。

### #179 用户可见干预卡片接口（2026-09-12）

- 新增共享 interventionCardSchema 与类型化操作枚举；认证接口 GET /api/v1/notifications/interventions/:id 从账号限定的仓库读取已发至站内的候选，非法ID400、未登录401、其他账号/无站内通知的抑制候选404。通知 history 增加 interventionId 关联，并匹配通知与候选相同user_id。
- 返回字段显式投影为标题、正文、为什么现在、失效说明/时间、食材/菜谱ID、可见操作、决策原因和版本，未返回原始policy_input_json、账号字段或内部候选扩展。SQLite字符串JSON/日期与PG对象JSON/Date输出一致。
- 读取时按当前时钟将已失效的未处理卡片显示expired并清空操作；已acted卡片也无待执行操作，不反写历史决策。后续实际动作仍须在事务中重新校验有效期/归属/确认，不能依赖客户端显示状态授权。
- 服务端全量475项、三端静态校验、架构和72文件SQL冻结通过；PG独立空库 issue179_card_20260912a 完整集成/迁移/恢复通过。共享仓库断言覆盖本人读取、跨账号拒绝、抑制不暴露和history关联；HTTP与投影测试覆盖错误码、敏感字段排除、驱动一致及时间边界。无schema变化。
- GitHub API已逐对象/提交哈希验证并成功同步8d7de70、7b2f457、1b6afef，远端及本地追踪指针确认到1b6afef。对应CI 34679231717和34679178612本轮仍运行中，不提前宣称通过。API恢复同步不等于本卡片提交已推送。
- 本批是客户端数据接口，尚未接页面、类型化写操作与结果归因；默认feature flag仍关闭，#179及全部issue目标未完成。

### #179 客户端卡片与推送入口（2026-09-12）

- 新增仅路由导出的 intervention 页面，使用 Screen、主题tokens及安全路由。通知中心识别主动干预类型并进入其interventionId；Expo payload解析仅接受64位十六进制ID，缺失/非法时返回通知中心，COMPLETE等推送标识不会直接写库存。
- 页面显示原因、失效说明、已处理/过期提示和关联菜谱，复用菜谱详情API读取标题，可查看菜谱与当前库存。加载失败可重试；账号/路由key变化重建页面，token限定的data/error及effect清理避免旧账号迟到响应覆盖当前内容，过期提示每30秒更新。
- 修复React规则发现的渲染时Date.now及effect同步清空状态，改为惰性初始化、派生校验与重试事件重置。组件测试验证账号切换后旧私密响应丢弃、错误重试和过期提示；push路径测试覆盖有效/无效ID与不执行库存动作。
- 最终202项客户端测试、tsc/ESLint、Web export及原有构建预算通过；JavaScript4,498,039字节，距4,500,000上限仅1,961字节，后续增量需要实际减重而非抬高阈值。未做浏览器/实体设备视觉验收，本批不能据组件测试宣称视觉验收完成。
- 699d7de已通过GitHub API按哈希同步，远端及本地追踪指针一致；7b2f457 CI34679178612和1b6afef CI34679231717成功，699d7de CI34679407510仍运行中。本客户端提交的同步另行确认。
- 页面本轮仅提供读取/浏览入口，plan_recipe、库存使用/丢弃、稍后提醒等类型化操作及归因继续待实现，默认flag保持关闭，未关闭#179。

### #179 类型化反馈确认与重放规则（2026-09-12）

- 新增 snooze/not_cooking_today/not_helpful 的严格请求/结果契约，要求confirmed=true和UUID幂等键，拒绝用户ID等额外字段。纯规则先检查可见卡片、操作清单、状态与失效；事务接线需先查账号限定幂等记录，已确认操作在卡片后来失效后仍可重放，相同key换卡片/动作返回冲突。
- 稍后提醒生成次日本地安静时段结束时间；未启用安静时段时取次日07:00。复用现有时区occurrence处理夏令时，无法定位本地次日返回明确冲突。跨午夜晚餐“不做饭”绑定卡片目标用餐日，不误写当前公历日。not_helpful仅返回反馈意图，不推断全局关闭、不改库存。
- 服务端全量479项、三端静态校验、架构及Web export/预算通过；补充not_helpful输入不变断言后5项反馈定向测试通过。客户端JS4,498,656字节，剩1,344字节，下一步客户端动作仍需先解决包体余量。无SQL/schema变化。
- 这是待事务使用的规则与确认契约，尚未持久化反馈或开放API。接线必须同事务更新动作记录/偏好/候选/站内通知并取消pending；已sending的结果完成不得把acted覆盖为sent。snooze等待期间不得永久预留次日来源为suppressed，否则到时无法提醒，需要同时补扫描与原子预留的延后机制。
- b189a7b的API同步在创建tree请求时60秒超时退出1，尚未更新分支；gh API重新确认远端699d7de，无仍在运行的旧同步。已上传对象可安全复用，后续重新校验哈希再fast-forward；本轮新规则提交同样待同步。

### #179 反馈事务与认证接口；按用户要求收尾（2026-09-12）

- 将 snooze/not_cooking_today/not_helpful 接入 SQLite 与 PostgreSQL 事务：账号限定的幂等重放、候选和站内通知已处理状态、偏好更新、pending 取消与动作记录一起提交。PG 使用账号行锁串行化同账号预留/反馈；重复确认不重复写入，相同幂等键改变动作返回409，跨账号404，未确认请求400。
- 扫描读取有效延后时间，原子预留同时复核延后与已保存的不做饭日期；延后中的来源返回 deferred、不永久消耗次日机会。推送回执只将 inbox 改为 sent，避免晚到回执覆盖 acted。
- 服务端全量480项测试通过；共享双数据库断言覆盖并发重放、延后结束恢复、持久化不做饭偏好阻止旧快照预留、发送中反馈与晚到回执。认证HTTP覆盖401/404/400/409与成功重放。三端静态校验、架构检查、72文件SQL边界及 git diff --check 通过。
- PostgreSQL17独立空库 issue179_feedback_20260912a 的完整迁移、仓库、备份恢复与HTTP运行时集成退出0。本批无schema变更、无客户端增量。
- 用户明确要求“把当前的任务做完就收尾了”，本次范围止于反馈事务/API/回归，不再展开客户端反馈按钮或其他issue。#179及原全部issue目标尚未全部完成；未部署、合并或关闭issue。提交同步结果以最终回复为准。
