# 26w37 P1 实施记录

基线：#207，181250784ee251278d5470397bf49a0ea3788d0d。范围：#62、#108、#171、#183、#187、#189–#194。

## 第一阶段：录音生命周期（2026-09-13）

会话创建、权限请求、原生录音启动都可能晚于退出返回。启动过程增加生命周期失效检查，迟到创建的远端会话会关闭，迟到启动的原生录音器会再次停止。静音后迟到的转写失败不再上传降级音频；旧转写不会阻塞新会话。重复启动合并；旧 Web 识别回调、原生中断回调和心跳失败不能改变新会话。AppState 非活动及 Web 隐藏/离页终止启动或监听。静音、停止先调用本地音频输出停止，再等待网络。

本地验证：全仓静态检查通过；客户端 58 套/229 项通过；服务端 486 项通过、1 项跳过。录音专项 12 项，使用录音器状态测试替身断言停止，并非 Android/Web 真机证据。

已有库存测试覆盖结构化新增/修正/部分使用/丢弃/用完、ID-only 拒绝、幂等重放、版本和单位冲突、历史与事务回滚。服务端全套包含制作、待吃餐、个人摄入和家庭共餐回归。这些结果不替代计划要求的跨入口实际验收。

#183、#189、#190 保持打开，尚需 Android/Web 实际回归以及完整实用场景证据。后续阶段和最终候选部署尚未完成。

## 外部验收条件

staging SSH 受控访问、独立媒体存储权限、语音测试账户及 Android 设备尚未在本任务中验证可用。不得把测试替身、模拟器或本地 PostgreSQL 结果当作这些验收。iOS 条件归 P0 #107 单列。

## 第二阶段：数据迁移与审核底稿（2026-09-13）

rc.7 原附件 SHA-256 校验通过。`datasets/base-data` 提供来源锁、无额外依赖的可复现构建器及 447/241/341 条记录的逐条证据盘点。重复构建输出 SHA-256 为 `908590ad4583f57f2ba070ee0f26ac76b73da62b0950fc354f1acc6d4121452d`。这些是字段和来源证据盘点，不冒充配方/许可人工审核或正式 v1 发布；所有记录仍需补证，执行资格保持 false。

SQLite 81 和 PostgreSQL 0023 正式接管 rc.7 临时业务字段，允许未知热量为 null；PostgreSQL 复制旧逻辑 ID 映射至公共受迁移管理的表。导入器不再执行 DDL。新增待吃餐执行保护，避免绕过旧 cooking_completions 触发器；家庭共餐校验同样尊重明确禁止执行标记。

验证：SQLite 迁移测试保留外键、管理员字段、索引、自增序列，失败时回滚并恢复外键开关；PostgreSQL 17 独立空库全套通过，另模拟 rc.7 已有字段和映射，验证接管及只读菜谱保护；厨具修复在真实 PostgreSQL 上通过。服务端 488 项通过、1 项跳过；模式一致性与服务端静态通过。

为支持已授权的双库增量迁移，SQLite 冻结检查仅对两个明确列出的 schema migration 文件豁免 DDL 计数，继续冻结全部运行时业务访问；迁移由模式一致性和双库测试约束，不对 storage 目录整体放行。

#187 剩余：配方/许可语义审核与缺项补录、完整 v1 数据构建与不可变发布、正式 v1 版本感知导入，以及 staging 脱敏副本全量升级/重复导入验收。当前原始归档仍须预先存在 base_data schema，尚未提供完整的双库数据包导入路径。不得关闭。

迁移 81 已补齐数据库演练旧库夹具：仅在演练新建的数据库上还原旧表结构，并拒绝对含未知营养的库执行该回退。`db:rehearse -- --owner=codex-p1` 通过，覆盖空库、旧候选升级、独立恢复及安全备份回滚后的登录、隔离、库存、摄入、媒体引用与管理汇总。

## 第二阶段：数量依据延续（2026-09-13）

扫描入库的数量依据现在随明确的库存事务延续到新版本：保质期/存放位置/名称修改不丢失数量依据；精确消耗保持 known/estimated/unknown，估计值不会被升级为已知。人工数量修正记录用户依据，改成未知数量保持 unknown；不带依据的旧记录继续兼容。绕过事务直接改版本仍使旧依据失效。

SQLite HTTP 回归覆盖人工修正、改名和部分消耗后的推荐数量与历史版本；PostgreSQL 覆盖估计入库、改存放、部分扣减、人工修正和无依据版本变化；纯函数验证旧版本、缺失值及不提高可信程度。全套服务端（新增纯函数测试前）488 通过、1 跳过，新增纯函数测试通过；静态、架构和数据库访问边界通过；PostgreSQL 独立全套通过。#191 仍需重启/弱网与真实照片重叠场景验收，非数量字段的完整依据展示仍待核对。
