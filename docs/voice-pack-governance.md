# 本地个人音色包发行与回归

音色训练只允许在受控电脑或训练服务中进行；移动端只下载并运行已导出的 ONNX 包。仓库不得包含原始录音、训练中间文件、私有权重或访问密钥。

## 发行门槛

每个 `VOICE_PACK_CATALOG_JSON` 条目必须记录：`voiceId`、语义版本、`zh-CN`、采样率、最低应用版本与内存、模型和词表资源、逐文件大小与 SHA-256、HTTPS 地址、许可证链接、可核验的说话人授权记录，以及“模型安装到终端后可能被提取”的风险说明。模型路径和词表路径必须同时出现在受控资源清单中；服务端会拒绝路径穿越、HTTP、缺失摘要、重复资源或已撤销版本。

训练代码、基础权重、运行库、说话人录音与最终模型分别做许可证审查。授权记录至少包含说话人身份确认、允许的产品与发行范围、撤销方式和日期。撤销版本加入清单的 `revoked: true` 后，客户端下次同步会停止并删除本地模型。

## 固定中文回归集

在目标 Android 中端机和 iOS 验证机上试听并记录：

- 水温八十五摄氏度，加入二百克番茄和十五毫升清水。
- 大火加热两分钟，再转小火炖十分钟。
- 鸡胸肉、土豆、西兰花、葱姜蒜和生抽各适量。
- 烤箱预热到一百八十摄氏度，烘烤二十五分钟。
- 现在要暂停计时吗？还需要增加三十秒吗？

首次安装或版本更新后记录模型加载时间、首段延迟、实时系数、连续十句稳定性和峰值内存。设计阈值为首段不超过 2.5 秒且实时系数不超过 1.2；明显超过阈值时客户端自动降级。最终发行验收必须附设备型号、系统版本、包版本、指标与听测人结论。

## 客户端行为

默认 Wi-Fi 下载；移动网络必须再次确认。下载写入暂存目录，逐文件校验后再原子切换，成功前保留旧版本。朗读严格按“本地 ONNX → 服务端 TTS → 系统语音”降级；同一时刻只保留一个播放代次，插话、离开做饭页和退出账号会立即取消当前输出。

公共步骤与固定提示可缓存；动态健康回答使用容量受限的短期缓存，缓存键包含音色版本、规范化文本、输出参数、schema 和账号范围。普通“清理缓存”不删除模型；删除音色包时用户可另选是否同时删除生成音频。
